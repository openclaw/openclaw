services:
  openclaw-gateway:
    image: ${OPENCLAW_IMAGE:-openclaw:local}
    environment:
      HOME: /home/node
      TERM: xterm-256color
      OPENCLAW_GATEWAY_TOKEN: ${OPENCLAW_GATEWAY_TOKEN}
      OPENCLAW_SKIP_BROWSER_CONTROL_SERVER: ${OPENCLAW_SKIP_BROWSER_CONTROL_SERVER:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      VENICE_API_KEY: ${VENICE_API_KEY}
      NVIDIA_API_KEY: ${NVIDIA_API_KEY}
      BRAVE_API_KEY: ${BRAVE_API_KEY}
      ELEVENLABS_API_KEY: ${ELEVENLABS_API_KEY}
      PYTHIA_BASE_PRIVATE_KEY: ${PYTHIA_BASE_PRIVATE_KEY}
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE}
      AGENTGLOB_RUNTIME_URL: ${AGENTGLOB_RUNTIME_URL:-}
      AGENTGLOB_RUNTIME_TOKEN: ${AGENTGLOB_RUNTIME_TOKEN:-}
    volumes:
      - ${OPENCLAW_CONFIG_DIR}:/home/node/.openclaw
      - ${OPENCLAW_WORKSPACE_DIR}:/home/node/.openclaw/workspace
    ports:
      - "${OPENCLAW_GATEWAY_PORT:-18789}:18789"
      - "${OPENCLAW_BRIDGE_PORT:-18790}:18790"
    init: true
    restart: unless-stopped
    # Per-container caps (channel-error-hardening B3): a looping/leaking agent
    # must not drag the whole host into swap. Steady-state RSS measured
    # 2026-06-10 across 24 prod gateways: 248–440 MiB (boot spike ~620 MiB),
    # so 1g is ~2.3× steady headroom. cpus=1.0 leaves a full core free on the
    # 2-vCPU US host even if one agent pegs its allowance.
    mem_limit: 1g
    cpus: 1.0
    command:
      [
        "node",
        "dist/index.js",
        "gateway",
        "--bind",
        "${OPENCLAW_GATEWAY_BIND:-lan}",
        "--port",
        "18789",
      ]

  openclaw-cli:
    image: ${OPENCLAW_IMAGE:-openclaw:local}
    environment:
      HOME: /home/node
      TERM: xterm-256color
      OPENCLAW_GATEWAY_TOKEN: ${OPENCLAW_GATEWAY_TOKEN}
      OPENCLAW_SKIP_BROWSER_CONTROL_SERVER: ${OPENCLAW_SKIP_BROWSER_CONTROL_SERVER:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      VENICE_API_KEY: ${VENICE_API_KEY}
      NVIDIA_API_KEY: ${NVIDIA_API_KEY}
      BRAVE_API_KEY: ${BRAVE_API_KEY}
      ELEVENLABS_API_KEY: ${ELEVENLABS_API_KEY}
      PYTHIA_BASE_PRIVATE_KEY: ${PYTHIA_BASE_PRIVATE_KEY}
      BROWSER: echo
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE}
      AGENTGLOB_RUNTIME_URL: ${AGENTGLOB_RUNTIME_URL:-}
      AGENTGLOB_RUNTIME_TOKEN: ${AGENTGLOB_RUNTIME_TOKEN:-}
    volumes:
      - ${OPENCLAW_CONFIG_DIR}:/home/node/.openclaw
      - ${OPENCLAW_WORKSPACE_DIR}:/home/node/.openclaw/workspace
    stdin_open: true
    tty: true
    init: true
    entrypoint: ["node", "dist/index.js"]
