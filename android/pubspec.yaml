name: zeke_ai
description: OpenClaw Android Node with Limitless Pendant Support
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # BLE
  flutter_blue_plus: ^1.32.0
  
  # WebSocket
  web_socket_channel: ^2.4.0
  
  # Audio
  record: ^5.2.0
  
  # Storage
  sqflite: ^2.3.0
  path: ^1.8.0
  
  # Location
  geolocator: ^11.0.0
  
  # UI
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
