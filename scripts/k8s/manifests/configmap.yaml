apiVersion: v1
kind: ConfigMap
metadata:
  name: mullusi-config
  labels:
    app: mullusi
data:
  mullusi.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18790,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.mullusi/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "Mullusi Assistant",
            "workspace": "~/.mullusi/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # Mullusi Assistant

    You are a helpful AI assistant running in Kubernetes.
