apiVersion: v1
kind: Service
metadata:
  name: mullusi
  labels:
    app: mullusi
spec:
  type: ClusterIP
  selector:
    app: mullusi
  ports:
    - name: gateway
      port: 18790
      targetPort: 18790
      protocol: TCP
