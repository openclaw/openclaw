PR chain validation note.

This branch exists only to validate the new submission path under `jlcbk/openclaw`.
If PR creation succeeds, we can immediately close it and continue with real contribution work.
