import { ConsoleShell } from "../../components/console-shell";

export default function SettingsPage() {
  return (
    <ConsoleShell title="设置">
      <p>模型诊断与 Provider 配置详情会显示在这里。</p>
    </ConsoleShell>
  );
}
