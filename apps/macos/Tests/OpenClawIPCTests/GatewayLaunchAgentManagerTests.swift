import Foundation
import Testing
@testable import OpenClaw

struct GatewayLaunchAgentManagerTests {
    @Test func `attach only runtime override does not uninstall gateway launch agent`() throws {
        let dir = FileManager().temporaryDirectory
            .appendingPathComponent("openclaw-attach-only-\(UUID().uuidString)", isDirectory: true)
        let marker = dir.appendingPathComponent("disable-launchagent")
        try FileManager().createDirectory(at: dir, withIntermediateDirectories: true)
        defer { try? FileManager().removeItem(at: dir) }
        defer {
            GatewayLaunchAgentManager.setTestingDisableLaunchAgentMarkerURL(nil)
            GatewayLaunchAgentManager.setTestingInterceptDaemonCommands(false)
            GatewayLaunchAgentManager.clearTestingDaemonCommandCalls()
        }

        GatewayLaunchAgentManager.setTestingDisableLaunchAgentMarkerURL(marker)
        GatewayLaunchAgentManager.setTestingInterceptDaemonCommands(true)
        GatewayLaunchAgentManager.clearTestingDaemonCommandCalls()

        let error = GatewayLaunchAgentManager.applyAttachOnlyRuntimeOverride()

        #expect(error == nil)
        #expect(FileManager().fileExists(atPath: marker.path))
        #expect(GatewayLaunchAgentManager.testingDaemonCommandCallsSnapshot().isEmpty)
    }

    @Test func `launch agent plist snapshot parses args and env`() throws {
        let url = FileManager().temporaryDirectory
            .appendingPathComponent("openclaw-launchd-\(UUID().uuidString).plist")
        let plist: [String: Any] = [
            "ProgramArguments": ["openclaw", "gateway", "--port", "18789", "--bind", "loopback"],
            "EnvironmentVariables": [
                "OPENCLAW_GATEWAY_TOKEN": " secret ",
                "OPENCLAW_GATEWAY_PASSWORD": "pw",
            ],
        ]
        let data = try PropertyListSerialization.data(fromPropertyList: plist, format: .xml, options: 0)
        try data.write(to: url, options: [.atomic])
        defer { try? FileManager().removeItem(at: url) }

        let snapshot = try #require(LaunchAgentPlist.snapshot(url: url))
        #expect(snapshot.port == 18789)
        #expect(snapshot.bind == "loopback")
        #expect(snapshot.token == "secret")
        #expect(snapshot.password == "pw")
    }

    @Test func `launch agent plist snapshot reads generated env wrapper file`() throws {
        let dir = FileManager().temporaryDirectory
            .appendingPathComponent("openclaw-launchd-env-\(UUID().uuidString)", isDirectory: true)
        let serviceEnvDir = dir.appendingPathComponent("service-env", isDirectory: true)
        try FileManager().createDirectory(at: serviceEnvDir, withIntermediateDirectories: true)
        defer { try? FileManager().removeItem(at: dir) }

        let envFile = serviceEnvDir.appendingPathComponent("com.openclaw.gateway.env")
        try """
        # generated by openclaw
        export OPENCLAW_GATEWAY_TOKEN='file token'
        export OPENCLAW_GATEWAY_PASSWORD='pa'\\''ss'
        export OPENCLAW_IGNORED_WITH_DASH='bad'
        export 1OPENCLAW_INVALID='bad'
        export OPENCLAW_EXTRA=plain
        """.write(to: envFile, atomically: true, encoding: .utf8)

        let plistURL = dir.appendingPathComponent("openclaw-launchd.plist")
        let wrapper = serviceEnvDir.appendingPathComponent("com.openclaw.gateway-env-wrapper.sh")
        let plist: [String: Any] = [
            "ProgramArguments": [
                wrapper.path,
                envFile.path,
                "openclaw",
                "gateway",
                "--port",
                "18789",
                "--bind",
                "tailnet",
            ],
            "EnvironmentVariables": [
                "OPENCLAW_GATEWAY_TOKEN": " inline-token ",
                "OPENCLAW_GATEWAY_PASSWORD": "inline-pass",
                "OPENCLAW_INLINE_ONLY": "inline",
            ],
        ]
        let data = try PropertyListSerialization.data(fromPropertyList: plist, format: .xml, options: 0)
        try data.write(to: plistURL, options: [.atomic])

        let snapshot = try #require(LaunchAgentPlist.snapshot(url: plistURL))
        #expect(snapshot.programArguments == ["openclaw", "gateway", "--port", "18789", "--bind", "tailnet"])
        #expect(snapshot.environment["OPENCLAW_GATEWAY_TOKEN"] == "file token")
        #expect(snapshot.environment["OPENCLAW_GATEWAY_PASSWORD"] == "pa'ss")
        #expect(snapshot.environment["OPENCLAW_EXTRA"] == "plain")
        #expect(snapshot.environment["OPENCLAW_INLINE_ONLY"] == "inline")
        #expect(snapshot.environment["OPENCLAW_IGNORED_WITH_DASH"] == "bad")
        #expect(snapshot.environment["1OPENCLAW_INVALID"] == nil)
        #expect(snapshot.port == 18789)
        #expect(snapshot.bind == "tailnet")
        #expect(snapshot.token == "file token")
        #expect(snapshot.password == "pa'ss")
    }

    @Test func `launch agent plist snapshot allows missing bind`() throws {
        let url = FileManager().temporaryDirectory
            .appendingPathComponent("openclaw-launchd-\(UUID().uuidString).plist")
        let plist: [String: Any] = [
            "ProgramArguments": ["openclaw", "gateway", "--port", "18789"],
        ]
        let data = try PropertyListSerialization.data(fromPropertyList: plist, format: .xml, options: 0)
        try data.write(to: url, options: [.atomic])
        defer { try? FileManager().removeItem(at: url) }

        let snapshot = try #require(LaunchAgentPlist.snapshot(url: url))
        #expect(snapshot.port == 18789)
        #expect(snapshot.bind == nil)
    }
}
