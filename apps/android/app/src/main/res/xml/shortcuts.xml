<shortcuts xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <capability
        android:name="custom.actions.intent.ASK_MULLUSI"
        app:queryPatterns="@array/ask_mullusi_query_patterns">
        <intent
            android:action="ai.mullusi.app.action.ASK_MULLUSI"
            android:targetPackage="ai.mullusi.app"
            android:targetClass="ai.mullusi.app.MainActivity">
            <parameter
                android:name="prompt"
                android:key="prompt"
                android:mimeType="https://schema.org/Text"
                android:required="true" />
        </intent>
    </capability>
</shortcuts>
