package ai.openclaw.app.ui.buddy

import androidx.compose.runtime.Composable

@Composable
fun BuddyInteractionsPlaceholder() {
  // Touch hit regions are added in the next task so this UI commit stays visual-only.
}
