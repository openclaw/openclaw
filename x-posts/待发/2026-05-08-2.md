<!-- angle: 反常识结论 | score: 8 | image: x-posts/images/2026-05-08-2.png -->

配置回退最怕的，不是报错，是悄悄改对你不利的东西

今天修 macOS 配置写入，发现 fallback 可能把 `gateway.mode` 这种关键字段静默抹掉。
现在的做法很硬：一旦检测到字段被删，直接 reject，顺手落一份 `openclaw.json.rejected.*`。
这刀一共 `335 insertions`，commit `f6a5405`。

我的态度很明确：配置系统宁可吵，也别安静地背刺你。
