import { type StreamingMode } from "openclaw/plugin-sdk/channel-streaming";
type DiscordPreviewStreamMode = StreamingMode;
export declare function resolveDiscordPreviewStreamMode(params?: {
    streamMode?: unknown;
    streaming?: unknown;
}): DiscordPreviewStreamMode;
export {};
