---
phase: 02-runtime-governance-enforcement
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - C:/devProjects/openclaw/src/infra/diagnostic-events.ts
  - C:/devProjects/openclaw/src/logging/diagnostic.ts
  - C:/devProjects/openclaw/src/plugins/runtime/types-core.ts
  - C:/devProjects/openclaw/src/plugins/runtime/runtime-events.ts
  - C:/devProjects/openclaw/extensions/diagnostics-otel/src/service.ts
  - C:/Users/fjventura20/myVault/.openclaw
  - C:/Users/fjventura20/myVault/BOOT_MEMORY.md
  - C:/devProjects/openclaw/CLAUDE.md
autonomous: false

must_haves:
  truths:
    - "Governance decisions emit structured compliance telemetry"
    - "Telemetry includes decision, reason code, tool, mode, run/session identity"
    - "OTEL exporter handles governance telemetry without breaking existing event types"
    - "Plugin runtime exposes diagnostics event APIs needed by enforcement extensions"
    - "Runtime config enables governance extension in shadow mode first"
    - "Boot and Claude instructions reference runtime governance policy and enforcement mode"
    - "Human verification confirms shadow-mode evidence before enforce-mode rollout"
  artifacts:
    - path: "C:/devProjects/openclaw/src/infra/diagnostic-events.ts"
      provides: "Typed governance diagnostic event schema"
      contains: "governance.decision"
    - path: "C:/devProjects/openclaw/src/logging/diagnostic.ts"
      provides: "Helper emitter for governance decision diagnostics"
      contains: "logGovernanceDecision"
    - path: "C:/devProjects/openclaw/extensions/diagnostics-otel/src/service.ts"
      provides: "OTEL metric/span handling for governance decisions"
      contains: "governance.decision"
    - path: "C:/Users/fjventura20/myVault/.openclaw"
      provides: "Runtime plugin activation and governance mode configuration"
      contains: "frankos-governance"
    - path: "C:/Users/fjventura20/myVault/BOOT_MEMORY.md"
      provides: "Startup loading instructions include runtime policy artifact"
      contains: "GOVERNANCE_RUNTIME_POLICY.json"
  key_links:
    - from: "diagnostic-events.ts"
      to: "diagnostics-otel/src/service.ts"
      via: "Typed event is exported and consumed"
      pattern: "governance\\.decision"
    - from: ".openclaw"
      to: "frankos-governance plugin"
      via: "Runtime configuration and mode rollout"
      pattern: "mode"
---

<objective>
Operationalize governance enforcement with telemetry, rollout controls, and human verification.

Purpose: ensure Phase 2 enforcement is observable and safely introduced in production-like usage before hard blocking becomes default behavior.

Output: governance telemetry pipeline + staged rollout configuration + verified shadow-mode evidence.
</objective>

<context>
## Dependencies

- Plan 02-01 provides policy artifact + enforcement extension.
- Plan 02-02 adds observability, runtime wiring, and go-live safety controls.

## Current Diagnostics Baseline

- Diagnostic event bus is type-safe and centralized (`src/infra/diagnostic-events.ts`).
- Logging helper emits structured events (`src/logging/diagnostic.ts`).
- OTEL exporter subscribes to diagnostic events via switch dispatch (`extensions/diagnostics-otel/src/service.ts`).

## Rollout Principle

Enforcement should not go straight to hard block. Start in `shadow` mode, gather evidence, tune policy, then explicitly promote to `enforce`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed governance decision telemetry</name>
  <files>
    C:/devProjects/openclaw/src/infra/diagnostic-events.ts
    C:/devProjects/openclaw/src/logging/diagnostic.ts
    C:/devProjects/openclaw/src/plugins/runtime/types-core.ts
    C:/devProjects/openclaw/src/plugins/runtime/runtime-events.ts
  </files>
  <action>
    Add `governance.decision` diagnostic event type with fields:
    - `decision`: `permit|prohibit|escalate`
    - `reasonCode`
    - `toolName`
    - `mode`: `off|shadow|enforce`
    - `runId`, optional `sessionId`, optional `sessionKey`
    - optional `policyVersion`
    - optional `durationMs`

    Add `logGovernanceDecision(...)` helper in `src/logging/diagnostic.ts`.

    Expose diagnostics APIs to plugin runtime events:
    - `onDiagnosticEvent`
    - `emitDiagnosticEvent`
    so governance plugins can emit structured telemetry directly.
  </action>
  <verify>
    - Type-check passes for updated diagnostic event unions
    - `governance.decision` can be emitted and observed in tests
    - Plugin runtime type surface includes diagnostics emit/listen functions
  </verify>
  <done>
    Governance decision telemetry is first-class, typed, and reachable from plugin runtime.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend OTEL exporter for governance decisions</name>
  <files>
    C:/devProjects/openclaw/extensions/diagnostics-otel/src/service.ts
  </files>
  <action>
    Update diagnostics OTEL service to handle `governance.decision` events:
    - add counters by decision/mode/reasonCode/tool
    - record optional duration histogram
    - optionally emit trace span for `prohibit`/`escalate` outcomes
    - keep existing event handling unchanged
  </action>
  <verify>
    - Switch dispatch includes `governance.decision`
    - OTEL tests updated for new event handling
    - No regression in existing diagnostics event tests
  </verify>
  <done>
    Governance enforcement outcomes are exported to telemetry backends for compliance monitoring.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire rollout config and startup documentation</name>
  <files>
    C:/Users/fjventura20/myVault/.openclaw
    C:/Users/fjventura20/myVault/BOOT_MEMORY.md
    C:/devProjects/openclaw/CLAUDE.md
  </files>
  <action>
    Configure runtime in `.openclaw`:
    - enable `frankos-governance` plugin
    - set mode to `shadow`
    - point to `GOVERNANCE_RUNTIME_POLICY.json`

    Update `BOOT_MEMORY.md` to include runtime policy artifact in startup reads.

    Update `CLAUDE.md` with operational guidance:
    - governance mode semantics (`shadow` vs `enforce`)
    - escalation handling expectations
    - where to inspect governance decision telemetry.
  </action>
  <verify>
    - Plugin appears enabled in runtime config
    - BOOT_MEMORY includes runtime policy path
    - CLAUDE contains governance rollout notes
  </verify>
  <done>
    Runtime config and startup docs are aligned with governance enforcement rollout.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 2 operationalization:
    1. Governance decision telemetry type and emit path
    2. OTEL export support for governance decisions
    3. Runtime shadow-mode rollout config and startup doc wiring
  </what-built>
  <how-to-verify>
    Perform verification in this order:

    1. **Shadow mode behavior**
       - Trigger one policy-prohibited tool scenario and one escalation-required scenario.
       - Confirm tool execution is allowed in shadow mode, but decision telemetry is emitted.

    2. **Telemetry integrity**
       - Confirm emitted events include: decision, reasonCode, toolName, mode, run/session identifiers.
       - If OTEL enabled, confirm governance metrics arrive in backend.

    3. **Rollout safety**
       - Switch to enforce mode in a controlled test.
       - Re-run prohibited/escalation scenarios.
       - Confirm actions are blocked with stable governance reason codes.

    4. **Prompt/runtime alignment**
       - Ask agent to evaluate a mutating action.
       - Confirm explanation matches decision hierarchy and runtime verdict.
  </how-to-verify>
  <resume-signal>Type "approved" when shadow-mode evidence and enforce-mode blocking both pass, or provide failing scenario details.</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:

1. `governance.decision` exists as a typed diagnostic event.
2. Governance events are emitted and exportable via diagnostics-otel.
3. Runtime is configured to start in shadow mode.
4. BOOT/CLAUDE guidance references runtime policy and enforcement flow.
5. Human checkpoint validates shadow and enforce behaviors on real scenarios.
</verification>

<success_criteria>
Phase 02 is complete when ALL are true:
- Runtime enforcement exists (Plan 02-01).
- Compliance telemetry exists and is observable (Plan 02-02).
- Rollout path is controlled and evidence-based (shadow -> enforce).
- Human verification confirms behavior on real constitutional scenarios.
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-governance-enforcement/02-02-SUMMARY.md`
</output>
