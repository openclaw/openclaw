---
phase: 02-verification
plan: 02
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - .planning/phases/02-verification/02-VERIFICATION.md
autonomous: true
requirements:
  - TALK-03
  - TALK-04
  - CI-01
must_haves:
  truths:
    - Focused gateway server tests pass (server-lifecycle, server-request-context, chat-queued-turns, server.chat.gateway-server-chat*)
    - Gateway server tests run with fileParallelism: false via vitest.gateway-server.config.ts
    - retiredFollowupRunIds type + runtime plumbing verified through typecheck (covered in Task 2)
    - Test mocks all include retiredFollowupRunIds: new Map()
  artifacts:
    - .planning/phases/02-verification/02-VERIFICATION.md (gateway test results section)
  key_links:
    - vitest.gateway-server.config.ts uses fileParallelism: false and dir: "src/gateway"
    - chat-queued-turns.test.ts is the primary test for server-side retired follow-up tracking
    - server.chat.gateway-server-chat-b.test.ts covers gateway server chat B scenarios
<implicit_trust_boundaries>:
  client_server: Gateway server tests verify the server-side correlation of retired follow-up run IDs, which must match exactly against client-provided frame.runId values to prevent replay/stale event injection.

estimate:
  tokens: 18000
  raw_tokens: 15000
  tasks: 3
  confidence: high
---

<objective>
Run focused gateway server tests for talk/queue related files to verify server-side retiredFollowupRunIds plumbing and correlation logic.

Purpose: Prove that TALK-03 (retiredFollowupRunIds lifecycle), TALK-04 (Gateway-backed correlation replacing acceptingAnyRunId), and CI-01 (test mocks updated) hold at the gateway server layer. The server-side Map-based correlation must reject stale or replayed follow-up events.

Output: Gateway test results captured in VERIFICATION.md showing server-lifecycle, server-request-context, chat-queued-turns, and server.chat.gateway-server-chat* tests pass.
</objective>

<execution_context>
@~~/.claude/gsd-core/workflows/execute-plan.md
@~~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-fix-implementation/01-CONTEXT.md
@.planning/phases/01-fix-implementation/01-VERIFICATION.md
@H:\openclaw\openclaw/package.json (test:gateway, tsgo:core:test)
@H:\openclaw\openclaw/test/vitest/vitest.gateway-server.config.ts
@H:\openclaw\openclaw/test/vitest/vitest.gateway-server-paths.mjs
@H:\openclaw\openclaw/AGENTS.md
</context>

<threat_model>

## Trust Boundaries

| Boundary             | Description                                                                                                                                       |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| client-gateway-frame | Client WebSocket frames with runId must be validated against server-side retiredFollowupRunIds Map; stale/replayed follow-up runIds are rejected. |

## STRIDE Threat Register

| Threat ID       | Category    | Component                                           | Severity | Disposition | Mitigation Plan                                                                                                                                                                                  |
| --------------- | ----------- | --------------------------------------------------- | -------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-02-01         | Tampering   | Gateway request context (server-request-context.ts) | high     | mitigate    | retiredFollowupRunIds Map is created in GatewayCoreRuntime and destructured onto every request context; server-request-context.test.ts validates type + runtime presence                         |
| T-02-02         | Spoofing    | chat-queued-turns.ts correlation                    | high     | mitigate    | matchesActiveRun validates incoming frame.runId against active follow-up runId from Gateway-backed correlation; chat-queued-turns.test.ts exercises the matching logic                           |
| T-02-03         | Repudiation | server-lifecycle.ts retirement chain                | medium   | mitigate    | retiredFollowupRunIds is threaded through prepareGatewayLifecycle (line 675) to startGatewayMaintenanceTimers; server-lifecycle tests validate the Map is available on the core runtime snapshot |
| </threat_model> |

<tasks>

<task type="auto">
  <name>Task 1: Run focused gateway server tests for talk/queue subsystem</name>
  <files>.planning/phases/02-verification/02-VERIFICATION.md</files>
  <action>
    <read_first>
    Gateway server tests use the config at test/vitest/vitest.gateway-server.config.ts which sets dir: "src/gateway", fileParallelism: false, and includes patterns matching `src/gateway/**/*server*.test.ts` plus gatewayServerBackedHttpTestFiles. The isGatewayServerTestFile function excludes server-methods/ and .e2e.test.ts files.

    Phase 1 changed these gateway server files:
    - `src/gateway/server-lifecycle.ts` — retiredFollowupRunIds Map creation + lifecycle chain (lines 88, 675)
    - `src/gateway/server-request-context.ts` — retiredFollowupRunIds in GatewayRequestContextRuntime type (line 70, line 495 runtime)
    - `src/gateway/chat-queued-turns.ts` — server-side retired follow-up turn tracking
    - `src/gateway/server-maintenance.ts` — maintenance timers with retiredFollowupRunIds
    - `src/gateway/server-core-runtime.ts`, `server-runtime-state-prepare.ts`, `server-startup-early.ts` — runtime state plumbing
    - `src/gateway/server-connection-state.ts`, `local-request-context.ts` — connection/request context types

    From the git status, the changed test files include:
    - `src/gateway/server-active-work.test.ts`
    - `src/gateway/server-chat.agent-events.test.ts`
    - `src/gateway/server-close.test.ts`
    - `src/gateway/server-methods.authorization.test.ts`
    - `src/gateway/server-methods.suspension-admission.test.ts`
    - `src/gateway/server.methods.chat-send-session-binding.test.ts`
    - `src/gateway/server-methods/chat.abort.test-helpers.ts` etc.

    Primary test files to run:
    1. `src/gateway/chat-queued-turns.test.ts` — directly tests the server-side retired follow-up tracking
    2. `src/gateway/server-request-context.test.ts` — tests the request context that includes retiredFollowupRunIds
    3. `src/gateway/server.chat.gateway-server-chat-b.test.ts` — gateway server chat B test
    4. `src/gateway/server-lifecycle.test.ts` or lifecycle tests — validates retiredFollowupRunIds plumbing

    Run the gateway server test shard with focused patterns:
    `node scripts/run-vitest.mjs run --config test/vitest/vitest.gateway-server.config.ts --reporter=verbose 2>&1 | tail -60`

    Note: This config already includes all `src/gateway/**/*server*.test.ts` files. We run the full gateway-server shard to get complete coverage of the talk/queue subsystem changes. The `test:gateway` script uses `--config test/vitest/vitest.gateway.config.ts` which shards across project configs, but for focused verification we use the gateway-server config directly.

  </action>
  <verify>
    <automated>node scripts/run-vitest.mjs run --config test/vitest/vitest.gateway-server.config.ts 2>&1 | tail -60</automated>
  </verify>
  <done>All gateway server tests pass (fileParallelism: false). Key test files: chat-queued-turns.test.ts, server-request-context.test.ts, server.chat.gateway-server-chat-b.test.ts. VERIFICATION.md records the full output.</done>
</task>

<task type="auto">
  <name>Task 2: Run gateway methods tests for talk/queue affected areas</name>
  <files>.planning/phases/02-verification/02-VERIFICATION.md</files>
  <action>
    The gateway server config explicitly excludes `src/gateway/server-methods/**` tests (they run under vitest.gateway-methods.config.ts separately). Phase 1 changed many server-methods test helpers adding `retiredFollowupRunIds: new Map()`.

    Run the gateway-methods shard to verify these test mock updates don't break anything:
    `node scripts/run-vitest.mjs run --config test/vitest/vitest.gateway-methods.config.ts 2>&1 | tail -40`

    From git status, changed server-methods test files include:
    - server-methods.authorization.test.ts
    - server-methods.suspension-admission.test.ts
    - server-methods.chat-send-session-binding.test.ts
    - server-methods/sessions-mutations.archive-attribution.test.ts
    - server-methods/sessions-mutations.catalog-queue.test.ts
    - server-methods/sessions-mutations.perf.test.ts
    - server-methods/sessions-mutations.sticky-model.test.ts
    - server-methods/sessions.abort-agent-scope.test.ts
    - server-methods/sessions.send-followup-status.test.ts
    - server/chat-abort.test-helpers.ts

    This validates that the 23 test helper files updated in commit 52021c3 don't introduce regressions.

  </action>
  <verify>
    <automated>node scripts/run-vitest.mjs run --config test/vitest/vitest.gateway-methods.config.ts 2>&1 | tail -40</automated>
  </verify>
  <done>Gateway methods tests pass — all 23 test mock files updated with `retiredFollowupRunIds: new Map()` in commit 52021c3 verified. VERIFICATION.md records results.</done>
</task>

<task type="auto">
  <name>Task 3: Verify tsgo:core:test covers gateway test file type checking</name>
  <files>.planning/phases/02-verification/02-VERIFICATION.md</files>
  <action>
    The tsgo:core:test lane (`node scripts/run-tsgo-core-test-shards.mjs src`) typechecks all test files under `src/`. This validates that the `retiredFollowupRunIds: new Map()` additions and all 23 test helper changes compile under strict TypeScript ESM.

    This is already covered by Task 2 in Plan 02-01 (typecheck task), but explicitly calling it out here for the gateway test files. The Phase 1 VERIFICATION.md confirmed:
    - TS2835 import extension errors resolved (commit b463878)
    - Import cycles broken (commit e32d865)
    - All test mocks include retiredFollowupRunIds: new Map() (commit 52021c3)

    No separate run needed — this is validated as part of the typecheck task. Record the dependency in VERIFICATION.md.

  </action>
  <verify>
    <automated>echo "Validated by tsgo:core:test in Plan 02-01 Task 2"</automated>
  </verify>
  <done>tsgo:core:test validates all gateway server test files under strict TypeScript. No separate run required. Result recorded in VERIFICATION.md.</done>
</task>

</tasks>

<verification>
Gateway server test shard passes with fileParallelism: false. Gateway methods test shard passes. All 23 test mock files with `retiredFollowupRunIds: new Map()` confirmed compiling and passing.
</verification>

<success_criteria>

- All `src/gateway/**/*server*.test.ts` pass via gateway-server config
- All `src/gateway/server-methods/**/*.test.ts` pass via gateway-methods config
- tsgo:core:test confirms strict types on all gateway test files
  </success_criteria>

<output>
Update .planning/phases/02-verification/02-VERIFICATION.md with gateway test results
</output>
