---
phase: 02-verification
plan: 01
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - .planning/phases/02-verification/02-VERIFICATION.md
autonomous: true
requirements:
  - TALK-01
  - TALK-02
  - TALK-03
  - TALK-04
  - TALK-05
  - TALK-06
  - TYPE-01
  - TYPE-02
  - TYPE-03
  - CI-01
must_haves:
  truths:
    - `pnpm check:changed` passes for all changed files (assertion-SAFETY ratchet, max-lines ratchet, format, import-cycles)
    - `pnpm typecheck` passes with strict types (tsgo lanes cover core, ui, and test files)
    - Focused UI tests (`realtime-talk-*`, `chat-queue*`) pass with UI vitest config
    - Focused gateway tests (`server-lifecycle`, `server-request-context`, `chat-queued-turns`) pass with gateway-server vitest config
    - Pre-existing `check:bundled-channel-config-metadata` failure documented as environmental
  artifacts:
    - .planning/phases/02-verification/02-VERIFICATION.md
  key_links:
    - check:changed selects typecheck/lint/guard work based on changed paths (scripts/changed-lanes.mts)
    - tsgo lanes (tsgo:core, tsgo:ui, tsgo:core:test) provide strict TypeScript verification
    - UI tests use jsdom environment via vitest.ui.config.ts
    - Gateway server tests run with fileParallelism: false via vitest.gateway-server.config.ts

estimate:
  tokens: 25000
  raw_tokens: 20000
  tasks: 5
  confidence: high
---

<objective>
Verify all Phase 1 changes for the OpenClaw Talk/Queue Fix Initiative with appropriate proof.

Purpose: Confirm that the queued consult empty-completion fix, bounded unmatched-event buffer, retiredFollowupRunIds lifecycle plumbing, and import cycle resolution are correct and pass all repository check gates. This phase produces evidence that the Phase 1 implementation is sound before advancing to Phase 3 (Landing).

Output: .planning/phases/02-verification/02-VERIFICATION.md report containing pass/fail status for check:changed, typecheck, focused tests, and full subsystem test runs, plus documentation of any pre-existing/environmental failures.
</objective>

<execution_context>
@~~/.claude/gsd-core/workflows/execute-plan.md
@~~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-fix-implementation/01-CONTEXT.md
@.planning/phases/01-fix-implementation/01-VERIFICATION.md
@H:\openclaw\openclaw/package.json (scripts: check:changed, test:ui, test:gateway, tsgo:*)
@H:\openclaw\openclaw/test/vitest/vitest.ui.config.ts
@H:\openclaw\openclaw/test/vitest/vitest.gateway-server.config.ts
@H:\openclaw\openclaw/test/vitest/vitest.gateway-methods.config.ts
@H:\openclaw\openclaw/AGENTS.md (lines 70-74: check:changed, tsgo lanes, test conventions)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run pnpm check:changed and capture ratchet results</name>
  <files>.planning/phases/02-verification/02-VERIFICATION.md</files>
  <action>
    <read_first>
    Read scripts/check-changed.mts (already loaded above) to understand the changed-lanes selection logic. The check:changed script runs `node scripts/check-changed.mjs` which dispatches to tsx-cli-shim, then loads ./check-changed.mts. It selects formatting, typecheck, lint, and guard work based on changed paths (scripts/changed-lanes.mts). Key ratchets triggered for src/ and ui/src/ changes: assertion SAFETY comment ratchet, max-lines suppression ratchet, format check, import-cycles.

    Known pre-existing failure: `check:bundled-channel-config-metadata` fails due to missing `node_modules/@openclaw/ai/dist/internal/shared.mjs` build artifact. Confirmed environmental (reproduced on clean tree with all changes stashed). Must be documented as environmental, not a regression.

    Run `pnpm check:changed` from the repo root. Capture full stdout/stderr output. Verify:
    1. max-lines suppression ratchet passes
    2. assertion SAFETY comment ratchet passes
    3. format check passes
    4. runtime import cycles check passes (TALK-03 dependency: import cycle broken)
    5. The only failure is `check:bundled-channel-config-metadata` (pre-existing/environmental)

    If check:changed exits non-zero, capture the exact error output and diff against the known environmental failure. Any NEW failure (beyond bundled-channel-config-metadata) indicates a regression from Phase 1 changes and must be investigated.

  </action>
  <verify>
    <automated>pnpm check:changed 2>&1 | tee /tmp/check-changed-output.txt</automated>
  </verify>
  <done>`pnpm check:changed` output captured showing max-lines, assertion-SAFETY, format, and import-cycles all green; the only failure is the pre-existing `check:bundled-channel-config-metadata` environmental issue, documented in VERIFICATION.md.</done>
</task>

<task type="auto">
  <name>Task 2: Run pnpm typecheck (tsgo lanes) and validate strict types</name>
  <files>.planning/phases/02-verification/02-VERIFICATION.md</files>
  <action>
    <read_first>
    Per AGENTS.md line 73: "Formatting uses `oxfmt`; typechecking uses the repository's `tsgo` lanes." The `pnpm typecheck` shorthand referenced in ROADMAP.md maps to the tsgo lanes, not tsc. Three relevant lanes:

    - `tsgo:core`: `node scripts/run-tsgo.mjs -p tsconfig.core.json` — typechecks production source under `src/`
    - `tsgo:ui`: `node scripts/run-tsgo.mjs -p tsconfig.ui.json` — typechecks UI source under `ui/src/`
    - `tsgo:core:test`: `node scripts/run-tsgo-core-test-shards.mjs src` — typechecks test files under `src/`

    Phase 1 changed files span both `src/gateway/` (server-side) and `ui/src/pages/chat/` (client-side), plus test files in both trees. All three lanes must pass to validate the strict type contract.

    Run each lane sequentially, capturing output. The Phase 1 VERIFICATION.md confirmed these key type contracts:
    - TS2835 import extension errors resolved (commit b463878)
    - ESLint curly violations resolved (commit b463878)
    - Import cycles broken by moving AgentWaitResult to realtime-talk-followup-observation.ts (commit e32d865)
    - `retiredFollowupRunIds: new Map()` added to all 23 test mock files (commit 52021c3)

    Run `pnpm tsgo:core`, `pnpm tsgo:ui`, and `pnpm tsgo:core:test` (for src test files). Note: `ui/src` test files are covered by the UI vitest config, not tsgo:core:test. Verify all passes with no errors.

  </action>
  <verify>
    <automated>pnpm tsgo:core 2>&1 | tail -20</automated>
  </verify>
  <verify>
    <automated>pnpm tsgo:ui 2>&1 | tail -20</automated>
  </verify>
  <verify>
    <automated>pnpm tsgo:core:test 2>&1 | tail -20</automated>
  </verify>
  <done>All three tsgo lanes (core, ui, core:test) pass with zero type errors. Strict TypeScript ESM contract restored. VERIFICATION.md records the tsgo output for each lane.</done>
</task>

<task type="auto">
  <name>Task 3: Run focused UI tests (realtime-talk-* and chat-queue*) via UI vitest config</name>
  <files>.planning/phases/02-verification/02-VERIFICATION.md</files>
  <action>
    <read_first>
    UI tests use the config at test/vitest/vitest.ui.config.ts which sets environment: "jsdom" and includes patterns from controlUiTestGlobs. The test command `pnpm test:ui` runs `pnpm --dir ui test` which invokes `vitest run --config vitest.config.ts`. However, for focused verification, we should use the root-level runner with the UI config to target specific test files.

    The Phase 1 changes touched these UI files:
    - `ui/src/pages/chat/realtime-talk-shared.ts` — core fix: waitForEmptyFinalFallback
    - `ui/src/pages/chat/realtime-talk-chat-handler.ts` — matchesActiveRun, bounded bufferEvent
    - `ui/src/pages/chat/realtime-talk-followup-observation.ts` — AgentWaitResult, observePendingFollowupRunId

    The focused test files to run are all `realtime-talk-*.test.ts` in `ui/src/pages/chat/` (17 files found) and any `chat-queue*.test.ts` files.

    Run: `node scripts/run-vitest.mjs run --config test/vitest/vitest.ui.config.ts "ui/src/pages/chat/realtime-talk-*.test.ts"`

    If chat-queue test files are not found under ui/src/pages/chat, search broader. The phase context mentions `chat-queue*` as a test pattern — check ui/src/pages/chat/ for these.

    Expected: all tests pass (Phase 1 VERIFICATION.md confirmed regression tests were committed in 2dfeb1631b1 covering follow-up runId recovery and buffer bounds).

  </action>
  <verify>
    <automated>node scripts/run-vitest.mjs run --config test/vitest/vitest.ui.config.ts "ui/src/pages/chat/realtime-talk-*.test.ts" 2>&1 | tail -40
