---
phase: 02-verification
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 02-01
files_modified:
  - .planning/phases/02-verification/02-VERIFICATION.md
  - config/assertion-safety-baseline.txt
autonomous: true
requirements:
  - TYPE-01
  - TYPE-02
  - TYPE-03
  - CI-01
must_haves:
  truths:
    - assertion SAFETY comment ratchet passes for changed files
    - max-lines suppression ratchet passes for changed files
    - VERIFICATION.md report written with all results consolidated
    - Pre-existing check:bundled-channel-config-metadata failure documented as environmental
  artifacts:
    - .planning/phases/02-verification/02-VERIFICATION.md
    - config/assertion-safety-baseline.txt (checked, not modified unless ratchet failure)
  key_links:
    - check:assertion-safety validates type assertion safety comments per changed file
    - check:max-lines-ratchet validates max-lines suppression comments per changed file
    - Phase 1 VERIFICATION.md recorded baseline updated from 7->5 for realtime-talk-shared.ts

estimate:
  tokens: 15000
  raw_tokens: 12000
  tasks: 3
  confidence: high
---

<objective>
Run specific ratchet checks (assertion-SAFETY, max-lines), run full subsystems test suites, and write the consolidated Phase 2 VERIFICATION.md report.

Purpose: The Phase 1 implementation modified shared types and test helpers — the assertion-SAFETY and max-lines ratchets must be individually verified for the changed files to prove TALK-03 through TALK-06 and CI-01 hold at the code quality level. The full subsystem test suites (gateway + UI) must be run to catch any integration regressions. Finally, all evidence is consolidated into the VERIFICATION.md report required by the Phase 2 success criteria.

Output: .planning/phases/02-verification/02-VERIFICATION.md with all verification results, and confirmation that ratchets pass for every changed file.
</objective>

<execution_context>
@~~/.claude/gsd-core/workflows/execute-plan.md
@~~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-fix-implementation/01-VERIFICATION.md
@H:\openclaw\openclaw/package.json (check:assertion-safety, check:max-lines-ratchet, test:gateway, test:unit:fast)
@H:\openclaw\openclaw/scripts/check-changed.mts
@H:\openclaw\openclaw/AGENTS.md
</context>

<threat_model>

## Trust Boundaries

| Boundary     | Description                                                                                                          |
| ------------ | -------------------------------------------------------------------------------------------------------------------- |
| test-quality | Ratchet checks ensure type assertions and line counts remain auditable; skipping them risks silent test degradation. |

## STRIDE Threat Register

| Threat ID       | Category               | Component                                                             | Severity | Disposition | Mitigation Plan                                                                                                                         |
| --------------- | ---------------------- | --------------------------------------------------------------------- | -------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| T-03-01         | Repudiation            | assertion-SAFETY ratchet (scripts/check-assertion-safety-ratchet.mts) | medium   | mitigate    | Run `pnpm check:assertion-safety` directly and verify no regressions on changed files; baseline in config/assertion-safety-baseline.txt |
| T-03-02         | Repudiation            | max-lines ratchet (scripts/check-max-lines-ratchet.mts)               | low      | mitigate    | Run `pnpm check:max-lines-ratchet` directly and verify no regressions on changed files                                                  |
| T-03-03         | Information Disclosure | VERIFICATION.md report                                                | low      | accept      | Report captures test output; no sensitive data included; uses synthetic test fixtures only                                              |
| </threat_model> |

<tasks>

<task type="auto">
  <name>Task 1: Run assertion-SAFETY and max-lines ratchets directly</name>
  <files>config/assertion-safety-baseline.txt</files>
  <action>
    <read_first>
    The Phase 1 VERIFICATION.md recorded that the assertion-SAFETY baseline was updated from 7->5 for realtime-talk-shared.ts after type extraction reduced assertions (commit 54ab97d). The max-lines suppression ratchet also passed in Phase 1.

    Phase 1 changed these production files:
    - ui/src/pages/chat/realtime-talk-shared.ts
    - ui/src/pages/chat/realtime-talk-chat-handler.ts
    - ui/src/pages/chat/realtime-talk-followup-observation.ts
    - src/gateway/server-lifecycle.ts
    - src/gateway/server-request-context.ts
    - src/gateway/chat-queued-turns.ts
    - src/gateway/server-maintenance.ts
    - src/gateway/server-core-runtime.ts
    - src/gateway/server-runtime-state-prepare.ts
    - src/gateway/server-startup-early.ts
    - src/gateway/server-connection-state.ts
    - src/gateway/local-request-context.ts

    And ~23 test helper files (all updated in commit 52021c3 / 418a2d9).

    Run the ratchets directly to get explicit pass/fail evidence:
    1. `pnpm check:assertion-safety` — validates type assertion safety comments
    2. `pnpm check:max-lines-ratchet` — validates max-lines suppression comments

    Both scripts are at:
    - scripts/check-assertion-safety-ratchet.mts
    - scripts/check-max-lines-ratchet.mts

    The baseline file is at config/assertion-safety-baseline.txt. If either ratchet fails on a changed file, investigate and update the baseline only if the failure is due to a legitimate baseline adjustment (Phase 1 already did 7->5 for realtime-talk-shared.ts). Any new max-lines or assertion-safety failure on a changed file must be addressed, not shrunk.

    Per AGENTS.md line 45: "Do not weaken baselines, snapshots, ignores, expected failures, or checks to conceal defects. Exact shrink-only ratchet updates are maintenance; other exception changes require approval."

  </action>
  <verify>
    <automated>pnpm check:assertion-safety 2>&1 | tail -20</automated>
  </verify>
  <verify>
    <automated>pnpm check:max-lines-ratchet 2>&1 | tail -20</automated>
  </verify>
  <done>Both `pnpm check:assertion-safety` and `pnpm check:max-lines-ratchet` pass for all changed files. No new baseline adjustments needed. Results recorded in VERIFICATION.md.</done>
</task>

<task type="auto">
  <name>Task 2: Run full subsystem test suites (gateway + UI)</name>
  <files>.planning/phases/02-verification/02-VERIFICATION.md</files>
  <action>
    <read_first>
    After focused tests pass, run the full subsystem test suites to catch integration regressions. Two subsystems affected by Phase 1 changes:

    1. **Gateway server subset**: `pnpm test:gateway` runs `node scripts/run-vitest.mjs run --config test/vitest/vitest.gateway.config.ts` with OPENCLAW_GATEWAY_PROJECT_SHARDS=1. This runs the full gateway-server, gateway-core, gateway-client, gateway-methods, and gateway-methods-isolated shards. This is broader than the focused gateway-server tests in Plan 02.

    2. **UI subset**: `pnpm --dir ui test -- --config test/vitest/vitest.ui.config.ts` or use the root runner with the UI config. The ui/package.json test script runs `vitest run --config vitest.config.ts`. For Phase 2 verification, use the root-level runner with the UI config to match the focused test approach.

    However, the full gateway and UI test suites can be large. Per AGENTS.md line 75: "Run tests appropriate to the change and complete required checks. Once those pass, broaden or repeat testing only when new changes, failures, or unresolved concerns justify it."

    Given Phase 1 already ran focused tests (per 01-VERIFICATION.md), Phase 2 should:
    1. Run the gateway-server config (already done in Plan 02 Task 1)
    2. Run the gateway-methods config (already done in Plan 02 Task 1)
    3. Run the UI config for realtime-talk tests (already done in Plan 01 Task 3)
    4. Run the UI config for the broader chat page tests to catch cross-file regressions

    For the full subsystem run, use `pnpm test:gateway` which runs all gateway shards. This validates that the retiredFollowupRunIds plumbing across the full gateway runtime doesn't break unrelated gateway functionality.

    Command: `pnpm test:gateway 2>&1 | tail -80`

    Expected: all gateway tests pass. Any failure must be investigated — it could indicate the retiredFollowupRunIds Map threading broke something in the runtime lifecycle chain.

  </action>
  <verify>
    <automated>pnpm test:gateway 2>&1 | tail -80</automated>
  </verify>
  <done>Full gateway test suite passes across all shards (gateway-server, gateway-core, gateway-client, gateway-methods). No integration regressions from retiredFollowupRunIds plumbing. Results recorded in VERIFICATION.md.</done>
</task>

<task type="auto">
  <name>Task 3: Write Phase 2 VERIFICATION.md report</name>
  <files>.planning/phases/02-verification/02-VERIFICATION.md</files>
  <action>
    <read_first>
    The Phase 1 VERIFICATION.md (.planning/phases/01-fix-implementation/01-VERIFICATION.md) serves as the template for structure. It includes:
    - YAML frontmatter with status, phase, verified_by, verified_at
    - Summary section
    - Verification Results sections for each check
    - Requirements Checklist table
    - Commits section
    - Files Changed section
    - Conclusion

    The Phase 2 VERIFICATION.md should document:
    1. All check:changed results (assertion-SAFETY, max-lines, format, import-cycles)
    2. All tsgo lane results (core, ui, core:test)
    3. Focused UI test results (realtime-talk-*.test.ts)
    4. Focused gateway server test results (chat-queued-turns, server-request-context, etc.)
    5. Gateway methods test results (all 23 mock helper changes)
    6. Full gateway subsystem test results
    7. Pre-existing environmental failure documentation (check:bundled-channel-config-metadata)
    8. Requirements checklist (TALK-01 through TALK-06, TYPE-01 through TYPE-03, CI-01) — all marked Verified in Phase 2
    9. Conclusion: Phase 2 complete, ready for Phase 3 (Landing)

    Use the frontmatter format from the Phase 1 report:
    - status: passed
    - phase: "02"
    - verified_by: [method used]
    - verified_at: 2026-09-10

    Include specific evidence: commit hashes, test counts, output excerpts. Reference the Phase 1 commits that implemented each requirement and confirm Phase 2 verification.

  </action>
  <verify>
    <automated>test -f .planning/phases/02-verification/02-VERIFICATION.md && grep -c "## " .planning/phases/02-verification/02-VERIFICATION.md</automated>
  </verify>
  <done>VERIFICATION.md is created at .planning/phases/02-verification/02-VERIFICATION.md with YAML frontmatter (status: passed, phase: "02"), all verification results for check:changed/tsgo/focused tests/full subsystem tests, pre-existing environmental failure documented, requirements checklist with all 10 requirements marked Verified, and conclusion stating Phase 2 complete and ready for Phase 3.</done>
</task>

</tasks>

<verification>
VERIFICATION.md report written with all Phase 2 evidence. All 5 Phase 2 success criteria met: check:changed passes, typecheck (tsgo lanes) passes, focused tests pass, ratchets pass, full subsystem tests pass.
</verification>

<success_criteria>

- All 3 plans in Phase 2 complete
- 02-VERIFICATION.md exists with full evidence
- All 10 v1 requirements verified as passing
- Pre-existing environmental failure documented
- Phase 2 ready for Phase 3 (Landing)
  </success_criteria>

<output>
Create .planning/phases/02-verification/02-VERIFICATION.md with consolidated verification results
</output>
