---
phase: 08-browser-media-whatsapp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/browser/pw-session.browserless.live.test.ts
  - src/test-utils/live-test-reporter.ts
autonomous: false
user_setup:
  - service: browserless
    why: "Local CDP endpoint for browser session testing"
    env_vars:
      - name: OPENCLAW_LIVE_BROWSER_CDP_URL
        source: "Docker: docker run -p 3000:3000 browserless/chrome — then set to ws://localhost:3000"

must_haves:
  truths:
    - "Browserless CDP live test passes against local Docker instance"
    - "Live test reporter includes browser test in its key map"
  artifacts:
    - path: "src/browser/pw-session.browserless.live.test.ts"
      provides: "Browser CDP live test using describeLive pattern"
    - path: "src/test-utils/live-test-reporter.ts"
      provides: "LIVE_TEST_KEY_MAP entry for OPENCLAW_LIVE_BROWSER_CDP_URL"
      contains: "OPENCLAW_LIVE_BROWSER_CDP_URL"
  key_links:
    - from: "src/browser/pw-session.browserless.live.test.ts"
      to: "src/browser/pw-ai.ts"
      via: "dynamic import of pw-ai module"
      pattern: "import.*pw-ai"
---

<objective>
Verify the existing Browserless CDP live test passes against a local Docker Browserless instance.

Purpose: Confirm browser session integration (create, list, focus, close tabs) works end-to-end with a real CDP endpoint.
Output: Passing live test when `OPENCLAW_LIVE_BROWSER_CDP_URL` is set and Docker Browserless is running.
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/browser/pw-session.browserless.live.test.ts
@src/test-utils/live-test-helpers.ts
@src/test-utils/live-test-reporter.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify existing browser live test and ensure reporter coverage</name>
  <files>src/browser/pw-session.browserless.live.test.ts, src/test-utils/live-test-reporter.ts</files>
  <action>
    1. Read `src/browser/pw-session.browserless.live.test.ts` — it already uses `describeLive` with `OPENCLAW_LIVE_BROWSER_CDP_URL`. The test creates, lists, focuses, and closes tabs via Playwright CDP.

    2. Check `src/test-utils/live-test-reporter.ts` LIVE_TEST_KEY_MAP — verify it has an entry for `OPENCLAW_LIVE_BROWSER_CDP_URL`. It already does (keys: ["OPENCLAW_LIVE_BROWSER_CDP_URL"], files: ["src/browser/pw-session.browserless.live.test.ts"]).

    3. Run the live test with the env var set:
       ```
       OPENCLAW_LIVE_TEST=1 OPENCLAW_LIVE_BROWSER_CDP_URL=ws://localhost:3000 bun run vitest run --config vitest.live.config.ts src/browser/pw-session.browserless.live.test.ts
       ```

    4. If the test fails due to Docker not running or connection issues, that is an environmental issue (not a code bug). If it fails due to code issues (import errors, API mismatches), fix the test.

    5. If the test passes or skips cleanly (Docker not available), no code changes needed. The existing test file is already correct.
  </action>
  <verify>
    Run `OPENCLAW_LIVE_TEST=1 OPENCLAW_LIVE_BROWSER_CDP_URL=ws://localhost:3000 bun run vitest run --config vitest.live.config.ts src/browser/pw-session.browserless.live.test.ts` — test either passes (Docker running) or skips with clear message (Docker not running).
  </verify>
  <done>Browser CDP live test passes when Docker Browserless is running, or skips cleanly when it is not.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify browser live test passes against Docker Browserless</name>
  <files>src/browser/pw-session.browserless.live.test.ts</files>
  <action>
    Human verifies the Browserless CDP live test against local Docker instance.
    1. Start Docker Browserless: `docker run -p 3000:3000 browserless/chrome`
    2. In another terminal, run:
       `OPENCLAW_LIVE_TEST=1 OPENCLAW_LIVE_BROWSER_CDP_URL=ws://localhost:3000 bun run vitest run --config vitest.live.config.ts src/browser/pw-session.browserless.live.test.ts`
    3. Confirm the test passes (creates, lists, focuses, closes tabs).
    4. Stop Docker and run again — confirm it skips cleanly.
  </action>
  <verify>Test passes with Docker running, skips cleanly without.</verify>
  <done>Browser CDP live test verified against real Docker Browserless instance.</done>
</task>

</tasks>

<verification>
- `bun run vitest run --config vitest.live.config.ts src/browser/pw-session.browserless.live.test.ts` passes with Docker running
- Test skips with yellow `[live-skip]` message when `OPENCLAW_LIVE_BROWSER_CDP_URL` is not set
</verification>

<success_criteria>
Browserless CDP live test passes against local Docker Browserless, confirming tab create/list/focus/close operations work end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/08-browser-media-whatsapp/08-01-SUMMARY.md`
</output>
