---
phase: 07-agent-gateway-live-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gateway/gateway-cli-backend.live.test.ts
  - src/gateway/gateway-models.profiles.live.test.ts
autonomous: false

must_haves:
  truths:
    - "Gateway CLI backend live test passes — spawns gateway, connects client, runs agent pipeline, gets correct response"
    - "Gateway model profiles live test passes — spawns gateway, iterates models with profile keys, gets meaningful responses"
  artifacts:
    - path: "src/gateway/gateway-cli-backend.live.test.ts"
      provides: "Gateway CLI backend integration test"
    - path: "src/gateway/gateway-models.profiles.live.test.ts"
      provides: "Gateway model profiles integration test"
  key_links:
    - from: "src/gateway/gateway-cli-backend.live.test.ts"
      to: "src/gateway/server.ts"
      via: "startGatewayServer import"
      pattern: "startGatewayServer"
    - from: "src/gateway/gateway-cli-backend.live.test.ts"
      to: "src/gateway/client.ts"
      via: "GatewayClient WebSocket connection"
      pattern: "GatewayClient"
    - from: "src/gateway/gateway-models.profiles.live.test.ts"
      to: "src/gateway/server.ts"
      via: "startGatewayServer import"
      pattern: "startGatewayServer"
    - from: "src/gateway/gateway-models.profiles.live.test.ts"
      to: "src/agents/pi-model-discovery.ts"
      via: "discoverModels/discoverAuthStorage"
      pattern: "discoverModels|discoverAuthStorage"
---

<objective>
Run both gateway live tests, diagnose failures, and fix code until they pass.

Purpose: Satisfy GATE-01 and GATE-02 — gateway CLI backend and model profiles tests green against real services.
Output: Both gateway live test files passing with self-contained gateway lifecycle (spawn in beforeAll, teardown in afterAll).
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-test-infrastructure/06-01-SUMMARY.md
@src/test-utils/live-test-helpers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose and fix gateway live tests</name>
  <files>
    src/gateway/gateway-cli-backend.live.test.ts
    src/gateway/gateway-models.profiles.live.test.ts
  </files>
  <action>
Run both gateway live test files individually using `bun run test:live <file>` with appropriate env vars. Diagnose each failure:

**Gateway CLI backend test (GATE-01):**

- Test spawns a gateway via `startGatewayServer(port, opts)`, connects via WebSocket, sends agent request, validates response
- Per locked decision: gateway auto-spawns in test, uses a dynamically allocated free port (not the fixed 13337 — the test already uses `getFreeGatewayPort()`)
- Per locked decision: if gateway fails to start, that is a real bug to fix
- Check `startGatewayServer` signature matches what the test calls — the test passes `(port, { bind, auth, controlUiEnabled })` but the Zai fallback section calls `startGatewayServer({ configPath, port, token })` — verify both call signatures are supported
- Check `GatewayClient` constructor accepts the fields the test passes (`clientName`, `clientVersion`, `mode`, `onHelloOk`, etc.)
- Check `client.request("agent", ...)` and `client.request("sessions.reset", ...)` work correctly
- The CLI backend test requires `OPENCLAW_LIVE_CLI_BACKEND=1` and a working `claude` CLI command

**Gateway model profiles test (GATE-02):**

- Much more complex — spawns gateway, iterates modern models, runs prompt/tool/image probes per model
- Uses `runGatewayModelSuite` helper with temp config, temp agent dir, temp state dir
- Check that `startGatewayServer` call in the Zai fallback path matches current API (it passes `{ configPath, port, token }` which may differ from main path's `(port, opts)` signature)
- Check model discovery, auth profile resolution, and session management work through the gateway
- Requires `OPENCLAW_LIVE_GATEWAY=1` or `CLAWDBOT_LIVE_TEST=1`

For both tests, apply the failure strategy: assume our bug first. Fix code issues in the test files or in the source files they import. If `startGatewayServer` signature has changed, update the test calls to match. If `GatewayClient` API has changed, update accordingly.

Do NOT add new tests. Only fix existing code to make tests pass.
</action>
<verify>
Run each test file individually:

- `OPENCLAW_LIVE_TEST=1 OPENCLAW_LIVE_CLI_BACKEND=1 bun run test:live src/gateway/gateway-cli-backend.live.test.ts`
- `OPENCLAW_LIVE_TEST=1 OPENCLAW_LIVE_GATEWAY=1 bun run test:live src/gateway/gateway-models.profiles.live.test.ts`

Each must either pass or skip with a clear describeLive message.
Also run `bun run check` to confirm no type/lint regressions.
</verify>
<done>Both gateway live test files pass (or skip with clear messages). Gateway spawns/tears down cleanly. No type or lint regressions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify gateway live test results</name>
  <files>src/gateway/*.live.test.ts</files>
  <action>
Human verifies that both gateway live tests pass against real services. Run the gateway tests and confirm each shows pass or clear skip (not failure).
  </action>
  <verify>Run gateway live tests with appropriate env vars and check results.</verify>
  <done>Both gateway tests confirmed passing or skipping with clear messages.</done>
  <what-built>Both gateway live tests diagnosed and fixed. Tests should now pass with self-contained gateway lifecycle.</what-built>
  <how-to-verify>
    1. Run `OPENCLAW_LIVE_TEST=1 OPENCLAW_LIVE_CLI_BACKEND=1 bun run test:live src/gateway/gateway-cli-backend.live.test.ts`
    2. Run `OPENCLAW_LIVE_TEST=1 OPENCLAW_LIVE_GATEWAY=1 bun run test:live src/gateway/gateway-models.profiles.live.test.ts`
    3. Verify both tests show pass or clear skip
    4. Review any code changes for reasonableness
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Both gateway test files run without unhandled failures
- `bun run check` passes (no type/lint regressions)
- Gateway spawns on dynamic port and tears down cleanly
- Client connects, sends requests, receives correct responses
</verification>

<success_criteria>
Requirements GATE-01 and GATE-02 satisfied: gateway CLI backend and model profiles live tests pass against real services, with self-contained gateway lifecycle management.
</success_criteria>

<output>
After completion, create `.planning/phases/07-agent-gateway-live-tests/07-02-SUMMARY.md`
</output>
