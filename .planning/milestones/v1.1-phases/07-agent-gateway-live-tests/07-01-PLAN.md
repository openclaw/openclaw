---
phase: 07-agent-gateway-live-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents/anthropic.setup-token.live.test.ts
  - src/agents/google-gemini-switch.live.test.ts
  - src/agents/minimax.live.test.ts
  - src/agents/zai.live.test.ts
  - src/agents/pi-embedded-runner-extraparams.live.test.ts
  - src/agents/models.profiles.live.test.ts
autonomous: false

must_haves:
  truths:
    - "Anthropic setup-token live test passes with valid credentials"
    - "Gemini switch live test passes with valid GEMINI_API_KEY"
    - "MiniMax live test passes or gracefully skips on provider instability"
    - "Zai live test passes or gracefully skips on provider instability"
    - "Pi embedded extra params live test passes with valid OPENAI_API_KEY"
    - "Agent model profiles live test passes with valid credentials"
  artifacts:
    - path: "src/agents/anthropic.setup-token.live.test.ts"
      provides: "Anthropic setup-token integration test"
    - path: "src/agents/google-gemini-switch.live.test.ts"
      provides: "Gemini switch integration test"
    - path: "src/agents/minimax.live.test.ts"
      provides: "MiniMax integration test"
    - path: "src/agents/zai.live.test.ts"
      provides: "Zai integration test"
    - path: "src/agents/pi-embedded-runner-extraparams.live.test.ts"
      provides: "Pi embedded extra params integration test"
    - path: "src/agents/models.profiles.live.test.ts"
      provides: "Agent model profiles integration test"
  key_links:
    - from: "src/agents/*.live.test.ts"
      to: "@mariozechner/pi-ai"
      via: "completeSimple/streamSimple/getModel"
      pattern: "completeSimple|streamSimple|getModel"
    - from: "src/agents/*.live.test.ts"
      to: "src/test-utils/live-test-helpers.ts"
      via: "describeLive helper"
      pattern: "describeLive"
---

<objective>
Run all six agent provider live tests, diagnose failures, and fix code until they pass.

Purpose: Satisfy AGNT-01 through AGNT-06 — every agent provider integration test green against real services.
Output: All six agent provider live test files passing (or gracefully skipping secondary providers on genuine provider instability).
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-test-infrastructure/06-01-SUMMARY.md
@src/test-utils/live-test-helpers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose and fix agent provider live tests</name>
  <files>
    src/agents/anthropic.setup-token.live.test.ts
    src/agents/google-gemini-switch.live.test.ts
    src/agents/minimax.live.test.ts
    src/agents/zai.live.test.ts
    src/agents/pi-embedded-runner-extraparams.live.test.ts
    src/agents/models.profiles.live.test.ts
  </files>
  <action>
Run each of the six agent provider live test files individually using `bun run test:live <file>` with the appropriate env vars set. Diagnose each failure:

1. **Core providers (Anthropic, Gemini):** Must pass. Fix our code first (per locked decision). Common issues to check:
   - Import paths or type mismatches after recent refactors
   - API response shape changes (content block types, stop reasons)
   - Timeout values too aggressive for real API calls
   - Model IDs stale (e.g. `gemini-3-pro-preview` may have changed)

2. **Secondary providers (MiniMax, Zai):** Should pass but tolerate genuine provider instability. Use longer timeouts (30s+), more retries. If a provider's API is genuinely down/changed, add a describeLive skip with a clear message rather than letting it fail the suite.

3. **Pi embedded extra params:** Uses OpenAI's streamSimple — check that `applyExtraParamsToAgent` correctly patches the stream function and that the maxTokens=16 constraint is respected by current OpenAI API behavior (minimum may have changed).

4. **Agent model profiles:** The most complex test — iterates across all available models. Check that `discoverModels`, `getApiKeyForModel`, and `completeOkWithRetry` work correctly. Fix any model-not-found or auth resolution issues.

For each failing test, apply the failure strategy: assume our bug first. If truly a provider issue (rate limit, service down, API changed), use the describeLive skip mechanism or add tolerance (retries, skip conditions) per the provider tier decision.

Do NOT add new tests. Only fix existing code to make tests pass.
</action>
<verify>
Run each test file individually:

- `OPENCLAW_LIVE_TEST=1 bun run test:live src/agents/anthropic.setup-token.live.test.ts`
- `OPENCLAW_LIVE_TEST=1 bun run test:live src/agents/google-gemini-switch.live.test.ts`
- `OPENCLAW_LIVE_TEST=1 bun run test:live src/agents/minimax.live.test.ts`
- `OPENCLAW_LIVE_TEST=1 bun run test:live src/agents/zai.live.test.ts`
- `OPENCLAW_LIVE_TEST=1 bun run test:live src/agents/pi-embedded-runner-extraparams.live.test.ts`
- `OPENCLAW_LIVE_TEST=1 bun run test:live src/agents/models.profiles.live.test.ts`

Each must either pass or skip with a clear describeLive message (not a cryptic failure).
Also run `bun run check` to confirm no type/lint regressions.
</verify>
<done>All six agent provider live test files pass (or skip with clear messages for genuine provider-side issues). No type or lint regressions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify agent provider live test results</name>
  <files>src/agents/*.live.test.ts</files>
  <action>
Human verifies that all six agent provider live tests pass against real provider APIs. Run the full live test suite and confirm each provider test shows pass or clear skip (not failure).
  </action>
  <verify>Run `OPENCLAW_LIVE_TEST=1 bun run test:live` and check agent provider test results.</verify>
  <done>All agent provider tests confirmed passing or skipping with clear messages.</done>
  <what-built>All six agent provider live tests diagnosed and fixed. Tests should now pass against real provider APIs.</what-built>
  <how-to-verify>
    1. Run `OPENCLAW_LIVE_TEST=1 bun run test:live` and check the agent provider test results
    2. Verify each provider test shows pass or clear skip (not failure)
    3. Review any code changes for reasonableness
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- All six agent test files run without unhandled failures
- `bun run check` passes (no type/lint regressions)
- Core providers (Anthropic, Gemini) pass with valid keys
- Secondary providers (MiniMax, Zai) pass or skip with clear messages
</verification>

<success_criteria>
Requirements AGNT-01 through AGNT-06 satisfied: every agent provider integration live test passes against real services with valid API keys, or gracefully skips with a clear message on genuine provider instability.
</success_criteria>

<output>
After completion, create `.planning/phases/07-agent-gateway-live-tests/07-01-SUMMARY.md`
</output>
