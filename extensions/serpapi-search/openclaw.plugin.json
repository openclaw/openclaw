{
  "id": "serpapi-search",
  "name": "SerpApi Search",
  "description": "1 universal search tool covering web, news, flights, hotels, maps, shopping, Amazon, YouTube, scholar, finance, events and 100+ engines via SerpApi",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "apiKey": { "type": "string" },
      "hl": {
        "type": "string",
        "description": "Default language code for all searches (e.g. en, de, ja). Defaults to en."
      },
      "webSearch": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "apiKey": { "type": "string" }
        }
      }
    }
  }
}