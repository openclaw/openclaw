{
  "id": "stateful-ssh",
  "name": "Stateful SSH",
  "description": "Persistent SSH session management with state preservation.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "maxSessions": {
        "type": "number",
        "description": "Maximum number of concurrent SSH sessions",
        "default": 5
      },
      "sessionTimeoutMs": {
        "type": "number",
        "description": "Timeout for idle sessions in milliseconds",
        "default": 600000
      },
      "commandTimeoutMs": {
        "type": "number",
        "description": "Timeout for individual commands in milliseconds",
        "default": 300000
      }
    }
  }
}
