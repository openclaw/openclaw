# OpenClaw AIOnly Provider

Official OpenClaw provider plugin for AIOnly. It adds OpenAI-compatible inference with AIOnly models.

Install from OpenClaw:

```bash
openclaw plugin add @openclaw/aionly-provider
```

Configure AIOnly API credentials through your normal OpenClaw credential/profile setup, then select AIOnly models with the `aionly/...` provider prefix.
