{
  "id": "fin-data-bus",
  "name": "Data Bus",
  "description": "Unified multi-market data bus with OHLCV cache and regime detection",
  "kind": "financial",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "yahooFinanceEnabled": {
        "type": "boolean",
        "description": "Enable Yahoo Finance as free equity data source (default: true when yahoo-finance2 is installed)"
      },
      "alphaVantageApiKey": {
        "type": "string",
        "description": "Alpha Vantage API key for equity data (reserved for future use)"
      }
    }
  }
}
