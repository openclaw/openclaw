# OpenClaw Databricks Provider

Official OpenClaw provider plugin for Databricks Unity Gateway model services.

Install from OpenClaw:

```bash
openclaw plugins install @openclaw/databricks-provider
openclaw gateway restart
```

See <https://docs.openclaw.ai/providers/databricks> for setup and configuration.
