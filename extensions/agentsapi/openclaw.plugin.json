{
  "id": "agentsapi",
  "name": "OpenAI Agents API",
  "description": "OpenAI Agents API harness with hosted or self-hosted sessions.",
  "categories": ["agent-runtimes"],
  "activation": {
    "onStartup": false,
    "onAgentHarnesses": ["agentsapi"]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "environment": {
        "type": "string",
        "enum": ["openai_hosted", "self_hosted"],
        "default": "openai_hosted",
        "description": "Execution environment for new sessions. Self-hosted uses the prepared OpenClaw workspace and requires an externally connected executor. Reset existing sessions after changing this setting."
      }
    }
  }
}
