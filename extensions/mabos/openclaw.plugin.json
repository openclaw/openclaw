{
  "id": "mabos",
  "name": "MABOS - Multi-Agent Business Operating System",
  "version": "0.1.0",
  "description": "BDI cognitive architecture with ontology-based reasoning, case-based learning, and multi-business management for OpenClaw",
  "author": "Kingler Bercy",
  "license": "MIT",
  "skills": ["skills"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "ontologyDir": {
        "type": "string",
        "description": "Path to shared ontology templates"
      },
      "cbrEnabled": {
        "type": "boolean",
        "default": true,
        "description": "Enable case-based reasoning"
      },
      "cbrMaxCases": {
        "type": "number",
        "default": 10000,
        "description": "Maximum cases per business before pruning"
      },
      "bdiCycleIntervalMinutes": {
        "type": "number",
        "default": 30,
        "description": "Interval between full BDI cycles"
      },
      "reasoningMethods": {
        "type": "array",
        "items": { "type": "string" },
        "default": [
          "deductive",
          "inductive",
          "abductive",
          "bayesian",
          "cbr",
          "causal",
          "heuristic",
          "htn"
        ],
        "description": "Enabled reasoning methods"
      },
      "stakeholderApprovalThresholdUsd": {
        "type": "number",
        "default": 5000,
        "description": "Expenditure threshold requiring stakeholder approval"
      },
      "temporalModelEnabled": {
        "type": "boolean",
        "default": true,
        "description": "Enable three-date temporal model for memory items (observed_at, referenced_dates)"
      },
      "observerEnabled": {
        "type": "boolean",
        "default": true,
        "description": "Enable Observer compression pipeline for conversation context"
      },
      "observerMode": {
        "type": "string",
        "enum": ["heuristic", "llm", "hybrid"],
        "default": "hybrid",
        "description": "Observer mode: heuristic (zero API cost), llm (always refine), hybrid (heuristic first, LLM when quality threshold not met)"
      },
      "observerTokenThreshold": {
        "type": "number",
        "default": 30000,
        "description": "Token threshold before observer triggers compression"
      },
      "reflectorEnabled": {
        "type": "boolean",
        "default": true,
        "description": "Enable Reflector GC pipeline for observation log"
      },
      "reflectorTokenThreshold": {
        "type": "number",
        "default": 40000,
        "description": "Token threshold before reflector triggers observation pruning"
      },
      "cacheAwareLayoutEnabled": {
        "type": "boolean",
        "default": false,
        "description": "Enable prompt-cache-aware context layout (splits into stable systemPrompt + dynamic prependContext). Opt-in as it relies on core systemPrompt hook fix."
      }
    }
  }
}
