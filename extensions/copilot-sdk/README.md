# GitHub Copilot SDK (OpenClaw plugin)

Scaffold-only bundled plugin for the `copilot-sdk` agent harness.

See `qa/copilot-sdk-capabilities.md` and `docs/proposals/copilot-sdk-harness.md` for the planned runtime and capability details.
