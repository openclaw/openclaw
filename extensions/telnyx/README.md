# OpenClaw Telnyx Provider

Official OpenClaw provider plugin for Telnyx AI inference (OpenAI-compatible chat completions with live model discovery).

Install from OpenClaw:

```bash
openclaw plugins install @openclaw/telnyx-provider
openclaw gateway restart
```

See <https://docs.openclaw.ai/providers/telnyx> for setup and configuration.
