# Published pre-Reserve binding: transparent SQL snapshot

This is output from executing the published producer, not a current binding relabeled as old state. All data is synthetic: no real account, token, native rollout, operator state, or inference was used. The committed SQL is a complete readable schema/data snapshot, not an encoding of an opaque binary.

## Immutable source

GitHub release `v2026.9.5` was published on September 19, 2026 at 01:55:23 UTC. GitHub-verified annotated tag `ce2a56d4f41a756662328f4098e303014fdbb38c` points to commit `ec9c1a13db8938e5a3eaa51fca2e981cde2395a9`, tree `ac00d08eb2766b4fd114bee710e78a1df6c3cc03`, in `openclaw/openclaw`.

The local tag was stale: object `0fc5a023a8319e463d0481a75232d08f110296b3` pointed to `c396abe66d9d3635113ae33de31a21bd0943d959`. Neither local tags nor shared checkouts were changed. Regenerate from the immutable published commit, not a moving tag.

Published source blob IDs:

| Owner                                                       | Git blob                                   |
| ----------------------------------------------------------- | ------------------------------------------ |
| `extensions/codex/src/app-server/session-binding.ts`        | `e401a4073898fedfae66b5cf3fb6a67320686466` |
| `extensions/codex/src/app-server/session-binding-record.ts` | `e4e53172081e6f4ee33e4660700f97efa7fdd777` |
| `src/plugin-state/plugin-state-store.ts`                    | `36f943f20657373f8d45fc53da81c5bd1bcea70d` |
| `src/plugin-state/plugin-state-store.sqlite.ts`             | `1feac45a705fd383be8cf1bd4880a6bd5f4f4c5a` |
| `src/state/openclaw-state-db.ts`                            | `ea2ba293783a5446a93cff7e3c2b77bbd10cbed3` |
| `src/state/openclaw-state-db-contract.ts`                   | `53de054a9e9ff1bed9d087fd019b19d651bf2eaf` |

## Production and export procedure

1. Export that exact commit with `git archive` into an isolated, task-owned directory. Do not switch a shared checkout.
2. Execute that copy’s actual `createCodexAppServerBindingStore` over its `createPluginStateSyncKeyedStore`, with a fresh synthetic `OPENCLAW_STATE_DIR`, namespace `app-server-thread-bindings`, capacity 50,000, and `reject-new`. The adjacent producer recipe documents the inputs.
3. Call `store.mutate(identity, { kind: "set", binding })`, verify the old-reader round trip and absence of `reserveReturn`, then close the old shared database owner. The schema and row serialization come from old code.
4. Preserve the closed, checkpointed original main file (WAL is zero bytes). For inspection/export, use a private backup outside any active OpenClaw state directory.
5. Export the complete database with `sqlite3 -readonly <private-copy> .dump`. Preserve `page_size` and `auto_vacuum` before the dump, then `application_id` and `user_version` afterward. Finish with `journal_mode=WAL` and `wal_checkpoint(TRUNCATE)`. The committed `openclaw.sql` includes those pragmas.
6. Restore into a second isolated SQLite file and compare every schema object, every row in every table, version/pragmas, foreign-key checks, and integrity with the original. All comparisons passed for this snapshot. No candidate binding writer or schema generator supplies the historical data.

Execution used Node 26.8.2 and existing linked dependency installations, with TypeScript paths resolved against the old copy’s `tsconfig.json`. No package install, dependency change, migration, Doctor, Gateway, or installed updater was run. This is a **published source-owner × registered candidate-caller** test, not a complete installed-package updater or real ChatGPT subscription test.

Only two tables contain rows: `schema_meta` (one synthetic creation record, app version 2026.9.5/schema 17) and `plugin_state_entries` (one binding). Identity is main / session-1 / agent:main:session-1. The binding uses thread-1, a synthetic workspace, gpt-5.6-luna/openai, priority tier, a fixed history watermark, and the released web-search-disabled fingerprint matching the candidate configuration. Stored envelope: version 1, active state, sessionId session-1, no Reserve marker.

Missing policy metadata intentionally triggers an existing rotation policy. This test holds that policy constant rather than confusing a config change with a Reserve upgrade.

- Original SQLite SHA-256: `10d8089896422dd882c9cef70853ed0a181f4057a41f933543b182979bc46e84`
- Retained compressed original SHA-256: `f8623ccff751fd8cd678a3afdaf7e00a80dddacae8751c818618a6460c401d93`
- Committed SQL SHA-256: `5fb7bfb875593e244de9974d1234c4bc1c8c974ea02f6cde72d6fb591924175a`

The binary remains a retained proof artifact, not a runtime dependency of this regression. The SQL exposes the complete schema and data to review; nothing is omitted or concealed in an encoded payload. Reconstitution can change physical page layout while preserving the verified schema, rows, and version. Creation timestamps are generated by the old owner, so regeneration is logically reproducible, not byte-identical. Verify provenance before changing hashes.

## Candidate proof

`run-attempt.reserve-upgrade.test.ts` checks the SQL hash, restores the historical schema and data into isolated SQLite state, admits normal schema upgrades through the core database owner while asserting every historical binding-row field survives unchanged, and reads through the candidate plugin owner without rewriting the admitted file. It exercises `plugin.register`’s actual registered harness with the real Codex client serialization/retry/lifecycle owner. Only the native transport and account handoff are synthetic.

The original physical database output also passed the same three scenarios before switching the committed fixture to readable SQL. The committed regression checks logical schema/data compatibility, not byte-identical historical page layout. It covers ordinary resume, offered entry, reopen, guarded recovery, explicit new-session escape, no Reserve-specific schema/version change after core admission, and integrity. It does not establish genuine eligibility, billing, or policy acceptance.
