{
  "id": "memory-external-reranker",
  "name": "Memory External Reranker",
  "description": "OpenClaw memory reranker plugin that proxies to a Cohere-compatible /v1/rerank endpoint (Cohere, Jina, Voyage, llama.cpp).",
  "activation": {
    "onStartup": false
  },
  "kind": "memory",
  "contracts": {
    "tools": [],
    "memoryRerankers": ["memory-external-reranker"]
  },
  "uiHints": {
    "model": {
      "label": "Model",
      "placeholder": "cohere/rerank-english-v3.0",
      "help": "Reranker model id, formatted as 'providerId/model'"
    },
    "modelFallbacks": {
      "label": "Model Fallbacks",
      "advanced": true,
      "help": "Ordered list of alternate 'providerId/model' candidates tried on failure"
    },
    "endpointPath": {
      "label": "Endpoint Path",
      "placeholder": "/v1/rerank",
      "advanced": true,
      "help": "HTTP path appended to the provider baseUrl (default '/v1/rerank')"
    },
    "topN": {
      "label": "Top N",
      "advanced": true,
      "help": "Maximum number of results returned by the rerank endpoint"
    },
    "providers": {
      "label": "Providers",
      "advanced": true,
      "help": "Map of providerId → { baseUrl, apiKey } credentials"
    }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "model": { "type": "string" },
      "modelFallbacks": {
        "type": "array",
        "items": { "type": "string" }
      },
      "endpointPath": { "type": "string" },
      "topN": { "type": "integer", "minimum": 1 },
      "providers": {
        "type": "object",
        "additionalProperties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "baseUrl": { "type": "string" },
            "apiKey": { "type": "string" }
          },
          "required": ["baseUrl"]
        }
      }
    }
  }
}
