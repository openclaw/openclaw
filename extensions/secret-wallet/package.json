{
  "name": "@baekho-lim/secret-wallet",
  "version": "0.2.0",
  "description": "OpenClaw plugin for secret-wallet with selective inject and gated write tools",
  "files": [
    "index.ts",
    "src",
    "dist",
    "openclaw.plugin.json",
    "SKILL.md",
    "README.md"
  ],
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "npm run clean && tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "pack:smoke": "TMPDIR=$(mktemp -d) && npm pack --pack-destination \"$TMPDIR\" >/dev/null && TAR=$(ls -1 \"$TMPDIR\"/*.tgz | head -1) && tar -tzf \"$TAR\" | grep -q '^package/index.ts$' && tar -tzf \"$TAR\" | grep -q '^package/src/tools.ts$' && tar -tzf \"$TAR\" | grep -q '^package/openclaw.plugin.json$'",
    "prepack": "npm run build",
    "test": "vitest run -c vitest.config.ts"
  },
  "dependencies": {
    "@sinclair/typebox": "^0.34.47"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  },
  "peerDependencies": {
    "openclaw": ">=2026.1.29"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
