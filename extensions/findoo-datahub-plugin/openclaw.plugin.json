{
  "id": "findoo-datahub-plugin",
  "name": "Findoo DataHub",
  "description": "Unified financial data source — 172 endpoints covering equity (A/HK/US), crypto, macro, derivatives, index, ETF. Works out of the box.",
  "kind": "financial",
  "version": "2026.3.2",
  "skills": ["./skills"],
  "configSchema": {
    "type": "object",
    "properties": {
      "datahubApiKey": {
        "type": "string",
        "description": "DataHub API key (required).",
        "sensitive": true
      }
    }
  },
  "uiHints": {
    "datahubApiKey": {
      "label": "API Key",
      "help": "DataHub API 密钥（必填）。",
      "sensitive": true,
      "placeholder": "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
    }
  }
}
