# NAS plugin

Bundled OpenClaw plugin that adds owner-only NAS tools:

- `nas_list`
- `nas_search`
- `nas_read`
- `nas_summary`

Default root path is `/mnt/nas` (override with `plugins.entries.nas.config.root` or `OPENCLAW_NAS_ROOT`).
