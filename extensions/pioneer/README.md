# OpenClaw Pioneer Provider

Official OpenClaw provider plugin for Pioneer.

Install from OpenClaw:

```bash
openclaw plugins install @openclaw/pioneer-provider
openclaw gateway restart
```

See <https://docs.openclaw.ai/providers/pioneer> for setup and configuration.
