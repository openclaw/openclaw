# NewsAPI Extension

Add NewsAPI.org integrations to OpenClaw.

## Configuration

In `~/.openclaw/openclaw.json`:

```json
{
  "plugins": {
    "entries": {
      "newsapi": {
        "enabled": true,
        "config": {
          "apiKey": "YOUR_API_KEY_HERE"
        }
      }
    }
  }
}
```
