{
  "name": "@openclaw/mark-plugin",
  "version": "2026.7.2",
  "private": true,
  "description": "OpenClaw session mark command plugin (bundled)",
  "type": "module",
  "devDependencies": {
    "@openclaw/plugin-sdk": "workspace:*"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
