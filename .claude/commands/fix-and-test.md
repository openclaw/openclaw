Fix the following issue in the OpenClaw codebase, then validate with `pnpm build && pnpm check && pnpm test`.
If tests pass, commit with `scripts/committer` (scoped, naming modified files explicitly).
If tests fail, revert all changes and explain what went wrong.
NEVER git push — the Dev agent handles that.

Issue: $ARGUMENTS
