Review the current state of ALL runtime patches documented in ~/.openclaw/docs/ARCHITECTURE.md §13.

For each documented patch:

1. Check if the patch is still applied in the dist files
2. Check if it's still needed (has it been fixed upstream?)
3. Report status: ✅ Applied / ❌ Missing / ♻️ No longer needed

Also check for any unpatched dist files that should have patches.
$ARGUMENTS
