/**
 * Provider 入口模块 (Plugin SDK 包)
 * 
 * 本文件是 `src/plugin-sdk/provider-entry.ts` 的重新导出模块。
 * 它作为 Plugin SDK 包对外暴露的 Provider 插件入口点，将核心 Provider 入口功能重新导出供外部使用。
 * 
 * @see {@link ../src/plugin-sdk/provider-entry.ts} 原始实现
 */

// 直接从核心 plugin-sdk 目录重新导出所有内容
export * from "../../../src/plugin-sdk/provider-entry.js";
