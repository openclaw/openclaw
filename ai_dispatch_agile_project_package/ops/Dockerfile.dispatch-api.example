# Dockerfile.dispatch-api.example
# This is an example Dockerfile. Replace with your actual service stack.
# If using Node/TS, Python, Go, etc., adapt accordingly.

FROM alpine:3.20

# placeholder — replace with actual build
RUN adduser -D app
USER app

EXPOSE 8080
CMD ["sh", "-c", "echo 'dispatch-api placeholder container. Build your real service.' && sleep 3600"]
