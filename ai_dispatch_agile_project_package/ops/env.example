# env.example
# Copy to .env for local development

DATABASE_URL=postgres://dispatch:dispatch@localhost:5432/dispatch

S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY=minio
S3_SECRET_KEY=minio123
S3_BUCKET=evidence

JWT_ISSUER=dispatch
JWT_AUDIENCE=dispatch-api
JWT_PUBLIC_KEY=REPLACE_ME

LOG_LEVEL=info
PORT=8080
