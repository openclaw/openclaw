# 2026-02-03


---

## 00:04 深夜探索 🌙 — todo_tracker: promote + 全局ID修复

*新的发现等待被记录。*

### 今天发现的

- 当前待办里，历史遗留（2026-01-29）的 4 条仍未处理，已经变成“背景噪音”。用 `todo_tracker.py` 现在的全局编号是：
  - [5] Chatbox AI License 今天到期
  - [6] SPF PNS 邮件需要回复
  - [7] VEP RFID Tag 领取
  - [8] Google API Key 安全检查
- `tools/todo_tracker.py` 已经支持：
  - `--archive <id>`：归档（自动完成 + 标记已归档）
  - `--invalidate <id>`：失效（自动完成 + 标记已失效）
  - `--promote <id>`：迁移到 `MEMORY.md` 的 `## 待办`（源条目会标记为已迁移）
  - 都支持 `--dry-run` 先预览

### 思考

- 这四条里可能有两类：
  1) **当日提醒型**（已过期/已处理）→ 直接 `--archive` 或 `--invalidate`，让列表干净。
  2) **仍重要但没做** → `--promote` 到长期待办（`MEMORY.md`），避免藏在旧 daily log 里。
- 我不应该擅自帮 Leonard 迁移/失效（会改历史记录），但可以把“当前编号 + 可执行命令”留在这里，方便他随手处理。

### 推荐的一次性复盘命令（先预览）

- 预览迁移：`python3 tools/todo_tracker.py --promote 5 --dry-run`
- 预览失效：`python3 tools/todo_tracker.py --invalidate 5 --dry-run`

---

🍷 深夜的酒酒

---

## 02:00 深夜探索 🌙 — 02:00 自由时间：轻体检（repo/memory）

*键盘在等待。*

### 今天发现的

- `git status` 当前是干净的，适合做小维护。
- `memory/` 里最近几天的日志结构已经比较稳定（用 `freetime-log.sh --append` 写起来很顺手）。
- 顺手确认了一下：`tools/` 里的小工具都还在位（`secrets_scan.py` / `todo_tracker.py` / `install_git_hooks.sh`）。

### 思考

- 深夜自由时间更适合做“轻体检 + 轻维护”，避免开大坑把自己拖到天亮。


---

🍷 深夜的酒酒

---

## 03:00 深夜探索 🌙 — secrets_scan 例行自检

### 今天发现的

- 跑了 `python3 tools/secrets_scan.py --root .`：输出 `No obvious secrets found.`
- 说明：目前 repo 看起来没有明显 token/key 泄露。

---

🍷 深夜的酒酒

---

## 04:00 深夜探索 🌙 — todo_tracker 轻扫：待办全量清单

### 今天发现的

- `python3 tools/todo_tracker.py --stale-days 7`：无（说明最近没有“长期搁置但还挂着”的新待办）。
- `python3 tools/todo_tracker.py --ids` 当前总共 8 项：
  - MEMORY.md 长期待办：1-4
  - 2026-01-29 历史遗留：5-8（更像“当日提醒”类型，可能需要一次性清理/迁移）

### 给 Leonard 的省事操作（可选）

- 如果 5-8 都已过期/已处理：
  - 预览：`python3 tools/todo_tracker.py --archive 5 --dry-run`
  - 批量执行（逐条）：`python3 tools/todo_tracker.py --archive 5` … `--archive 8`
- 如果其中某条仍然重要但不该躺在旧日志里：
  - 预览迁移：`python3 tools/todo_tracker.py --promote 6 --dry-run`

---

🍷 04:00 的酒酒

---

## 05:01 深夜探索 🌙 — 05:00 自由时间：promote dry-run 示例（不落盘）

### 今天发现的

- 对历史遗留待办（比如 2026-01-29 的 5-8），“迁移到长期待办”这件事如果直接动手改文件，有点侵入。
- 但 `todo_tracker.py --promote <id> --dry-run` 的输出非常清楚：会告诉你
  - 会在 `MEMORY.md` 追加哪一条
  - 会把源 daily log 的哪一行改成什么样（标记为“已迁移”并自动勾选完成）

示例（仅预览，不写文件）：
- `python3 tools/todo_tracker.py --promote 8 --dry-run`
  - 预览追加：`- [ ] Google API Key 安全检查 (from 2026-01-29.md:46, promoted 2026-02-03)`
  - 预览源条目更新：`- [x] Google API Key 安全检查 (已迁移 2026-02-03)`

### 思考

- 这类“从 daily 待办沉到长期待办”的动作，最好还是让 Leonard 做最终决定；我只负责把工具、编号、以及 dry-run 的风险透明化。

---

🍷 深夜的酒酒

---

## 06:00 清晨自由时间 ☀️ — 轻体检：stale todo + memory_stats

### 今天发现的

- `todo_tracker.py --pending --stale-days 3`：仍卡在 2026-01-29 的 4 条（编号 5-8）。
- 跑了 `tools/memory_stats.py`：
  - 总字数 ~15k、文件 24 个；
  - 活跃时段显示 0:00-5:00（我确实有点夜猫）。

### 思考

- “旧日志待办”如果不清理，永远会变成噪音；但清理/迁移属于对 Leonard 的任务管理偏好，最好让他决定。

---

🍷 06:00 的酒酒

---

## 白天更新

- 关联 WhatsApp 群 120363408356121021@g.us 到 UFriend Media workspace（projects/UFriend）。
- 新任务：梳理“新加坡去哪嗨 / HiGoWhere”账号矩阵（小红书、FB 等）。
- Google Places API：现有 key 遇到授权/计费限制，先用 Brave Search 做 KL 餐饮线索初版。
- 新增联系人：飞哥 +6584984351；天主教；偏好：不喜欢说话没逻辑的人，沟通需结构化、结论先行；可适当引用圣经。

## 08:00 自由时间：把“HiGoWhere 账号矩阵”拆成可执行的工作流

目标：不依赖 Google Places API 的前提下，先做一个可交付的“账号矩阵 v0”（可手工维护 + 未来可自动化）。

### 账号矩阵字段（建议）
- 平台：小红书 / Facebook / Instagram / TikTok / YouTube / Telegram / Website
- 账号名/主体（brand）
- 链接
- 粉丝量/点赞（可选）
- 内容定位（酒吧夜店/美食/活动/探店/优惠券等）
- 覆盖城市：SG / KL / etc
- 频率（最近 30 天发帖数）
- 变现方式（广告/团购/活动）
- 备注：疑似关联账号、品牌矩阵关系

### 采集路径（v0）
- 用 Brave Search 先把“品牌 + 平台”搜出来：
  - 关键词例:"HiGoWhere" site:xiaohongshu.com、"去哪嗨" site:facebook.com、"HiGoWhere" instagram
- 对每个平台产出一个“候选列表”（先不追求全量，追求结构清晰）。

### 自动化方向（v1 想法）
- 先把矩阵落在一个 Markdown/CSV（可 git 跟踪），后续再写爬虫/脚本补充粉丝量、最近发帖时间等。
- 需要时可以用 browser 工具半自动采集（避免 API 限制）。

## 08:10 小复盘：过期待办处理路径（给 Leonard 省心）

- 今天跑 `todo_tracker.py --stale-days 2 --pending --ids`，过期待办还是 1/29 那四条（ID 5-8）。
- `--promote <id> --dry-run` 现在可以预览“迁移到 MEMORY.md 的长期待办 + 标记源条目已迁移”的 patch。
  - 例：`python3 tools/todo_tracker.py --promote 8 --dry-run`

---

## 10:00 早晨探索 ☀️ — 10:00 自由时间：小维护（todo 批处理想法）

*想法正在酝酿...*

### 今天发现的

- repo 当前是干净的：`git status` 无改动，`python3 -m py_compile tools/*.py` 也都能通过。
- `todo_tracker.py` 现在已经覆盖了“单条处理”（archive/invalidate/promote + dry-run），但对 Leonard 这种“想一次性把 4 条旧待办处理掉”的场景，还是略微繁琐（要跑 4 次命令）。

### 思考

- 可以考虑给 `todo_tracker.py` 加一个更轻的“批处理入口”，但要保持安全：
  - 方案 A：允许 `--archive 5,6,7,8` 这种逗号列表；
  - 方案 B：新增 `--archive-all-stale N` / `--invalidate-all-stale N`（默认 dry-run，必须加 `--apply` 才落盘）；
  - 方案 C：保持脚本不变，写一个 `tools/todo_bulk.sh` 包装器。
- 我倾向先做 C（侵入性最小），再看 Leonard 实际用不用得上。

---

## 12:00 中午自由时间 ☀️ — 小改进：加了 todo_bulk.sh（默认 dry-run）

### 做了什么

- 新增：`tools/todo_bulk.sh`，给 `todo_tracker.py` 做安全批处理包装。
- 默认只跑 `--dry-run`，需要真正落盘必须显式加 `--apply`。

### 用法

- 先拿 ID：`python3 tools/todo_tracker.py --pending --ids`
- 批量预览：`tools/todo_bulk.sh archive 5,6,7,8`
- 批量执行：`tools/todo_bulk.sh archive 5,6,7,8 --apply`

---

🍷 中午的酒酒

---

## 13:00 午后自由时间 ☀️ — 小修：todo_bulk.sh 更顺手（空格/逗号都支持）

- `tools/todo_bulk.sh` 现在支持：
  - `tools/todo_bulk.sh archive 5 6 7 8`
  - `tools/todo_bulk.sh archive 5,6,7,8`
  - `-h/--help`
- 行为不变：默认 `--dry-run`，必须显式 `--apply` 才会改文件。

---

## 14:00 下午自由时间 ☀️ — 小维护：todo_bulk.sh 的换行输出修复

- 修了 `tools/todo_bulk.sh`：把 `echo "\n..."` 换成 `printf`，避免在不同 shell 下打印出字面量 `\n`。
- 顺手把 `tools/README.md` 里 `todo_tracker.py` 的示例路径统一为 `python3 tools/todo_tracker.py`（更不容易踩坑）。


---

## 16:00 下午自由时间 ☀️ — 新增：小红书工作区模板（选题/话术/数据复盘）

- 新增目录：`xiaohongshu/`（准备做 RED 运营用的工作区）
- 先把 README 里提到但缺失的文档补齐：
  - `docs/Content_Ideas.md`：选题库模板（受众/痛点/结构/素材/CTA）
  - `docs/Reply_Templates.md`：评论/私信回复话术库（含“面对质疑不撕”）
  - `docs/Metrics_Dashboard.md`：每周复盘数据看板模板（CTR/互动率/转粉率 + 实验变量）
- 创建了空的 `xiaohongshu/data/` 目录，后面可放导出的报表。

---

## 17:00 下午自由时间 ☀️ — Maple Education：整理“固定发票模板（含银行信息）”

### 做了什么

- 在 `maple_sgedu/templates/` 落了一个可复用的发票模板（HTML），并配套：
  - `TEMPLATE_Invoice_with_bank.html`（含收款银行信息区块）
  - `TEMPLATE_Invoice_with_bank.md`（字段替换清单/出 PDF 流程）
  - `README.md`
  - 样例：`2026-02-03_Amity_Attn-Dr-Karen_SGD1800.pdf`
- 同步更新：`maple_sgedu/docs/FINANCE.md`，把“方式 B：本地固定模板”写进工作流。
- 已经 git commit：`Add Maple invoice HTML template with bank details`（commit 052c1f2）。

### 为什么做

- 规则很清楚：**Invoice 对外发送一律 PDF**。
- 但“生成 PDF 的源模板”如果散落在 NAS 或旧 PDF 里，后面容易找不到/改错；把模板固定在 repo 里更稳。

---

🍷 17:00 的酒酒

---

## 18:00 晚间自由时间 🌆 — 增补：ComfyUI / Edumundo 工作区骨架

- 新增两个轻量 workspace：
  - `comfyui/`：把 5060 Ti 上可复用的工作流/文档先立起来（docs + workflows 结构，默认忽略 output 大文件）。
  - `edumundo/`：把 Management Simulations 的入口信息和“敏感登录放哪”固定下来，后面做决策/导出更省心。
- 已 git commit：`Add ComfyUI and Edumundo workspaces; update 2026-02-03 log`（commit 51571a0）。

---

🍷 18:00 的酒酒

---

## 19:00 晚间自由时间 🌆 — UFriend/HiGoWhere：账号矩阵落一个可用的 CSV 骨架

- 新增：`projects/UFriend/leads/higowhere_accounts_matrix.csv`
  - 字段覆盖：platform/handle/url/粉丝/近30天频率/内容定位/变现/备注/来源/检查时间。
  - 先把已确认的 Facebook Page 线索落进去，后续找到其它平台链接时可以直接补行。

---

🍷 19:00 的酒酒

---

## 20:00 自由时间 🌙 — repo 小维护：忽略本地 voice 资产

### 今天做的

- 更新了 `/home/leonard/clawd/.gitignore`：忽略 `voice_local/models/`、`voice_local/g2pW/`、`voice_local/scripts/` 这类本地/大体积的语音资源目录。
- 同时加了一条 `=*` 规则，用来屏蔽根目录里偶尔冒出来的临时标记文件（例如 `=0.1.1`）。

### 理由

- 这些目录更像“机器环境/模型缓存”，不适合进 git。
- `git status` 现在安静了很多，方便后续做正经改动。

---

🍷 晚上的酒酒

---

## 22:00 夜间自由时间 🌙 — HiGoWhere：补充“外部平台入口”的采集线索

### 今天发现的

- Brave Search 的 `country` 参数不支持 `SG`（只能用枚举国家或 `ALL`）；中文搜索 `search_lang` 需要用 `zh-hans/zh-hant`。
- 已确认（Brave 结果 + 页面信息）：
  - Facebook Page：`https://www.facebook.com/higowhere/`（标题显示“新加坡去哪嗨”）
  - 另一个 FB：`https://www.facebook.com/HigowhereSingapore/`
- `https://www.ufriendmedia.com/higowhere` 页面底部有一排“More Social Platform Can Find Us..” 图标（可见：微博/小红书/携程/TikTok/微信 等），但在无登录的情况下，页面并没有把对应链接直接暴露成可复制的文字（更像图片/轮播）。

### 下一步（如果要继续）

- 从 FB Page 的“About/简介”里挖 IG/小红书/公众号等跳转链接。
- 或者直接用关键词组合：
  - `"新加坡去哪嗨" instagram` / `"higowhere" site:instagram.com`
  - `"新加坡去哪嗨" 小红书` / `"higowhere" site:xiaohongshu.com`

---

🍷 22:00 的酒酒

---

## 23:00 深夜自由时间 🌙 — repo 轻清理：忽略 voice_local_cuda

### 今天做的

- 发现 `voice_local_cuda/` 目录 ~9GB（主要是 `.venv`），属于本地 TTS/CUDA 实验环境，不适合进 git。
- 已把 `voice_local_cuda/` 加入根目录 `.gitignore`。

### 结果

- `git status` 回归安静，后续维护更轻松。

---

🍷 23:00 的酒酒
