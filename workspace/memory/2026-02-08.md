# 2026-02-08 (Sun)

## Free time (16:00)

- Ran workspace housekeeping and saved report:
  - `tmp/housekeeping_2026-02-08.md`
- Notable disk usage: `voice_local_cuda/` dominates (~16G), mainly two venvs:
  - `voice_local_cuda/.venv` (~9G)
  - `voice_local_cuda/.venv_xtts` (~7G)
  - (see also `voice_local_cuda/CLEANUP.md` for slimming plan)
- Checked stale todos (>7 days) via `tools/todo_tracker.py --stale-days 7`:
  - From `memory/2026-01-29.md`: Chatbox AI License 到期、SPF PNS 邮件回复、VEP RFID Tag 领取、Google API Key 安全检查
  - 没有擅自 archive/invalidate，先保留（需要 Leonard 决定哪些还 relevant）。

## Free time (17:00)

- 改进 `tools/radar_site_monitor.py`：新增 RSS/Atom 支持（配置 `rss:` 时优先走 feed），并在 `tools/README.md` 补了一段用法说明。
- 试跑发现脚本无参数、输出在末尾才 flush；后续可考虑加 `--max-sites/--timeout/--verbose` 之类参数，方便调试与控制运行时长。

## Free time (19:00)

- 新增 `tools/housekeeping_daily.sh`：把 `housekeeping.sh` 的报告命名统一成 `memory/housekeeping-YYYY-MM-DD.md`（可选 `--timestamp`），并自动维护 `memory/housekeeping_index.md`。
- 扩展 `tools/todo_scan.sh`：支持 `--out <path>` 落盘和 `-q/--quiet` 静默模式（只写文件不刷屏），并同步更新 `tools/README.md`。

---

## 20:00 晚上探索 🌆 — 自由时间：workspace housekeeping --stale-days 3

*让我想想...*

### 今天发现的

- 又跑了一次 `tools/housekeeping_daily.sh --timestamp`，输出：`memory/housekeeping-2026-02-08-2000.md`
- 当前 workspace 体积最大头：`voice_local_cuda/`（~16G），其次是 `projects/`（~408M）、`node_modules/`（~44M）
- `git status` 显示有一批未跟踪/已修改文件（主要是 memory 报告与 tools 脚本更新），暂时不动，等 Leonard 决定是否要 commit

### 思考

- housekeeping 报告现在按日索引了，后面如果要“找某天的体检结果/对比变化”会舒服很多。


---

🍷 晚上的酒酒

---

## Free time (21:00)

- 优化 `tools/radar_site_monitor.py`：加了参数（`--budget/--max-sites/--timeout/--max-new/--per-site-new` 等），默认会更快收敛，避免一次跑把所有站点/链接全抓导致卡很久。
- `tools/README.md` 补了“更快/调试”的示例用法。
- 试跑：`python3 tools/radar_site_monitor.py --max-sites 2 --budget 15 --timeout 6 -v`（无新条目时会安静退出）。

## Free time (22:00)

- 例行跑了一次：
  - `bash tools/housekeeping.sh`（dry-run）：再确认一次最大空间占用依旧是 `voice_local_cuda/` 两套 venv。
  - `bash tools/todo_scan.sh`：输出落盘到 `memory/todo-scan-2026-02-08.txt`。
  - `python3 tools/todo_tracker.py --stale-days 14`：14 天以上未完成待办 = 无。
- 当前 `git status` 仍有一批 modified/untracked（主要是 memory 报告、housekeeping 索引、tools 脚本与 radar 配置）；先不动，等 Leonard 决定怎么收敛/是否要 commit。
