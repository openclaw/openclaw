# 2026-02-01 (Sun)

## 04:00 自由时间小维护

- 发现 `projects/ddxsg.com/` 是一个独立 git repo（内含自己的 `.git/`）。
- 为了保持顶层仓库 `git status` 干净，已在顶层 `.gitignore` 中忽略：`projects/ddxsg.com/`。

## 05:00 自由时间小维护 — todo_tracker 增强（归档/失效）

- 给 `tools/todo_tracker.py` 增加了“归档/失效”机制，减少待办长期不处理带来的背景噪音：
  - `--ids`：输出待办编号（方便后续操作）
  - `--archive <id>`：将指定编号待办标记为 **已归档**（自动勾选完成，并追加日期）
  - `--invalidate <id>`：将指定编号待办标记为 **已失效**（自动勾选完成，并追加日期）
  - 默认会隐藏带“已归档/已失效”标记的条目（可用 `--include-archived` 显示）
- 同步更新了 `tools/README.md` 的用法说明。

## 07:00 自由时间：小体检 + 记录

- 检查顶层仓库：`git status` 干净（无未提交改动）。
- 扫了一眼 `memory/`：最近几天的日记/想法都在，结构挺清晰。
- 想法：我很喜欢这种“自由时间 → 小维护/小迭代 → 立刻写进 memory”的节奏，能把存在感落在可追溯的地方。

## 08:00 自由时间：待办清点（不改动，只标注风险）

- 跑了 `python3 tools/todo_tracker.py --pending --ids`，当前未完成待办主要在两处：
  - `MEMORY.md`（长期待办）：iCloud 照片备份、下载 maple education、修 Windows SSH、NAS 视频重命名。
  - `memory/2026-01-29.md`（当日待办）：Chatbox AI License 到期、SPF PNS 邮件回复、VEP RFID Tag 领取、Google API Key 安全检查。
- 观察：`2026-01-29.md` 这几个更像“当日提醒型”，现在已过去两天，可能需要 Leonard 复盘一下：
  - 该做的已经做完（可勾选）
  - 或者确实不再需要（可用 `todo_tracker.py --invalidate <id>` 标记失效，降低背景噪音）
  - 或者仍然重要（建议迁移到 `MEMORY.md` 的长期待办里）

## 18:00 自由时间：todo_tracker 增强（stale-days 过期待办清单）

- 给 `tools/todo_tracker.py` 增加了 `--stale-days N`：
  - 用于筛出 **超过 N 天仍未完成** 的“daily log 待办”（仅统计 `memory/YYYY-MM-DD.md`，不包含 `MEMORY.md`）。
  - 这样可以把“当日提醒型待办”快速捞出来复盘（做完/迁移/失效）。
- 顺手调整了参数优先级：`--stale-days` 会优先于 `--done/--pending` 生效（避免用户不小心同时带上导致看不到 stale 列表）。
- 同步更新了 `tools/README.md` 用法说明。


## 20:00 自由时间：加了一个“子仓库扫描器”小工具

- 新增 `tools/subrepo_scan.py`：扫描 `projects/`（或任意目录）下的独立 git repo（含 `.git/` 的目录），并输出建议的顶层 `.gitignore` 条目。
- 目的：让 workspace 里“多 repo 并存”这件事更可控，避免 `git status` 长期被噪音淹没。
- 同步更新 `tools/README.md`。

## 21:00 自由时间：为后续 heartbeat 批量检查做一点准备

- 新增 `memory/heartbeat-state.json`：准备以后在 heartbeat 里批量做“邮件/日历/天气/mentions”等周期检查时，用它记录上次检查时间，避免重复打扰。

## 23:00 自由时间：补了一个小同步脚本的说明

- 发现有个新脚本 `tools/sync_maple_education.sh`（rsync 从 NAS 同步到本地工作目录），给 `tools/README.md` 补上了用法说明，方便以后直接跑。
