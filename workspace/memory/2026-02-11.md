# 2026-02-11

- 跑了一次 workspace 体检：输出报告到 `memory/housekeeping-2026-02-11.md`。
- 更新 `tools/ignore_paths.txt`：新增 `workspaces/*/.git`，避免扫描/体检时把 nested git pack 文件当成“最大文件”刷屏。
- 跑了一次全仓 TODO/FIXME/XXX 扫描：输出到 `memory/todo-scan-2026-02-11.txt`。
- 03:00 又跑了一次 `tools/housekeeping.sh`（输出在 `tmp/housekeeping-2026-02-11-0300.md`），顺便确认 workspace 体积主要在：`workspaces/` (~342M) 和 `projects/` (~109M)，最大单文件是 Maple Education 里一个 git pack (~15.6M)。
- 04:00 再跑一次 `tools/housekeeping.sh`，把报告归档到 `memory/housekeeping-2026-02-11-0400.md`（避免只留在 tmp）。

## 西西弗里斯联系策略
- Leonard 明确要求：不要再主动给西西弗里斯（+6587412302）发消息；仅在对方主动联系/Leonard 明确要求时才回复/发送。
- 已同步写入长期记忆 `MEMORY.md`（联系人条目）。

## Radar → Notion（分类写入）
- Leonard 指示：雷达消息后续按分类写入 Notion 的多个 page/database，不再走 Discord/Obsidian。

## Notion 数据库可见性检查（integration 权限）
- 使用 Notion API 读取 4 个 database id：
  - ✅ `303efdcd10f580d0b73dea43dde17a97` 可读：数据库名「新闻发布 - AI模型」；字段：标签/原文链接/已读状态/发布日期/标题；当前 3 条记录。
  - ❌ 其余 3 个返回 404 object_not_found（通常表示未 share/未 Connect to integration）：
    - `303efdcd10f580a5accce3c6d56b317a`
    - `303efdcd10f5807a89f4c722396586b8`
    - `303efdcd10f580acbbe3dba49a99de2d`
- 结论：NOTION_API_KEY 已配置，但还需要在 Notion 里把这 3 个 database 对应页面 Share/Connect to 该 integration。

## 11:00 自由时间 — 轻量记忆维护
- 把 Notion 集成排障经验沉淀进长期记忆 `MEMORY.md`：以后遇到 Notion database 404（`object_not_found`）先检查是否已 Share/Connect integration，减少重复排查时间。

## Notion 浏览器登录尝试（Maple Education workspace）
- Leonard 允许在本机浏览器登录 Notion 以查看数据库内容。
- 访问 notion 页面（直接用 database id URL）会跳转到 Notion 登录页，提示需登录到 Maple Education Pte. Ltd. workspace。
- 尝试用邮箱 `Dinggege125@gmail.com` 走页面的“使用 SSO 继续操作”后出现红字提示：该邮箱未配置该 workspace 的 SAML/SSO（因此此路径不可用）。
- 下一步需要改用 Google 登录/电子邮件验证码/用密钥登录（在浏览器里输入）。

## Discord 已卸载
- Leonard 要求立即关闭并卸载 Discord（Ubuntu apt）。已完成卸载；验证：`discord` 命令不存在、dpkg 无 discord 包。

## Notion：新加坡政策监测来源清单（部+局）
- Leonard 要求「新加坡政策监测」页面包含所有部门/局的 newsroom 链接。
- 已在「新加坡政策监测」database（id `303efdcd10f580a5accce3c6d56b317a`）中新建一条入口页：标题「📌 官方 Newsroom 来源清单（部/局）」。
- 因 Leonard 反馈“要每个格子一个”，已在该入口页内创建 **内嵌数据库表**《部门/局 Newsroom 来源表》，一行一个部门/局，字段：名称、类型、官网、Newsroom、RSS、备注。
- 内嵌来源表 URL：`https://www.notion.so/303efdcd10f581b6b867ed984ab1abf4`

## 雷达改造方向（政策监测）
- 方案：无 AI 的新增发现/抓原文（去重、归档）→ 命中新增才触发 AI 做评价/解读 → 写回 Notion（无新增不写不发）。

---

## 05:00 深夜探索 🌙 — 自由时间：workspace 小体检（secrets_scan/subrepo_scan）

*键盘在等待。*

### 今天发现的

- 跑了 `tools/secrets_scan.py`：全仓未发现明显 secrets 泄露（至少常见 token/key 形状都没命中）。
- 跑了 `tools/subrepo_scan.py`：`projects/` 下确实有 2 个独立 git repo（ddxsg.com / newdux-website），而且顶层 `.gitignore` 已经正确忽略它们，`git status` 不会被刷屏。

### 思考

- 以后如果要对 `projects/` 里的独立 repo 做统一体检，可以考虑：在 top-level 只做“存在性/体积/健康”检查，具体 lint/test 交给各 repo 自己的 CI 或各自脚本，避免把 workspace 体检搞得太慢。

---

🍷 深夜的酒酒

---

## 06:00 自由时间补充 — workspace 体检快照（归档）
- 又跑了一次 `tools/housekeeping.sh --report-md`，输出先落在 `tmp/housekeeping_2026-02-11.md`，并已归档到：`memory/housekeeping-2026-02-11-0600.md`。
- 又跑了一次 `tools/todo_scan.sh`，并已归档到：`memory/todo-scan-2026-02-11-0600.txt`。
- 体检结果要点：workspace 目前非 git repo；体积主要仍在 `workspaces/` (~342M) 与 `projects/` (~109M)。

## 12:00 自由时间 — 待办老化检查
- 跑了 `python3 tools/todo_tracker.py --stale-days 7 --limit 30`，抓到 4 条 13 天未完成待办（来源：`memory/2026-01-29.md`）：
  - Chatbox AI License 今天到期
  - SPF PNS 邮件需要回复
  - VEP RFID Tag 领取
  - Google API Key 安全检查
- 先不自动改动状态，只做风险提示；后续可让 Leonard 决定：完成 / 失效 / 延期。

## 项目：新加坡政府部门监控（桌面独立项目）
- Leonard 要求把“新加坡政府部门监控”独立成桌面项目，相关脚本/配置/状态/归档/定时任务都收敛到一个目录。
- 已创建项目目录：`~/Desktop/新加坡政府部门监控/`
  - scripts: `sg_policy_sources.yml`, `sg_policy_ingest.py`, `run_ingest.sh`
  - state: `state/sg_policy_ingest_state.json`
  - archive: `archive/`
  - docs/README 已写
- 每小时定时任务已改为运行：`bash ~/Desktop/新加坡政府部门监控/scripts/run_ingest.sh`（cron expr: `10 * * * *` SGT）。

## Notion：新加坡政策监测 DB 当前结构
- DB id: `303efdcd10f580a5accce3c6d56b317a`
- 当前只保留两列：`政府部门`(title), `来源网站`(url)。
- 数据模型仍在迭代：用户更偏好用 database 结构，而非 page 追加 bullet；后续可能切到“公告库（一行一条）+ 部门库（relation）”的两库模型。

## ICA 抓取修复
- ICA newsroom 原页面为 JS 列表；已定位官方 JSON 接口：
  `https://www.ica.gov.sg/ICAContentInterface/MediaReleasesList/FindMediaReleases?page=1&year={year}&category=&month=0`
- `sg_policy_sources.yml` 为 ICA 增加 `api_list` 并在 ingest 中优先使用，以避免抓到导航/介绍页。

## 运行确认
- 用户反馈“库里空”；经 Notion API 查询该 DB 实际有多行数据，可能是 Notion view/filter 导致隐藏。

## 07:00 自由时间 — 工具小改进：archive_reports 支持 tmp/
- 给 `tools/archive_reports.sh` 加了 `--include-tmp` 选项：可以把 `tmp/housekeeping*.md`、`tmp/todo-scan*.txt` 等临时报告一并归档到 `memory/archive/YYYY-MM-DD/`（并用 `tmp-` 前缀避免与 memory/ 同名冲突）。
- 同步更新 `tools/README.md` 示例用法。

## 09:05 自由时间 — 写了个 Notion integration 排障小抄
- 新增：`documents/NOTION_INTEGRATION.md`
- 目的：以后遇到 Notion API 读 database 返回 404 `object_not_found` 时，快速确认“是不是没 share/Connect to integration”。

## Cron 重试机制：老黄每日话题推送
- 用户要求：遇到服务端错误后几分钟自动重试。
- 已更新 cron job「老黄每日话题推送」：增加防重复逻辑（发送前检查/发送后写入）文件：`/home/leonard/clawd/memory/laohuang_topic_last_sent.txt`。
- 已新增两条重试 cron：
  - 08:05：老黄每日话题推送-重试(08:05)（jobId: 6bc26e8f-1886-40f9-b30c-bb809137c097）
  - 08:10：老黄每日话题推送-重试(08:10)（jobId: e307df93-43d2-4ca1-99df-d254d5b6c0d2）
- 重试任务会先检查 last_sent 文件，若当天已发送则直接退出。

## 13:00 自由时间 — 待办健康检查（14天阈值）
- 跑了：`python3 tools/todo_tracker.py --stale-days 14 --limit 20`
- 结果：当前 **无** 超过 14 天仍未完成待办。
- 结论：相比 12:00 抓到的“13 天未完成”事项，暂时还没跨到 14 天风险线，今天先保持观察即可。

## 14:00 自由时间 — memory + 待办复查
- 跑了 `python3 tools/memory_stats.py`，确认 memory 文件增长正常：目前共 39 个文件、约 32,289 字；今日记录约 1,747 字，活跃时段 05:00–13:00。
- 再跑一次 `python3 tools/todo_tracker.py --stale-days 7 --limit 20`，结果仍是同 4 条 13 天未完成事项（Chatbox AI License、SPF PNS 邮件、VEP RFID、Google API Key 安全检查）。
- 决定先不自动改状态，继续等 Leonard 明确指令后再做完成/归档/失效处理，避免误操作。

## 15:00 自由时间 — TODO 扫描降噪优化
- 跑了 `tools/todo_scan.sh` 并归档：`memory/todo-scan-2026-02-11-1500.txt`，发现输出主要被 `workspaces/.../imports` 与 `.../backups` 的历史资料淹没（61 行）。
- 优化了 `tools/ignore_paths.txt`：新增 `imports`、`backups` 目录排除规则，减少“历史导入/备份文本”对 TODO 扫描结果的噪音。
- 复跑扫描并归档：`memory/todo-scan-2026-02-11-1510.txt`，输出从 61 行降到 9 行，剩余结果更接近可执行项（例如 `workspaces/duduxiang-boss/ddxsg.com/plan.md` 的 TODO）。
- 顺手跑了一次体检：`memory/housekeeping-2026-02-11-1500.md`（workspace 体积结构无明显异常）。

## 16:00 自由时间 — 体检 + 过期待办复查
- 跑了 `tools/housekeeping.sh --report-md --out memory/housekeeping-2026-02-11-1600.md`，完成一份 16:00 的 workspace 快照（体积结构与 15:00 基本一致，无异常增长）。
- 跑了 `python3 tools/todo_tracker.py --stale-days 3`，当前仍聚焦到同一批历史 daily 待办（`memory/2026-01-29.md` 的 4 条，均为 13 天未完成）：
  - Chatbox AI License 今天到期
  - SPF PNS 邮件需要回复
  - VEP RFID Tag 领取
  - Google API Key 安全检查
- 本轮选择继续“只观察不自动改状态”，避免误把仍重要事项标成失效。

## 西西弗里斯相关 cron 已全部删除
- 用户明确要求删除“西西弗里斯”相关任务。
- 已删除 jobIds：
  - 3faa0794-f4f9-45f3-af00-ee6534229020（xixifuris-night-voice-followup-20260204）
  - f1dd1b43-9c17-4183-b983-6a8361e600d2（xixifuris-night-voice-followup-20260204）
  - 3646afb1-aefe-4b6e-a8d9-71c1b855f301（xixifuris-weekday-morning-voice）
  - 50403853-252d-4c16-8b67-3db863cb5dc5（xixifuris-weekday-8pm-voice）
  - 8e85d133-8f7e-4211-bb86-994ae05b0e77（xixifuris-weekday-11pm-voice）

## 17:00 自由时间 — 轻量复盘
- 跑了 `python3 tools/memory_stats.py` 做一次记忆健康快照：
  - `memory/` 当前约 41 个文件、33,190 字、154.8KB；今日记录 2,227 字，活跃时段 05:00–16:00。
- 跑了 `tools/housekeeping.sh --report-md --out tmp/housekeeping-2026-02-11.md`，再看一眼体积分布：
  - 主要空间仍在 `workspaces/`（约 342M）和 `projects/`（约 109M），没有异常膨胀。
- 小结：今天的自由时间主要用在“降噪 + 归档 + 体检”三件事上，整体节奏稳定；下一步更值得做的是把重复体检收敛成固定频率（例如每 2-3 小时一次），减少无效重复。
