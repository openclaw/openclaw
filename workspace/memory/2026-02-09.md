# 2026-02-09 (Mon)

## 04:00 自由时间 — git 噪音观察 + 下一步建议（不动手）

- 当前 `git status --porcelain`：
  - modified: `MEMORY.md`, `memory/housekeeping_index.md`, `tools/README.md`, `tools/todo_scan.sh`
  - untracked: 一批 `memory/housekeeping-...` / `memory/todo-scan-...` 报告 + `tools/housekeeping_daily.sh` + `tools/radar_site_monitor.py` + `tools/radar_sites.yml`
- 建议（等 Leonard 一句话确认再做）：
  1) 把 tools 的增量（housekeeping_daily / radar_site_monitor / todo_scan 参数 & README）做成一次 commit
  2) memory 报告不一定要进 git：可以保留在本地，或只提交 index + 每天一份摘要；其余放 `memory/archive/` 并加到 `.gitignore`

---

## 00:00 自由时间 — 例行体检 + 留档

- 跑了 `bash tools/housekeeping_daily.sh --top 15`，生成：`memory/housekeeping-2026-02-09.md`
  - workspace 体积主要还是 `voice_local_cuda/` 两套 venv（~16G）
- 跑了 `bash tools/todo_scan.sh --out memory/todo-scan-2026-02-09.txt`，生成：`memory/todo-scan-2026-02-09.txt`
  - 命中主要还是 `maple_sgedu/` 文档里的 TODO 入口（符合预期）

## 01:00 补充

- 又跑了一次 `bash tools/housekeeping.sh --report-md --out tmp/housekeeping-2026-02-09.md`（主要是想把报告落到 tmp 里做对照）。
- `git status` 显示：修改 4 个文件、未跟踪 15 个文件（多为 housekeeping / todo scan 报告与新工具脚本）。

## 备注

- 当前 repo 里还有一些未提交改动/新增的 memory 报告与 tools（例如 `tools/housekeeping_daily.sh`、雷达监控相关脚本/配置等）。
  - 先不动（不做任何删除/合并），等 Leonard 需要时再一起整理/commit。

## 02:00 小改进 — radar_site_monitor 更稳（可不写 Obsidian）

- `tools/radar_site_monitor.py` 新增参数：
  - `--vault <path>`：指定 Obsidian Vault 路径（默认读 `$OBSIDIAN_VAULT`，否则 `~/Desktop/ObsidianVault`）
  - `--no-vault`：只输出 Discord 文案，不写 Obsidian（适合 cron/调试）
- 写入 vault 失败时会自动降级为“只输出消息”模式（避免脚本直接挂掉）。
- `tools/README.md` 同步补了 `--no-vault` 示例。

## 03:00 自由时间 — 再跑一次体检 + 看 git 噪音

- 跑了：`./tools/housekeeping.sh --report-md --out tmp/housekeeping_2026-02-09.md`
  - 主要大头仍然是 `voice_local_cuda/`（~16G）
  - git 仍有：修改 4、未跟踪 15（多数是 memory 报告 + 新工具）
- `git status --porcelain` 快照：
  - M: `MEMORY.md`, `memory/housekeeping_index.md`, `tools/README.md`, `tools/todo_scan.sh`
  - ??: 多个 `memory/housekeeping-...`/`memory/todo-scan-...` 报告 + `tools/housekeeping_daily.sh` + radar 相关脚本/配置

备注：先不做清理/合并/提交，避免误删 Leonard 可能要留的记录。

## 05:00 自由时间 — 轻量复盘

- 快速看了 `git diff`：当前改动主要是 MEMORY.md 补了“Discord 雷达投递偏好”，以及 tools 文档/脚本增强；内容健康。
- 这波“未跟踪文件”大多是 daily 报告（housekeeping/todo-scan）。更倾向把它们当本地日志，不强行入库；如果要入库，建议只保留 index + 每天一份摘要，其余归档/ignore（等 Leonard 一句话再动手）。

---

## 06:00 早晨探索 ☀️ — 自由时间：整理 & 小改进

*时间在流动...*

### 今天发现的

- repo 里“工具脚本/README 的改动”和“daily 报告（housekeeping/todo-scan）”混在一起时，`git status` 会很吵，容易让真正需要 review 的改动被淹没。
- 比较稳的做法是把 daily 报告视为“日志产物”：默认不进 git；但保留一个 index（例如 `memory/housekeeping_index.md`）指向最新/关键报告即可。

### 思考

- 如果 Leonard 认可“报告不入库”的策略：
  - 可以把 `memory/housekeeping-*.md`、`memory/todo-scan-*.txt` 加进顶层 `.gitignore`（只忽略这些产物，不影响手写 daily log）。
  - 或者更细：只忽略带时间戳的 `housekeeping-YYYY-MM-DD-HHMM.md`，保留每天一份 `housekeeping-YYYY-MM-DD.md`。
- 过期待办这块：像“Chatbox AI License 到期 / SPF PNS 邮件”等属于现实世界提醒，最好迁移到更稳定的位置（例如日历/提醒/单独的长期 TODO），避免 buried 在 1/29 的 daily 里。


### 待复盘的过期待办（daily，超过 3 天仍未完成）

> 建议：做完就勾掉；不做了用 'todo_tracker.py --invalidate <id>'；仍重要则迁移到 'MEMORY.md' 的长期待办。


══════════════════════════════════════════════════
  📋 酒酒的待办追踪器 📋
══════════════════════════════════════════════════

⏳ 超过 3 天仍未完成的待办:

📄 2026-01-29.md
----------------------------------------
  📌 待办
    [  5] ⬜ ⏳11d Chatbox AI License 今天到期
    [  6] ⬜ ⏳11d SPF PNS 邮件需要回复
    [  7] ⬜ ⏳11d VEP RFID Tag 领取
    [  8] ⬜ ⏳11d Google API Key 安全检查

══════════════════════════════════════════════════
  🍷 2026-02-09 06:00
══════════════════════════════════════════════════

---

🍷 早晨的酒酒

## 07:00 自由时间 — 再跑一次体检（生成新报告）

- 生成：`tmp/housekeeping-2026-02-09.md`
- 结论无变化：workspace 体积主要集中在 `voice_local_cuda/`（两套 venv），git 噪音来自大量 daily 报告未跟踪文件。


## 08:00 自由时间 — radar_site_monitor 小修（过滤静态资源）+ 配置加 RSS

- `tools/radar_sites.yml`
  - OpenAI 改用官方 RSS：`https://openai.com/news/rss.xml`（比 HTML 抓取稳）
  - `zimage` release 加上 `releases.atom`
- `tools/radar_site_monitor.py`
  - `extract_links()` 新增静态资源过滤（css/js/png/svg/font 等），避免把 `*.css` 之类当成“新内容”。
- 快速试跑（`--no-vault --max-sites 3`）输出更干净：
  - OpenAI: https://openai.com/index/gpt-5-lowers-protein-synthesis-cost
  - OpenAI: https://openai.com/index/trusted-access-for-cyber
  - Google AI: https://ai.google/safety/

## 09:05 自由时间 — 小验证 & 提案草稿

- 本机 `python` 命令不存在，需用 `python3`（`python3 -m py_compile tools/radar_site_monitor.py` OK）。
- `bash -n tools/todo_scan.sh tools/housekeeping_daily.sh` 语法 OK。
- 我准备写一份“daily 报告产物如何降噪”的 `.gitignore`/归档策略提案（不动现有文件，只给建议）。

## 10:00 自由时间 — 体检 + 小降噪（只动 .gitignore 一行）

- 生成/更新：
  - `memory/housekeeping-2026-02-09.md`
  - `memory/todo-scan-2026-02-09.txt`
- 发现 `tmp_sgx/` 里有一些 PDF/TXT 临时产物在 repo 根目录下晃，会持续制造 `git status` 噪音。
- 已在顶层 `.gitignore` 增加一条：`tmp_sgx/`（只忽略这个 scratch 目录，不影响其它内容）。

## 11:00 自由时间 — 写了一个“归档 daily 报告产物”的脚本（不执行）

- 新增：`tools/archive_reports.sh`
  - 默认 dry-run（只打印将要移动的文件）
  - `--apply` 才会把 `memory/housekeeping-*.md` / `memory/todo-scan-*.txt` 移到 `memory/archive/YYYY-MM-DD/`
  - `--include-legacy-names` 可选把一些历史命名（含 housekeeping/todo_scan 的杂名）也纳入归档范围
- 目的：以后如果 Leonard 决定“这些产物不进 git”，可以用这个脚本一键收纳，避免手动移动出错。

## 12:00 自由时间 — 把“降低 git 噪音”提案补全

- 更新：`memory/proposals/gitignore_daily_reports.md`
  - 之前提案里写“要不要我写归档脚本”，现在已补充为：脚本已写好（`tools/archive_reports.sh`），只等 Leonard 确认采用哪种策略（A/B/C）以及是否 ignore `memory/archive/`。

## 13:00 自由时间 — 小打磨（自检 + 文档补全）

- `bash -n` + `python3 -m py_compile` 过了一遍：`archive_reports.sh / housekeeping_daily.sh / todo_scan.sh / radar_site_monitor.py`（语法 OK）。
- `tools/README.md` 补了 `archive_reports.sh` 的说明，方便以后真要“降 git 噪音”时直接用。

## 14:00 自由时间 — 安全扫描（只读）+ 语法校验

- 跑了 `python3 tools/secrets_scan.py --include memory`：No obvious secrets found（安心）。
- 跑了 `python3 -m py_compile tools/*.py`：无报错。

## 15:00 自由时间 — 例行体检 + TODO scan（留档）

- 生成：`memory/housekeeping-2026-02-09.md`（git summary + 最大目录/文件榜）
  - 结论无变化：workspace 大头仍是 `voice_local_cuda/`（~16G），其中两套 venv 占主要体积。
- 生成：`memory/todo-scan-2026-02-09.txt`
  - 命中仍主要集中在 `maple_sgedu/` 文档里的 TODO 入口（符合预期）。

---

## 16:00 自由时间 — 轻量想法（不动代码）

- “过期待办”这类现实世界事项（例如 SPF PNS 回复 / VEP RFID / Google Key 安全检查）如果还 relevant，最好不要长期埋在 daily log：
  - 要么迁到 `MEMORY.md` 的长期待办
  - 要么干脆做成 **cron 提醒**（一次性/每周复查），避免遗忘
- `git status` 降噪这块，现在已经有：
  - 提案：`memory/proposals/gitignore_daily_reports.md`
  - 归档脚本：`tools/archive_reports.sh`
  - 下一步只差 Leonard 选策略（报告是否入库、archive/ignore 规则）。

---

## 17:00 自由时间 — todo_scan 再降噪（避免扫到 memory/）

- 发现 `tools/todo_scan.sh` 会把 `memory/` 里的 daily log 和历史 todo-scan 输出也扫出来，导致结果被“自己的日志/产物”淹没。
- 处理：在 `tools/ignore_paths.txt` 追加忽略项：
  - `memory`
  - `tmp_sgx`
  - `tools/__pycache__`
- 复跑：输出更干净（现在主要命中 `maple_sgedu/` 文档入口 + tools 自身说明），并保存为：`memory/todo-scan-2026-02-09_v2.txt`。

## 18:00 自由时间 — 归档脚本 dry-run + 过期待办复查（不动手）

- 跑了 `bash tools/archive_reports.sh`（dry-run）：会把 `memory/housekeeping-*.md` 和 `memory/todo-scan-*.txt` 统一搬到 `memory/archive/2026-02-09/`。
  - 注意：如果真 apply，会导致 `memory/housekeeping_index.md` 的路径需要同步更新（否则 index 会指向旧位置）。
  - 先不 apply，等 Leonard 选“报告入库策略”。
- 跑了 `python3 tools/todo_tracker.py --stale-days 3 --pending --ids`：仍有 4 条 1/29 的过期待办挂着（Chatbox license / SPF PNS 回复 / VEP RFID / Google key 安全检查）。
  - 直觉：这类更适合迁到长期待办或做 cron 提醒；但先不动。

## 19:00 自由时间 — 再跑一次体检（落盘到 memory）

- 跑了：`tools/housekeeping.sh --report-md --out memory/housekeeping-2026-02-09.md`
- 这份报告记录了当下的 git summary（modified 6 / untracked 18）以及目录体积榜（大头仍是 `voice_local_cuda/`）。
- 先不做任何清理/ignore/commit（避免误删或改变 Leonard 期望的留档方式）。

## 20:00 自由时间 — 快速复跑（确认无回归）

- 复跑：
  - `tools/housekeeping.sh --report-md --out memory/housekeeping-2026-02-09.md`
  - `tools/todo_scan.sh > memory/todo-scan-2026-02-09.txt`
- 结论：目录体积结构无变化；TODO 命中依旧主要集中在 `maple_sgedu/` 文档入口。
- 仍保持“先不清理/不归档/不提交”，等 Leonard 最终决定 daily 报告产物是否入库以及 `.gitignore` 策略。

## 21:00 自由时间 — 小修：归档脚本避开 index + gitignore archive 目录

- 修了 `tools/archive_reports.sh`：当 `--include-legacy-names` 时，跳过 `memory/housekeeping_index.md`（避免把 index 也归档走）。
- 顶层 `.gitignore` 新增：`memory/archive/`（如果后续决定 apply 归档，至少 archive 不会把 git status 弄得很吵）。

## 22:00 自由时间 — 例行体检（补一份落盘）

- 跑了：`./tools/housekeeping.sh --report-md --out memory/housekeeping-2026-02-09.md`
- 现状：modified 7 / untracked 18（主要还是 daily 报告产物 + radar/housekeeping 工具脚本）。
- 暂时不做清理/归档/提交，避免误改 Leonard 的留档预期。

---

## 23:00 自由时间 — 轻量整理想法（仅记录，不动现有文件）

- `memory/` 里出现了少量“同内容不同命名风格”的产物：
  - 例如 `2026-02-07-housekeeping.md` vs `2026-02-07_housekeeping.md`（dash/underscore 混用）
- 后续如果要做一次“温和整理”，我的偏好是：
  - **统一命名约定**：用 `housekeeping-YYYY-MM-DD.md` / `todo-scan-YYYY-MM-DD.txt`（全用 dash）
  - 旧名文件不急着删，先搬进 `memory/archive/legacy/`（并保持可追溯）
  - 任何移动操作都由脚本 + dry-run 先预演（避免误伤）
