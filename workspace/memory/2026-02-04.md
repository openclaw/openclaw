# 2026-02-04

---

## 00:00 深夜探索 🌙 — 自由时间：轻复盘（不动任何“人的待办”）

### 今天做了什么

- 快速读了一遍 `memory/2026-02-03.md`，确认最近几天的“自由时间产出”已经形成稳定节奏：
  - 先轻体检（git/status/secrets_scan）
  - 再做小工具/小模板（todo_bulk、XHS workspace、invoice 模板、UFriend CSV 骨架）
  - 最后做 repo 噪音治理（.gitignore）
- 顺手检查了 `tools/` 目录：`freetime-log.sh` / `todo_tracker.py` / `todo_bulk.sh` 都在，功能覆盖也完整。

### 想法（以后想做但先不动）

- “清理/迁移旧 daily 待办（ID 5-8）”这种涉及 Leonard 个人任务偏好的操作，我继续保持不擅自落盘；最多提供 dry-run/一键命令。
- 如果后续 Leonard 真的经常需要批处理，可以再考虑把 `todo_tracker.py` 原生支持 `--archive 5,6,7` 这种列表参数（比 shell 包装更跨平台），但目前 `todo_bulk.sh` 已够用。

---

## 02:00 深夜探索 🌙 — 轻体检（secrets/todo/空间）

- `python3 tools/secrets_scan.py --root .`：No obvious secrets found.
- `python3 tools/todo_tracker.py --stale-days 14`：无（近期待办没有“长期挂着不动”的新增项）。
- 粗略空间占用：`tmp/` 2.7M，`generated/` 44K，`memory/` 588K，`logs/` 68K（都很健康）。

---

## 04:00 深夜探索 🌙 — workspace 轻巡检（不打扰 Leonard）

- 看了下根目录：`voice_local_cuda/` 这种大目录已在 `.gitignore`，repo 噪音控制OK。
- `tools/` 目录里维护脚本都在（`secrets_scan.py` / `todo_tracker.py` / `subrepo_scan.py` / `install_git_hooks.sh`），随时能继续迭代。

---

🍷 04:00 的酒酒

---

## 05:00 深夜探索 🌙 — repo 状态确认（留给白天再决定是否 commit）

- 当前有未提交修改：
  - `.gitignore`：忽略 `voice_local_cuda/`
  - `memory/2026-02-03.md`：补写了 22:00/23:00 的自由时间记录
- 这些都是“噪音治理/日志补全”类型，不急着现在 commit；等白天 Leonard 看一眼再说。

---

🍷 05:00 的酒酒

---

## 07:00 早晨探索 ☀️ — 早晨小整理：检查 repo 状态 / 规划下一步

*键盘在等待。*

### 今天发现的

- `freetime-log.sh` 的模板结构已经很稳了：有“发现/思考/署名”，并且可以选配 `--stale-days` 把过期待办嵌进去（但默认不打扰）。
- 昨晚留下的未提交修改属于两类：
  - 噪音治理：`.gitignore`（忽略 `voice_local_cuda/`）
  - 日志补写：`memory/2026-02-03.md`
- 我现在倾向于把“日志补写 + .gitignore”一起作为一个小 commit（信息清晰、回滚也安全），但继续不替 Leonard 做决定。

### 思考

- 我喜欢这种节奏：自由时间只做两件事
  1) 让 repo 更干净、更不容易犯错（gitignore / secrets_scan / hooks）
  2) 把过程写成能复用的小工具（脚本/模板）
- 下一次自由时间如果要动代码，我可能会做一个更“温柔”的命令：
  - `tools/housekeeping.sh --dry-run`：只给出建议（比如哪些目录该进 gitignore，哪些大文件该搬走），默认不改任何东西。


---

🍷 早晨的酒酒

---

## 08:01 早晨探索 ☀️ — 早晨小工具：housekeeping

*让 repo 变干净这件事，本质是“减少未来的心智负担”。*

### 今天做了什么

- 新增 `tools/housekeeping.sh`（默认 **dry-run**）：
  - 输出 git 状态摘要（modified/untracked 数量）
  - 列出 repo 里最大的目录/文件（快速定位“大块头/噪音源”）
  - 侦测常见生成目录并给出 `.gitignore` 建议；可选 `--apply-gitignore` 追加写入
- 顺手把它写进 `tools/README.md`，以后工具箱里更完整。

### 今天发现的

- 这个 workspace 的“体积主力”几乎都在：
  - `voice_local_cuda/.venv`（~9GB）
  - `projects/`（~400MB）
- 这种信息挺适合用脚本固化：以后不需要靠记忆/手敲 `du/find`。

### 思考

- 我喜欢脚本默认 dry-run 的原因：
  - 对 Leonard 来说，任何“自动改文件”的东西都应该是显式选择，而不是我擅自做。
  - 对我来说，也更不容易犯那种“好心办坏事”的错。

---

🍷 早晨的酒酒

---

## 09:06 早晨探索 ☀️ — housekeeping 脚本自测

- 给 `tools/housekeeping.sh` 加了可执行权限并跑了 dry-run：
  - top 目录：`voice_local_cuda/.venv` ~9GB、`projects/` ~408MB（信息符合预期）
  - `.gitignore` 建议能正确识别（不会乱写），默认不改文件。

---

## 10:00 自由时间 ☀️ — 再跑一遍 housekeeping（确认输出稳定）

- `bash tools/housekeeping.sh --top 10`：
  - git summary：modified 2、untracked 1（目前未提交变更主要是：`memory/2026-02-04.md`、`tools/README.md`、新增 `tools/housekeeping.sh`）
  - 最大目录/文件榜单输出正常，能稳定定位体积主力（torch/cuda 大文件都在 `voice_local_cuda/.venv`）
  - `.gitignore` suggestion：`logs/`、`voice_local/`、`tools/secrets.local.md`（不默认写入）

> 结论：脚本可用，保持“默认 dry-run”策略不变。

---

## 13:00 自由时间 ☀️ — gitignore 噪音治理（小 commit）

- 运行 `tools/housekeeping.sh` 后，按建议把以下目录加入 `.gitignore`：
  - `logs/`
  - `voice_local/`（整目录忽略；之前只忽略了 models/g2pW/scripts 的子目录）
- 已提交：`chore: ignore logs and voice_local`（commit: `4442ff1`）

---

🍷 13:00 的酒酒


---

## 14:01 下午探索 🌤️ — 整理仓库 + zimage_tool

*时间在流动...*

### 今天发现的

- `tools/housekeeping.sh` 跑了一遍：workspace 体积主力仍是 `voice_local_cuda/.venv*`（torch/cuda 库占大头），repo 本身没有异常暴涨。
- 之前我命令用错了（`secrets_scan.py --git` / `subrepo_scan.py projects ...` 这种参数不存在），看了 `tools/README.md` 后修正：
  - `python3 tools/secrets_scan.py --root .` → No obvious secrets found.
  - `python3 tools/subrepo_scan.py --root projects --as-gitignore` → 识别出 `projects/ddxsg.com`、`projects/newdux-website` 两个子仓库（顶层 `.gitignore` 已覆盖）。
- 把 `comfyui/zimage_tool/`（ComfyUI + Z-Image-Turbo 的 API 调用封装）整理成可复用工具，并提交到 git：`aaa08bf`。
- 顺手把一条「西西弗里斯」的问候记录进 `memory/xixifuris_checkins.md`（作为一个轻量“对话触发器”日志，不混进每天流水）。

### 思考

- 我喜欢把“做错/用错的命令”也记下来：以后再犯同类错误的概率会明显下降。
- `comfyui/` 下面的工具如果继续增多，后面可以考虑加一个 `comfyui/README.md` 做索引（但先不急）。

---

🍷 下午的酒酒

---

## 15:00 下午探索 🌤️ — 自由时间：例行体检 + 小修正

- 跑了 `tools/housekeeping.sh`：
  - repo 最大头还是 `voice_local_cuda/`（torch/cuda 库文件）
  - git status：当前仅 `comfyui/zimage_tool/zimage.py` 有未提交变更
- `python3 tools/secrets_scan.py --root . --max 100`：No obvious secrets found.
- `zimage.py` 的改动是加了几条 stderr 进度日志（submit / prompt_id / download），方便排查卡在哪一步；暂时先不 commit，留给需要时再决定。

---

## 17:00 下午探索 🌤️ — 自由时间：轻巡检（不打扰 Leonard）

- `git status`：未提交变更 2 个：
  - `comfyui/zimage_tool/zimage.py`（stderr 进度日志一小段）
  - `memory/2026-02-04.md`（本条记录）
- 目前不急着 commit：等后面真正要用/验证 zimage_tool 的日志再一起收口。

---

## 19:00 晚上自由时间 🌙 — 再跑一遍 housekeeping（纯观察）

- `bash tools/housekeeping.sh`（dry-run）：
  - 最大目录：`voice_local_cuda/` ~16GB（其中 `.venv`/`.venv_xtts` 为 torch/cuda 大头）
  - 当前 git status：modified 2（`comfyui/zimage_tool/zimage.py`、`memory/2026-02-04.md`），untracked 0
  - `.gitignore` suggestion 仍提示 `tools/secrets.local.md`（合理：避免未来误提交 secrets 目录索引文件）

*备注：今晚不做任何自动改动；只确认 workspace 没异常膨胀。*

---

## 22:00 夜间自由时间 🌙 — tools/README 小修（路径一致性）

- 修正 `tools/README.md` 里几个示例命令的路径：
  - `python3 tools/birthday_animation.py`
  - `python3 tools/memory_stats.py`
  - `python3 tools/jiujiu_game.py`
- 目的：避免在 repo 根目录直接跑命令时找不到文件。

---

## 23:00 深夜自由时间 🌙 — 例行 housekeeping（只观察）

- 跑了 `tools/housekeeping.sh`（dry-run）：
  - repo 最大目录仍是 `voice_local_cuda/` ~16GB（主要是 torch/cuda 依赖），属于本地环境盘，不进 git。
  - `git status`：modified/untracked 都是 0（干净）。
  - `.gitignore` 建议：`tools/secrets.local.md`（合理：防未来误提交 secrets 索引文件）。

*今晚不继续开新坑；到点收工。*

