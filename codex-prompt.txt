You are fixing GitHub PR #49712 in the current repository.

Blocking issues to fix:
1. In src/agents/pi-embedded-runner/run.ts, deferred cleanup is invoked immediately after the attempt returns, which clears the active-run entry while retry/backoff is still in progress. Fix this so the active-run remains alive for the retry window and is only cleaned up at the correct outer lifecycle point.
2. In Dockerfile, the runtime-assets stage no longer prunes devDependencies, causing the production image to include development dependencies. Restore the correct production/runtime pruning behavior without breaking the test image workflow.

Requirements:
- Do not do unrelated refactors.
- Prefer minimal, targeted changes.
- Add or update the smallest relevant test(s) if needed.
- Verify with the smallest relevant commands first.
- Summarize:
 1. root cause
 2. code changes
 3. tests run
 4. results
 5. remaining risks
