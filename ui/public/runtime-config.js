// Runtime config hook (intentionally a no-op by default).
// `make build-deploy` may overwrite this in `dist/control-ui/` to inject
// ephemeral settings like a default gateway URL or password without baking
// them into the bundle.
(function () {})();

