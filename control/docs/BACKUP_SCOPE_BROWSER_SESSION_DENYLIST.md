# Backup Scope Browser and Session Denylist

Backup scope must exclude browser cache, browser cookies, local storage, session storage, auth tokens, wallet state, SSH private keys, credential vault exports, profile lock files, and temporary download folders containing authenticated exports.
