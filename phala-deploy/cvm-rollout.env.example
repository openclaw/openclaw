# Copy to phala-deploy/.env.rollout and edit for your environment.
# Usage example:
#   set -a; source phala-deploy/.env.rollout; set +a
#   ./phala-deploy/cvm-rollout.sh rollout
#
# NOTE:
# This is the low-level rollout config for a single role at a time.
# For mixed OpenClaw + mux deployments, prefer:
#   phala-deploy/cvm-rollout-targets.env.example
#   ./phala-deploy/cvm-rollout-targets.sh all --wait

# Comma-separated CVM UUID list
PHALA_CVM_IDS="replace-with-cvm-uuid"

# Compose file to deploy
PHALA_COMPOSE_FILE=phala-deploy/docker-compose.yml

# Temp env file generated from rv-exec --dotenv
PHALA_DEPLOY_ENV_FILE=/tmp/openclaw-phala-deploy.env

# Space-separated Redpill Vault keys needed by deploy
PHALA_DEPLOY_SECRETS="MASTER_KEY REDPILL_API_KEY S3_BUCKET S3_ENDPOINT S3_PROVIDER S3_REGION AWS_ACCESS_KEY_ID AWS_SECRET_ACCESS_KEY"
