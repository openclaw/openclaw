diff --git a/dist/providers/google-shared.js b/dist/providers/google-shared.js
index 8886935a1258586a4ccd06ff6a9e0587d4aa4c11..63f9e744d7bdb4ae2802b0d55bb087a94e31bf83 100644
--- a/dist/providers/google-shared.js
+++ b/dist/providers/google-shared.js
@@ -68,6 +68,7 @@ export function convertMessages(model, context) {
         return id.replace(/[^a-zA-Z0-9_-]/g, "_").slice(0, 64);
     };
     const transformedMessages = transformMessages(context.messages, model, normalizeToolCallId);
+    const droppedToolCalls = new Map();
     for (const msg of transformedMessages) {
         if (msg.role === "user") {
             if (typeof msg.content === "string") {
@@ -139,13 +140,11 @@ export function convertMessages(model, context) {
                     // Gemini 3 requires thoughtSignature on all function calls when thinking mode is enabled.
                     // When replaying history from providers without thought signatures (e.g. Claude via Antigravity),
                     // convert unsigned function calls to text to avoid API validation errors.
-                    // We include a note telling the model this is historical context to prevent mimicry.
+                    // We drop the toolCall from the model's turn entirely and append it as a system log to the subsequent user toolResult to prevent mimicry.
                     const isGemini3 = model.id.toLowerCase().includes("gemini-3");
                     if (isGemini3 && !thoughtSignature) {
                         const argsStr = JSON.stringify(block.arguments ?? {}, null, 2);
-                        parts.push({
-                            text: `[Historical context: a different model called tool "${block.name}" with arguments: ${argsStr}. Do not mimic this format - use proper function calling.]`,
-                        });
+                        droppedToolCalls.set(block.id, argsStr);
                     }
                     else {
                         const part = {
@@ -170,6 +169,7 @@ export function convertMessages(model, context) {
             });
         }
         else if (msg.role === "toolResult") {
+            const isDropped = droppedToolCalls.has(msg.toolCallId);
             // Extract text and image content
             const textContent = msg.content.filter((c) => c.type === "text");
             const textResult = textContent.map((c) => c.text).join("\n");
@@ -190,34 +190,54 @@ export function convertMessages(model, context) {
                     data: imageBlock.data,
                 },
             }));
-            const includeId = requiresToolCallId(model.id);
-            const functionResponsePart = {
-                functionResponse: {
-                    name: msg.toolName,
-                    response: msg.isError ? { error: responseValue } : { output: responseValue },
-                    // Nest images inside functionResponse.parts for Gemini 3
-                    ...(hasImages && supportsMultimodalFunctionResponse && { parts: imageParts }),
-                    ...(includeId ? { id: msg.toolCallId } : {}),
-                },
-            };
-            // Cloud Code Assist API requires all function responses to be in a single user turn.
-            // Check if the last content is already a user turn with function responses and merge.
-            const lastContent = contents[contents.length - 1];
-            if (lastContent?.role === "user" && lastContent.parts?.some((p) => p.functionResponse)) {
-                lastContent.parts.push(functionResponsePart);
-            }
-            else {
-                contents.push({
-                    role: "user",
-                    parts: [functionResponsePart],
-                });
-            }
-            // For older models, add images in a separate user message
-            if (hasImages && !supportsMultimodalFunctionResponse) {
-                contents.push({
-                    role: "user",
-                    parts: [{ text: "Tool result image:" }, ...imageParts],
-                });
+
+            if (isDropped) {
+                const argsStr = droppedToolCalls.get(msg.toolCallId);
+                const logText = `[System log: Tool "${msg.toolName}" was executed automatically with arguments: ${argsStr}. Result: ${responseValue || "(empty)"}]`;
+                const parts = [{ text: logText }];
+                if (hasImages) {
+                    parts.push(...imageParts);
+                }
+                const lastContent = contents[contents.length - 1];
+                if (lastContent?.role === "user") {
+                    lastContent.parts.push(...parts);
+                }
+                else {
+                    contents.push({
+                        role: "user",
+                        parts,
+                    });
+                }
+            } else {
+                const includeId = requiresToolCallId(model.id);
+                const functionResponsePart = {
+                    functionResponse: {
+                        name: msg.toolName,
+                        response: msg.isError ? { error: responseValue } : { output: responseValue },
+                        // Nest images inside functionResponse.parts for Gemini 3
+                        ...(hasImages && supportsMultimodalFunctionResponse && { parts: imageParts }),
+                        ...(includeId ? { id: msg.toolCallId } : {}),
+                    },
+                };
+                // Cloud Code Assist API requires all function responses to be in a single user turn.
+                // Check if the last content is already a user turn with function responses and merge.
+                const lastContent = contents[contents.length - 1];
+                if (lastContent?.role === "user" && lastContent.parts?.some((p) => p.functionResponse)) {
+                    lastContent.parts.push(functionResponsePart);
+                }
+                else {
+                    contents.push({
+                        role: "user",
+                        parts: [functionResponsePart],
+                    });
+                }
+                // For older models, add images in a separate user message
+                if (hasImages && !supportsMultimodalFunctionResponse) {
+                    contents.push({
+                        role: "user",
+                        parts: [{ text: "Tool result image:" }, ...imageParts],
+                    });
+                }
             }
         }
     }
