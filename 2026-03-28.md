# 2026-03-28 Memory

## Session Summary (2026-03-27 深夜 - 2026-03-28 凌晨)

### 📋 定时任务配置修复

**问题：** 多个 cron job 报错 "Delivering to openclaw-weixin requires target"

**修复：** 将 6 个定时任务的 delivery.channel 从隐式渠道改为显式的 `webchat`

- PR 监控 - 我的 PR
- github-monitor
- Issue 监控 - 顽固问题
- 每日任务汇报
- daily-issue-fix-report
- evening-issue-fix-report

**状态：** ✅ 已修复并验证

---

### 🔧 PR #55892 创建

**Issue:** #55816, #55760

**问题：** pi-coding-agent 0.63.0 升级后，自定义 provider 和 OAuth 认证的 provider 报错 "No API key for provider"

**根因：**

- createAgentSession() 包装 streamFn 来注入 API key
- OpenClaw 的 attempt.ts 用 raw streamSimple 覆盖了它
- 导致 auth 注入 wrapper 丢失

**修复：** 在 streamFn 赋值后恢复 getApiKey 回调

```typescript
// Restore getApiKey callback after streamFn override for custom/OAuth providers.
if (!(activeSession.agent as typeof activeSession.agent & { getApiKey?: unknown }).getApiKey) {
  (
    activeSession.agent as typeof activeSession.agent & {
      getApiKey: (provider: string) => Promise<string | undefined>;
    }
  ).getApiKey = (provider: string) => params.authStorage.getApiKey(provider);
}
```

**文件：** `src/agents/pi-embedded-runner/run/attempt.ts` (+11 行)

**Greptile 评价：** Confidence Score 5/5, Safe to merge

**PR 链接：** https://github.com/openclaw/openclaw/pull/55892

**备注：** 这是 #55883 的重新提交（原 PR 因活跃 PR>10 个被自动关闭）

---

### ✅ CI 通过的 PR

| PR     | 标题                                                                   | 状态              |
| ------ | ---------------------------------------------------------------------- | ----------------- |
| #54569 | fix(matrix): lazy import music-metadata to avoid ESM \_\_dirname issue | ✅ CI 全绿，CLEAN |
| #54611 | fix(telegram): always return currentChannelId to prevent DM regression | ✅ CI 全绿，CLEAN |

这两个 PR 的 Greptile 建议已全部修复：

- #54611: 返回类型去掉 `| undefined`，添加 `|| undefined` 处理空字符串
- #54569: import 顺序修正，简化 catch 块

---

### 📝 微信渠道配置恢复

**配置位置：** `/home/w/.openclaw/openclaw.json`

**修复内容：**

```json
{
  "plugins": {
    "entries": {
      "openclaw-weixin": {
        "enabled": true
      }
    }
  },
  "channels": {
    "openclaw-weixin": {
      "accounts": {
        "0ce74580e8da": {
          "enabled": true,
          "accountId": "0ce74580e8da-im-bot"
        }
      }
    }
  }
}
```

**账号信息：**

- 账号文件：`/home/w/.openclaw/openclaw-weixin/accounts/0ce74580e8da-im-bot.json`
- 用户 ID：`o9cq80ygl265UwODbnrWV9dvYZ-k@im.wechat`

**Gateway：** 已重启，运行正常

---

### 📋 HEARTBEAT.md 恢复

**内容：**

- PR CI 监测频率：每 30 分钟
- Issue 分析频率：每天上午 10:00
- 工作时间（8:00-22:00）：每 15 分钟检查 PR 状态
- 排查 Bug 和提交 PR 的标准流程（3 步最简流程）
- 重要教训：fork 的 main 只在 PR 合并后才同步

**监控脚本：**

- `/home/w/.openclaw/workspace/scripts/monitor-pr-ci.sh` (每 30 分钟)
- `/home/w/.openclaw/workspace/scripts/monitor-issues.sh` (每天 10:00)

---

### 🎯 当前活跃 PR (10 个)

| PR     | 标题                                                             | 状态       |
| ------ | ---------------------------------------------------------------- | ---------- |
| #55892 | fix: restore getApiKey callback after streamFn override          | 新建       |
| #55778 | fix(feishu): clear session bindings on /new command              | OPEN       |
| #54611 | fix(telegram): always return currentChannelId                    | ✅ CI 全绿 |
| #54569 | fix(matrix): lazy import music-metadata                          | ✅ CI 全绿 |
| #53743 | fix(browser): add logging for silent CDP/MCP errors              | OPEN       |
| #53636 | fix(ui): display error message when channel config save fails    | OPEN       |
| #53634 | fix: update fast-xml-parser to 5.5.8                             | OPEN       |
| #53633 | fix: remove unnecessary escape in markdown-to-line.ts regex      | OPEN       |
| #53632 | fix(browser): add missing logWarn imports                        | OPEN       |
| #53631 | fix(telegram): improve 403 bot not member/blocked error messages | OPEN       |

---

### ⚠️ 重要教训

1. **保持 main 纯净** - 绝不允许 PR 包含其他 PR 的内容
2. **PR 提交流程：**

   ```bash
   # 1. 同步本地 main 到上游
   git checkout main && git pull origin main

   # 2. 从干净的 main 创建分支
   git checkout -b fix/issue-xxx

   # 3. 修改 → 测试 → 提交 → 推送
   git add . && git commit -m "fix: ..."
   git push fork fix/issue-xxx
   gh pr create --title "fix: ..." --body "Fixes #xxx"

   # 4. PR 合并后才同步 fork 的 main
   git checkout main && git pull origin main && git push fork main
   ```

3. **活跃 PR 限制：** 不要超过 10 个，否则新 PR 会被 bot 自动关闭

---

_Last updated: 2026-03-28 00:14_
