# Task: Fix models.json validation error silently breaking ALL providers (Issue #21584)

## Problem

When models.json (or models.providers in openclaw.json) contains a validation error
in ANY single provider, the entire models.json is silently rejected. This causes ALL
custom models and provider-level baseUrl overrides to be lost silently.

No warning is logged. Users have their proxy/custom endpoints silently replaced by
built-in URLs, causing authentication failures.

## Expected behavior

1. Log a clear warning identifying WHICH provider failed validation and WHY
2. Continue loading the other valid providers
3. Only the broken provider should be skipped — not the entire config

## Key files to investigate

- `src/agents/models-config.*.ts` — model loading and validation
- `src/config/` — config validation pipeline
- Look for where `loadCustomModels()` or `validateConfig()` errors are caught silently

## Instructions

1. Read key files to trace the silent failure
2. Plan — catch per-provider errors, log warning, continue with valid providers
3. Implement with minimal diff
4. Run formatter: `npx oxfmt` on changed files
5. Run tests: `npx vitest run --reporter=verbose` on changed test files
6. Show output — only commit if ALL tests pass
7. Do NOT use `String(err)` — use `err instanceof Error ? err.message : "unknown error"`
8. Do NOT run npm/pnpm install

## When done

- git add changed files
- Commit: "fix(models): log warning and continue when one provider fails validation"
- Run: openclaw system event --text "Claude Code: issue 21584 PR ready" --mode now
