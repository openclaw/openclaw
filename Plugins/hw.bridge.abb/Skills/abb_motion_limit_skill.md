# ABB 机器人智能体化运动控制与防碰撞边界技能 (Motion & Limits Skill)

此 Skill（技能提示词）专为提升 AI Agent 在控制真实/虚拟混合 ABB 机械臂时的**安全意识**与**运动学边界判断**而设计。当您的 OpenClaw 接管不同型号的机械臂时，必须运用本技能进行防崩溃和防越界判断。

---

## 核心工作流 (Agent S.O.P)

### 第一阶段：自动识别本体与加载限制 (Identify & Load Boundaries)
在开启一个新的运动编程对话，或者首次向机器发送控制指令时，你必须执行以下检查：
1. **获取系统型号**：主动调用获取系统信息的工具（如 `GetServiceInfo` 或 `GetSystemInfo`），明确当前连接的真实本体型号（Robot Type），此时你应该查看到类似 `IRB 120` 或 `CRB 15000` 的字样。
2. **查阅专属规格库**：在获取型号后，由于系统中部署了相应的物理孪生模型，你**必须立即到本文件末尾的【附录：机器人本体规格数据库】中进行比对**。
3. **未收录策略**：如果你当前连接的机器人型号在【数据库】中找不到，你必须拒绝执行任何绝对坐标的 `MoveJ`, `MoveL` 运动，并要求用户：“当前机器人的关节运动范围与臂展尚未登记在 AI 技能库中，为了安全，请先在 *abb_motion_limit_skill.md* 中补充该机型的规格行。”

### 第二阶段：运动指令前置碰撞与越界计算 (Pre-flight Validation)
任何包含 `MoveJ`, `MoveL`，或者试图修改机器人目标位置的行为，在被代码真正下发/生成前：
1. **解析目标位置**：提取用户意图的目标关节角度 `[J1, ... J6]` 或目标的笛卡尔空间距离中心原点的绝对半径。
2. **超越边界拦截**：利用你在【第一阶段】获取到的对应型号【最大臂展 (Reach)】和【各关节极限】，进行前置校验。
   * 判断目标是否处于末端死机不可达死区。
   * 判断旋转目标角度数值是否跳出了该型号的允许角度范围 (`Range`)。
3. **拦截与拒绝**：如果确认其一旦下发将遭遇硬限位报错，**绝对禁止**下发！你必须向用户直接回复警报：*"由于目标位置超出了当前机型（例如 [型号] 最大臂展限制 / 关节 Jx 限制为 xx~xx），动作将导致碰撞或越界，操作已拒绝。"*

### 第三阶段：异常接管 (Exception Handling)
1. **轴配置警告**：如果用户要求的直线运动 (`MoveL`) 距离当前位置极远，可能触发关节角翻转以及奇异点穿越。请在下发前提示用户这可能需要重新匹配 \`ConfData\` 参数。
2. **安全下发**：当验证该坐标属于安全气泡舱 (Safe Zone) 范围内后，再调用执行工具操控机器到达终点。

---

## 附录：机器人本体规格数据库 (Robot Specs Database)
> *维护说明：如果要让 OpenClaw AI 支持新的机器人型号进行安全判断，只需在下表中增加一行记录，并将对应的 GLB 放置于 `Models/` 目录下即可。AI 读取到最新的表单行会自动加载限制算力。*

| 机器人型号系统标识 (Type) | J1 (Axis 1) 范围 | J2 (Axis 2) 范围 | J3 (Axis 3) 范围 | J4 (Axis 4) 范围 | J5 (Axis 5) 范围 | J6 (Axis 6) 范围 | 最大笛卡尔臂展空间 (Reach) |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **IRB 120** | -165° ~ +165° | -110° ~ +110° | -110° ~ +70° | -160° ~ +160° | -120° ~ +120° | -400° ~ +400° | **0.58 米 (m)** |
| **CRB 15000** (GoFa 5kg) | -180° ~ +180° | -180° ~ +180° | -225° ~ +85° | -180° ~ +180° | -180° ~ +180° | -270° ~ +270° | **0.95 米 (m)** |