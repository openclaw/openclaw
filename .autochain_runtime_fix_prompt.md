Implement the runtime fix described in docs/ops/AUTO_CHAIN_RUNTIME_FIX_SPEC.md.

Requirements (must implement in code + tests):

1. Atomic completion->next-step chain (launch or explicit blocker, no status-only branch)
2. Launch proof contract in execution updates
3. Running-claim truth guard
4. Pending policy guard (only pause/approval/hard blocker)
5. Auto-recovery watchdog (SLA breach ~60s)
6. Output hygiene guard for internal syntax leakage
7. Metrics + logging hooks sufficient for p95 completion->next-launch

Constraints:

- Make minimal, focused changes.
- Add/adjust tests for each acceptance case in spec.
- Keep existing behavior backward compatible unless it violates the new guarantees.

Output:

- Commit changes with message: "feat(runtime): enforce auto-chain completion launch guards"
- Print a short summary of touched files and test results.
