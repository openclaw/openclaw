# Curator 批量更新速查表 ⚡

## 🎯 何時使用
- Cruz 說「把其他課程都改 svg」
- 需要批量更新 3+ 個課程
- 想要提升執行效率

## ⚡ 三步驟完成

### 1️⃣ 批量讀取（1 次查詢）
```bash
jq '.courses[] | select(.course_id > 0) | {course_id, zh_name, pricing}' .kiro/personas/curator/memory.json
```

### 2️⃣ 批量生成（1 次 Edit）
```javascript
const svg2 = (courseId === 2 && index === 0) ? `...` : null;
const svg3 = (courseId === 3 && index === 0) ? `...` : null;
// ... 其他課程
const testSVG = svg2 || svg3 || svg4 || svg5 || svg6;
```

### 3️⃣ 快速驗證（regex 提取）
```bash
curl -s URL | grep -o '<text[^>]*>省 [0-9,]* 元</text>'
```

## 🚨 異常處理檢查清單

- [ ] 優惠價 <= 原價
- [ ] 一對一價 >= 團班價
- [ ] 所有價格 >= 0
- [ ] 千分位格式正確（>= 1000）

## 📊 效率對比

| 指標 | 逐一處理 | 批量處理 |
|------|----------|----------|
| 查詢次數 | 5 次 | 1 次 |
| Edit 次數 | 5 次 | 2 次 |
| 執行時間 | 20 分鐘 | 10 分鐘 |
| 第二次 | 15 分鐘 | 5 分鐘 |

## 🎓 核心技巧

### 1. jq 批量過濾
```bash
# ✅ 好：一次取得全部
jq '.courses[] | select(.course_id > 0)'

# ❌ 差：逐一查詢
jq '.courses[] | select(.course_id == 2)'
jq '.courses[] | select(.course_id == 3)'
```

### 2. 單次 Edit 模式
```javascript
// ✅ 好：一次新增所有 SVG
const svg2 = ...
const svg3 = ...
const svg4 = ...
const testSVG = svg2 || svg3 || svg4;

// ❌ 差：分多次 Edit
// (每次 Edit 都要讀檔 → 寫檔)
```

### 3. regex 快速驗證
```bash
# ✅ 好：只提取關鍵資訊
grep -o '<text[^>]*>省 [0-9,]* 元</text>'

# ❌ 差：讀取完整 HTML
curl -s URL | cat
```

## 💡 記憶點

1. **檔案位置**：`app/products/[id]/HighlightCard.js`
2. **JSON 讀取**：只用 `jq`（memory.json 太大）
3. **千分位**：>= 1000 要加逗號（1,680 / 10,000）
4. **異常處理**：發現 → 推測 → 修正 → 通知

## 🔗 詳細文件
- 完整流程：`CLAUDE.md` 模式 D
- 執行記錄：`.kiro/personas/curator/sessions/20251102_223628_batch_svg_update.md`
- 工具說明：`.kiro/personas/curator/TOOLS.md`

---

**目標**：從 20 分鐘 → 5 分鐘（效率提升 75%）
