import { readFileSync } from 'fs';

// 手動讀取 .env.local
const envFile = readFileSync('.env.local', 'utf8');
const env = {};
envFile.split('\n').forEach(line => {
  const [key, ...values] = line.split('=');
  if (key) {
    // 移除值中的換行符號和引號
    env[key.trim()] = values.join('=').trim().replace(/\\n|"|'/g, '');
  }
});

const NOTION_API_KEY = env.NOTION_TOKEN;
const PRODUCTS_DATABASE_ID = env.NOTION_PRODUCTS_DATABASE_ID;
const NOTION_VERSION = '2022-06-28';

console.log('🔑 環境變數檢查:');
console.log('NOTION_TOKEN:', NOTION_API_KEY?.substring(0, 10) + '...');
console.log('PRODUCTS_DB_ID:', PRODUCTS_DATABASE_ID);

// 第六課的 page_id（需要先查詢）
const COURSE_6_PAGE_ID = await getCourse6PageId();

const YOU_WILL_LEARN = `⚡ 超重要！這是「手機課程」

✅ 你只需要帶：
• 你的手機（iOS/Android 都可以）
• 充電線 + 行動電源
• 一顆想學習的心

❌ 你不需要：
• 筆電（真的不用！）
• 寫程式（完全不用！）
• 任何技術背景

💡 為什麼用手機？
因為你平常就是用手機經營自媒體，直接學會在手機上操作，回家立刻能用！

━━━━━━━━━━━━━━━━━━━━

✓ Day 1：AI 內容創作能力
• 手機就能生成專業圖文
• 10 分鐘完成以前 1 小時的工作
• 建立專屬的個人風格模板
• 產出 5 張可直接發布的 IG 貼圖

✓ Day 2：批量生產 + 自動化互動
• 30 分鐘產出一週完整素材
• 建立 ManyChat 自動私訊機制
• 設計從發文到轉換的互動漏斗
• 用 AI 分析數據優化內容策略

✓ Day 3：專屬 AI 工具包
• 打造個人 AI 助手網頁
• 建立可重複使用的 prompt 範本（輸入內容 → 自動組合 → 複製使用）
• 發布到 GitHub Pages（全程手機操作）
• 不用每次重新下 prompt，省時又方便`;

const SUMMERY = `三天 18 小時密集培訓，整合 Course 05-11 共 7 門課程。從 AI 基礎到自媒體經營，從內容創作到自動化互動，用 3 天建立完整的 AI 自媒體工作流。

🎯 適合對象：自媒體創作者、小編、行銷人員、想經營個人品牌的上班族
💡 零基礎可學！只要會用手機就能上課

📱 三天成果：
Day 1：圖文並茂的社群貼文 + 個人風格模板
Day 2：一週完整素材 + 自動化私訊機制
Day 3：專屬 AI 工具包網頁（可重複使用的 prompt 範本）

━━━━━━━━━━━━━━━━━━━━

📅 第一天：AI 協作基礎與內容創作（6 小時）

09:30-11:00 | Course 05: AI 新手村（1.5 小時）
• 學會和 AI 溝通，建立正確的協作思維
• 掌握 ChatGPT、Claude、Gemini 三大工具
• 3 句話讓 AI 做出你要的東西
• ✅ 實作：用手機生成一篇完整貼文

11:10-12:40 | Course 06: 寫作救星（1.5 小時）
• 用 AI 寫出吸引人的文案
• 掌握 5 種萬用文案公式（AIDA、故事、痛點解決）
• 10 分鐘產出以前要花 1 小時的內容
• ✅ 實作：寫出 3 種不同風格的產品文案

12:40-13:40 | 午餐休息

13:40-15:10 | Course 07: 找到你的風格（1.5 小時）
• 發掘你的隱藏特色與獨特價值
• 讓 AI 學會你的風格，但不失去個性
• 在 AI 時代保持不可替代性
• ✅ 實作：建立專屬的「個人風格 prompt 模板」

15:20-16:30 | Course 08: 圖片魔法師（1 小時 10 分）
• 用 ChatGPT（DALL-E 3）和 Gemini 做出專業美圖
• Logo、社群圖、Reels 封面，手機就能生成
• 建立專屬的視覺品牌風格
• ✅ 實作：產出 5 張可直接發布的 IG 貼圖

📅 第二天：批量生產與自動化互動（6 小時）

09:30-11:00 | Course 09: 內容製造機（1.5 小時）
• 建立內容製造流水線
• 用一個點子變出 10 種不同內容（IG/FB/Threads/LinkedIn）
• 30 分鐘做出一週的社群發文
• ✅ 實作：現場產出一週完整社群內容（圖 + 文）

11:10-13:10 | ManyChat 自動化互動設計（2 小時）
• 設計自媒體互動漏斗（貼文→留言→私訊→轉換）
• 建立 Reels/Post 留言自動私訊機制
• 設計歡迎訊息、關鍵字自動回覆
• ✅ 實作：建立你的第一個自動化互動流程

13:10-14:10 | 午餐休息

14:10-16:30 | Course 10+11: AI 決策與數據分析（2 小時 20 分）
• 用 AI 當你的自媒體策略顧問
• 複製 IG/FB 後台數據給 ChatGPT 分析
• 找出高互動內容的共同特徵
• 優化下一篇爆款內容
• ✅ 實作：分析你的帳號數據，找出優化方向

📅 第三天：工具包建立與系統整合（6 小時）

09:30-12:30 | 個人 AI 工具包製作（3 小時）
• 用 Claude 生成你的專屬工具網頁
• 建立可重複使用的 prompt 範本（週報/文案/hashtag 等）
• 輸入內容 → 自動組合 prompt → 複製到 ChatGPT 使用
• 發布到 GitHub Pages 成為你的個人頁面
• ✅ 實作：完成專屬的 AI 助手工具包（全程手機操作）

12:30-13:30 | 午餐休息

13:30-15:30 | 工作流展示與優化（2 小時）
• 學員展示個人自媒體工作流
• 講師點評：內容策略、視覺風格、互動設計
• 互相學習不同產業的自媒體經營方式
• 優化建議：從發文到變現的完整路徑

15:40-16:30 | 課程總結與未來規劃（50 分鐘）
• 從內容到互動到工具的完整回顧
• 建立你的自媒體 AI 協作 SOP
• 課後資源：prompt 範本庫、案例參考
• LINE 社群支援與持續學習資源

━━━━━━━━━━━━━━━━━━━━

💡 課後你會擁有：
✅ 個人風格 prompt 模板庫
✅ 一週份可發布的社群內容
✅ 自動化私訊互動機制
✅ 專屬 AI 工具包網頁
✅ 完整的自媒體經營 SOP
✅ LINE 社群持續支援`;

// 查詢第六課的 page_id
async function getCourse6PageId() {
  const response = await fetch(`https://api.notion.com/v1/databases/${PRODUCTS_DATABASE_ID}/query`, {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${NOTION_API_KEY}`,
      'Notion-Version': NOTION_VERSION,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      page_size: 100
    })
  });

  const data = await response.json();

  // Debug: 檢查 API 回應結構
  if (!response.ok) {
    console.error('❌ API 錯誤:', JSON.stringify(data, null, 2));
    throw new Error(`Notion API failed: ${data.message || 'Unknown error'}`);
  }

  console.log('\n📋 所有課程：');
  if (!data.results) {
    console.error('❌ 沒有 results 欄位:', JSON.stringify(data, null, 2));
    throw new Error('API 回應格式不正確');
  }

  data.results.forEach(page => {
    const courseId = page.properties.course_id?.number;
    const name = page.properties.zh_name?.rich_text?.[0]?.plain_text;
    console.log(`  - course_id: ${courseId}, name: ${name}`);
  });

  const course6 = data.results.find(page => page.properties.course_id?.number === 6);

  if (!course6) {
    throw new Error('找不到第六課 (course_id = 6)');
  }

  return course6.id;
}

// 更新 Notion 頁面
async function updateCourse6() {
  console.log('🔍 查詢第六課 page_id...');
  console.log('📄 Page ID:', COURSE_6_PAGE_ID);

  console.log('\n📝 更新課程內容...');

  const response = await fetch(`https://api.notion.com/v1/pages/${COURSE_6_PAGE_ID}`, {
    method: 'PATCH',
    headers: {
      'Authorization': `Bearer ${NOTION_API_KEY}`,
      'Notion-Version': NOTION_VERSION,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      properties: {
        you_will_learn: {
          rich_text: [{
            type: 'text',
            text: { content: YOU_WILL_LEARN }
          }]
        },
        summery: {
          rich_text: [{
            type: 'text',
            text: { content: SUMMERY }
          }]
        }
      }
    })
  });

  if (!response.ok) {
    const error = await response.text();
    throw new Error(`更新失敗: ${error}`);
  }

  console.log('✅ 更新成功！');
  console.log('\n📊 更新內容：');
  console.log('- you_will_learn: 已更新（三天課程架構）');
  console.log('- summery: 已更新（詳細課表）');
}

// 執行
try {
  await updateCourse6();
} catch (error) {
  console.error('❌ 錯誤:', error.message);
  process.exit(1);
}
