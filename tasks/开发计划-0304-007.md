0304-007-Phase C: 基金迁移 + Phase D: 旧扩展清理

Plan to implement │
│ │
│ Phase C: 基金迁移 + Phase D: 旧扩展清理 │
│ │
│ Context │
│ │
│ Phase B+ 已完成 (154 测试全绿)。findoo-trader-plugin 已合并 4 个旧扩展 (fin-core, fin-trading, fin-paper-trading, │
│ fin-strategy-engine)，但 fin-fund-manager 仍独立，且 12 个旧 fin- 目录仍存在*。 │
│ │
│ 目标: │
│ - Phase C: 将 fin-fund-manager (~2,700 LOC, 7 AI tools, 8 HTTP routes) 迁入 findoo-trader-plugin/src/fund/ │
│ - Phase D: 删除 12 个旧 fin-* 扩展目录 (保留 fin-evolution-engine + fin-shared-types) │
│ - 新增 ~100 个 fund 测试 → 总测试 ~254 │
│ │
│ --- │
│ Phase C: 基金模块迁移 │
│ │
│ C-1: 创建 src/fund/ 并迁移 10 个纯逻辑模块 │
│ │
│ 来源: extensions/fin-fund-manager/src/ (10 文件) │
│ 目标: extensions/findoo-trader-plugin/src/fund/ │
│ │
│ ┌───────────────────────────────┬─────┬──────────────────────────────────────────────────────────────────────────┐ │
│ │ 文件 │ LOC │ Import 改写 │ │
│ ├───────────────────────────────┼─────┼──────────────────────────────────────────────────────────────────────────┤ │
│ │ types.ts │ 102 │ ../../fin-shared-types/src/types.js → ../shared/types.js (4 处) │ │
│ ├───────────────────────────────┼─────┼──────────────────────────────────────────────────────────────────────────┤ │
│ │ fund-manager.ts │ 265 │ shared-types → ../shared/types.js + │ │
│ │ │ │ ../../fin-strategy-engine/src/fitness.js → ../strategy/fitness.js │ │
│ ├───────────────────────────────┼─────┼──────────────────────────────────────────────────────────────────────────┤ │
│ │ capital-allocator.ts │ 156 │ 无需改写 (只引用 local types) │ │
│ ├───────────────────────────────┼─────┼──────────────────────────────────────────────────────────────────────────┤ │
│ │ promotion-pipeline.ts │ 241 │ shared-types → ../shared/types.js (1 处) │ │
│ ├───────────────────────────────┼─────┼──────────────────────────────────────────────────────────────────────────┤ │
│ │ fund-risk-manager.ts │ 76 │ 无需改写 │ │
│ ├───────────────────────────────┼─────┼──────────────────────────────────────────────────────────────────────────┤ │
│ │ leaderboard.ts │ 66 │ shared-types → ../shared/types.js (1 处) │ │
│ ├───────────────────────────────┼─────┼──────────────────────────────────────────────────────────────────────────┤ │
│ │ correlation-monitor.ts │ 83 │ 无需改写 │ │
│ ├───────────────────────────────┼─────┼──────────────────────────────────────────────────────────────────────────┤ │
│ │ capital-flow-store.ts │ 101 │ 无需改写 (self-contained SQLite) │ │
│ ├───────────────────────────────┼─────┼──────────────────────────────────────────────────────────────────────────┤ │
│ │ performance-snapshot-store.ts │ 110 │ 无需改写 │ │
│ ├───────────────────────────────┼─────┼──────────────────────────────────────────────────────────────────────────┤ │
│ │ formatters.ts │ 227 │ 无需改写 │ │
│ └───────────────────────────────┴─────┴──────────────────────────────────────────────────────────────────────────┘ │
│ │
│ 验证: 所有需要的 shared types (DecayState, StrategyLevel, BacktestResult, WalkForwardResult, FitnessInput, │
│ StrategyRecord) 已存在于 src/shared/types.ts。calculateFitness 已存在于 src/strategy/fitness.ts。 │
│ │
│ C-2: 创建 src/fund/tools.ts — 从 index.ts 提取 AI 工具注册 │
│ │
│ 来源: fin-fund-manager/index.ts (lines 141-563) │
│ 模式: 参照现有 src/execution/trading-tools.ts, src/paper/tools.ts, src/strategy/tools.ts │
│ │
│ // src/fund/tools.ts (~500 LOC) │
│ export function registerFundTools(api: OpenClawPluginApi, deps: FundToolDeps): void { │
│ // 7 AI tools: fin_fund_status, fin_fund_allocate, fin_fund_rebalance, │
│ // fin_leaderboard, fin_fund_promote, fin_fund_risk, fin_list_promotions_ready │
│ } │
│ │
│ 依赖注入: 使用 getter 延迟获取 runtime services (与现有模式一致): │
│ type FundToolDeps = { │
│ manager: FundManager; │
│ config: FundConfig; │
│ flowStore: CapitalFlowStore; │
│ perfStore: PerformanceSnapshotStore; │
│ getRegistry: () => StrategyRegistryLike | undefined; │
│ getPaper: () => PaperEngineLike | undefined; │
│ }; │
│ │
│ C-3: 创建 src/fund/routes.ts — HTTP routes + SSE + dashboard │
│ │
│ 来源: fin-fund-manager/index.ts (lines 564-860) │
│ 预计: ~300 LOC │
│ │
│ 注册 9 个路由: │
│ - REST: /api/v1/fund/{status,leaderboard,risk,allocations,performance,capital-flows} (6 个) │
│ - SSE: /api/v1/fund/stream (10s interval) │
│ - Dashboard: /dashboard/fund │
│ - CLI: fund pipeline command │
│ │
│ 设计: 独立的 registerFundRoutes() (不合并到 route-handlers.ts，保持模块解耦) │
│ │
│ C-4: 在 index.ts 中集成 fund 模块 │
│ │
│ 修改 extensions/findoo-trader-plugin/index.ts: │
│ │
│ 1. 新增 imports (6 行): │
│ - FundManager, CapitalFlowStore, PerformanceSnapshotStore │
│ - registerFundTools, registerFundRoutes │
│ - FundConfig type │
│ 2. 实例化 (在 Strategy Engine 之后，~line 233): │
│ const fundConfig = resolveConfig(api).fundConfig; // 从 config.ts 扩展 │
│ const fundManager = new FundManager(api.resolvePath("state/findoo-fund-state.json"), fundConfig); │
│ const perfStore = new PerformanceSnapshotStore(api.resolvePath("state/findoo-performance-snapshots.sqlite")); │
│ const flowStore = new CapitalFlowStore(api.resolvePath("state/findoo-capital-flows.sqlite")); │
│ 3. 注册 service: │
│ api.registerService({ id: "fin-fund-manager", instance: fundManager, start: () => {} }); │
│ 4. 注册 tools + routes: │
│ registerFundTools(api, { manager: fundManager, config: fundConfig, ... }); │
│ registerFundRoutes(api, { manager: fundManager, config: fundConfig, ... }); │
│ 5. 新增 re-exports: FundManager, CapitalAllocator, PromotionPipeline 等 │
│ 6. 更新 plugin description 注释: 16 AI Tools → 23 AI Tools, 28 routes → 38 routes │
│ │
│ 注意: data-gathering.ts 中 4 处 runtime.services.get("fin-fund-manager") 保持不变 — fund-manager 注册为同名 │
│ service，现有查询代码自然工作。 │
│ │
│ C-5: 迁移测试文件 │
│ │
│ 来源: fin-fund-manager/src/_.test.ts (7 文件, ~1,507 LOC) │
│ 目标: findoo-trader-plugin/test/unit/ │
│ │
│ ┌─────────────────────────────┬───────────────────────────────────────┬─────┐ │
│ │ 旧文件 │ 新文件 │ LOC │ │
│ ├─────────────────────────────┼───────────────────────────────────────┼─────┤ │
│ │ capital-allocator.test.ts │ test/unit/capital-allocator.test.ts │ 175 │ │
│ ├─────────────────────────────┼───────────────────────────────────────┼─────┤ │
│ │ correlation-monitor.test.ts │ test/unit/correlation-monitor.test.ts │ 91 │ │
│ ├─────────────────────────────┼───────────────────────────────────────┼─────┤ │
│ │ formatters.test.ts │ test/unit/formatters.test.ts │ 188 │ │
│ ├─────────────────────────────┼───────────────────────────────────────┼─────┤ │
│ │ fund-manager.test.ts │ test/unit/fund-manager.test.ts │ 342 │ │
│ ├─────────────────────────────┼───────────────────────────────────────┼─────┤ │
│ │ fund-risk-manager.test.ts │ test/unit/fund-risk-manager.test.ts │ 108 │ │
│ ├─────────────────────────────┼───────────────────────────────────────┼─────┤ │
│ │ leaderboard.test.ts │ test/unit/leaderboard.test.ts │ 143 │ │
│ ├─────────────────────────────┼───────────────────────────────────────┼─────┤ │
│ │ promotion-pipeline.test.ts │ test/unit/promotion-pipeline.test.ts │ 460 │ │
│ └─────────────────────────────┴───────────────────────────────────────┴─────┘ │
│ │
│ Import 改写规则: │
│ - ./fund-manager.js → ../../src/fund/fund-manager.js │
│ - ../../fin-shared-types/src/types.js → ../../src/shared/types.js │
│ - 添加 vi.mock("ccxt", () => ({})) 到每个文件头部 (与现有测试一致) │
│ │
│ 注意: fin-fund-manager/index.test.ts (407 LOC) 含 tool/route 集成测试。需拆分为 │
│ test/integration/fund-routes.test.ts，使用现有 captureRoutes + mockReq/mockRes 模式。 │
│ │
│ C-6: 验证 │
│ │
│ npx vitest run extensions/findoo-trader-plugin/test/ │
│ # 预期: ~254 tests passed (154 + ~100 fund) │
│ │
│ --- │
│ Phase D: 旧扩展清理 │
│ │
│ D-1: 删除 6 个空壳扩展 (LOW risk) │
│ │
│ 仅在 FINANCIAL_PLUGIN_IDS 中被引用: │
│ rm -rf extensions/fin-market-data │
│ rm -rf extensions/fin-monitoring │
│ rm -rf extensions/fin-portfolio │
│ rm -rf extensions/fin-info-feed │
│ rm -rf extensions/fin-strategy-memory │
│ rm -rf extensions/fin-expert-sdk │
│ │
│ D-2: 删除 4 个已迁移扩展 │
│ │
│ 代码已完全合并到 findoo-trader-plugin: │
│ rm -rf extensions/fin-core │
│ rm -rf extensions/fin-trading │
│ rm -rf extensions/fin-paper-trading │
│ rm -rf extensions/fin-strategy-engine │
│ │
│ D-3: 删除 fin-fund-manager (Phase C 完成后) │
│ │
│ rm -rf extensions/fin-fund-manager │
│ │
│ D-4: 保留的扩展 (不删除) │
│ │
│ ┌──────────────────────┬────────────────────────────────────────────────────────────────────────────────────┐ │
│ │ 扩展 │ 原因 │ │
│ ├──────────────────────┼────────────────────────────────────────────────────────────────────────────────────┤ │
│ │ fin-evolution-engine │ 独立模块，有自己的 service + GEP 基因逻辑，不属于 trader 范围 │ │
│ ├──────────────────────┼────────────────────────────────────────────────────────────────────────────────────┤ │
│ │ fin-shared-types │ 纯类型库，findoo-backtest-plugin 通过 @openfinclaw/fin-shared-types workspace 引用 │ │
│ └──────────────────────┴────────────────────────────────────────────────────────────────────────────────────┘ │
│ │
│ D-5: 更新 FINANCIAL_PLUGIN_IDS │
│ │
│ 修改 src/core/data-gathering.ts:25-34: │
│ // 旧 (8 个): │
│ const FINANCIAL_PLUGIN_IDS = [ │
│ "findoo-trader-plugin", "findoo-datahub-plugin", │
│ "fin-market-data", "fin-portfolio", "fin-monitoring", │
│ "fin-strategy-memory", "fin-expert-sdk", "fin-info-feed", │
│ ] as const; │
│ │
│ // 新 (4 个): │
│ const FINANCIAL_PLUGIN_IDS = [ │
│ "findoo-trader-plugin", "findoo-datahub-plugin", │
│ "findoo-backtest-plugin", "fin-evolution-engine", │
│ ] as const; │
│ │
│ 同步更新 data-gathering.test.ts 中 plugins.total 断言 (8 → 4)。 │
│ │
│ D-6: 清理 dangling 引用 │
│ │
│ 1. findoo-datahub-plugin live test 中可能引用旧扩展 — 更新或标记 skip │
│ 2. 全局 grep 确认无其他 import 引用已删除目录 │
│ 3. pnpm install 验证 workspace 无报错 │
│ │
│ D-7: 最终验证 │
│ │
│ # 1. findoo-trader-plugin 测试 │
│ npx vitest run extensions/findoo-trader-plugin/test/ │
│ # 预期: ~254 tests passed │
│ │
│ # 2. 全量测试 (可能有其他扩展测试) │
│ pnpm test │
│ │
│ # 3. workspace 健康 │
│ pnpm install │
│ │
│ --- │
│ Agent 团队分工 │
│ │
│ team-lead (主控) │
│ ├── fund-core-agent → C-1 + C-2 + C-4 (源码迁移 + tools + index.ts 集成) │
│ ├── fund-routes-agent → C-3 + C-5 (routes + 测试迁移) — 可与 fund-core 并行 │
│ └── cleanup-agent → D-1~D-7 (等 Phase C 验证通过后执行) │
│ │
│ C-1/C-2/C-3 可并行。C-4 依赖 C-1/C-2/C-3。C-5 依赖 C-1。 │
│ Phase D 依赖 Phase C 全部完成。 │
│ │
│ --- │
│ 预期成果 │
│ │
│ ┌──────────────┬─────────────┬──────────────────────────────────────────┐ │
│ │ 指标 │ Phase B+ 后 │ Phase C/D 后 │ │
│ ├──────────────┼─────────────┼──────────────────────────────────────────┤ │
│ │ AI Tools │ 16 │ 23 (+7 fund) │ │
│ ├──────────────┼─────────────┼──────────────────────────────────────────┤ │
│ │ HTTP Routes │ 29 │ 38 (+9 fund) │ │
│ ├──────────────┼─────────────┼──────────────────────────────────────────┤ │
│ │ Services │ 8 │ 9 (+1 fin-fund-manager) │ │
│ ├──────────────┼─────────────┼──────────────────────────────────────────┤ │
│ │ Bot Commands │ 0 │ 5 (+5 fund) │ │
│ ├──────────────┼─────────────┼──────────────────────────────────────────┤ │
│ │ 测试 │ 154 │ ~254 (+~100 fund) │ │
│ ├──────────────┼─────────────┼──────────────────────────────────────────┤ │
│ │ 旧扩展目录 │ 13 │ 2 (保留 evolution-engine + shared-types) │ │
│ ├──────────────┼─────────────┼──────────────────────────────────────────┤ │
│ │ SSE Streams │ 4 │ 5 (+1 fund/stream) │ │
│ └──────────────┴─────────────┴──────────────────────────────────────────┘ │
│ │
│ --- │
│ 验证方案 │
│ │
│ 1. npx vitest run extensions/findoo-trader-plugin/test/ → ~254 全绿 │
│ 2. pnpm install 无 workspace 报错 │
│ 3. grep -r "fin-core\|fin-trading\|fin-paper-trading\|fin-strategy-engine\|fin-fund-manager" extensions/ │
│ --include="_.ts" --exclude-dir=node_modules | grep -v findoo-trader | grep -v fin-evolution | grep -v fin-shared → │
│ 0 results │
│ 4. fund SSE /api/v1/fund/stream 可达 │
│ 5. fund AI tools 可通过 registerTool 注册 │
╰────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╯

⏺ 我来先快速了解当前代码状态，然后创建团队并行开发。

⏺ 3 Explore agents finished (ctrl+o to expand)
├─ Explore fund-manager source · 13 tool uses · 71.7k tokens
│ ⎿ Done
├─ Explore findoo-trader-plugin state · 10 tool uses · 32.1k tokens
│ ⎿ Done
└─ Explore fund-manager remaining files · 8 tool uses · 31.9k tokens
⎿ Done

⏺ 现在我需要读取关键源文件的精确内容来指导实现。

⏺ Read 2 files (ctrl+o to expand)

⏺ Now let me read the remaining needed files for test patterns and data-gathering.

⏺ Read 4 files (ctrl+o to expand)

⏺ Let me read the remaining source files needed for the migration.

⏺ Read 10 files (ctrl+o to expand)

⏺ Good, I have all the source code needed. Now let me create the team and parallelize the implementation. Let me also
quickly read the test files for import patterns.

⏺ Read 3 files (ctrl+o to expand)

⏺ Now let me read the remaining test files and the index.test.ts.

⏺ Read 4 files (ctrl+o to expand)

⏺ 好，已完全掌握所有源码。创建团队并行执行 Phase C + D。

⏺ Now let me spawn 3 agents in parallel: fund-core (C-1 + C-2), fund-routes (C-3 + C-5), and I'll handle C-4 + C-6 + D
myself after they finish.  

