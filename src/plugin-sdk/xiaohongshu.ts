// Narrow plugin-sdk surface for the bundled xiaohongshu plugin.
// Keep this list additive and scoped to symbols used under extensions/xiaohongshu.

export { emptyPluginConfigSchema } from "../plugins/config-schema.js";
export type { AnyAgentTool, OpenClawPluginApi } from "../plugins/types.js";
