// Narrow plugin-sdk surface for the bundled jarvis plugin.
// Keep this list additive and scoped to symbols used under extensions/jarvis.

export type {
  AnyAgentTool,
  OpenClawPluginApi,
  OpenClawPluginService,
  OpenClawPluginServiceContext,
  OpenClawPluginToolFactory,
} from "../plugins/types.js";
