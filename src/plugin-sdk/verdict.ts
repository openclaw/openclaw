// Narrow plugin-sdk surface for the bundled verdict plugin.
// Keep this list additive and scoped to symbols used under extensions/verdict.

export type { OpenClawPluginApi } from "../plugins/types.js";
