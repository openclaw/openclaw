// Full session-store resolution for gateway CLI requests that need local routing facts.
export { resolveSessionKeyForRequestCore as resolveSessionKeyForRequest } from "../../agents/command/session.js";
