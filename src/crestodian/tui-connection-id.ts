// Stable identifier for the Crestodian TUI backend's connection.url.
// Kept in its own file so both the Crestodian backend and the TUI command
// handlers can import it without forming a cycle through ../tui/tui.js.
export const CRESTODIAN_TUI_CONNECTION_URL = "crestodian local";
