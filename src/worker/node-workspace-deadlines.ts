export const NODE_WORKER_WORKSPACE_COMMAND_TIMEOUT_MS = 10 * 60_000;
// Let the bounded subprocess return its terminal result before the RPC timer fires.
export const NODE_WORKER_WORKSPACE_RESULT_GRACE_MS = 5_000;
