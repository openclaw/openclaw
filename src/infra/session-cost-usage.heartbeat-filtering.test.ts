import { describe, expect, it } from \"vitest\";\nimport { stripHeartbeatContent } from \"../auto-reply/heartbeat.js\";\n\n// Mock the session-cost-usage logic for testing heartbeat filtering\nfunction extractFirstUserMessageWithHeartbeatFilter(messages: Array<{role: string, content: string}>): string | undefined {\n  for (const message of messages) {\n    if (message.role === \"user\") {\n      const rawContent = message.content;\n      \n      // Apply the same heartbeat filtering as in session-cost-usage.ts\n      const heartbeatFiltered = stripHeartbeatContent(rawContent, \"message\");\n      const finalContent = heartbeatFiltered.shouldSkip ? \"\" : (heartbeatFiltered.text || rawContent);\n      \n      if (finalContent.trim()) {\n        return finalContent.slice(0, 100);\n      }\n      // Continue to next message if this was pure heartbeat content\n    }\n  }\n  return undefined;\n}\n\ndescribe(\"session label extraction with heartbeat filtering (issue #66533)\", () => {\n  it(\"filters out pure heartbeat messages to prevent label pollution\", () => {\n    const messages = [\n      { role: \"user\", content: \"HEARTBEAT_OK\" },\n      { role: \"assistant\", content: \"All systems running normally.\" },\n      { role: \"user\", content: \"Help me analyze this data\" },\n    ];\n\n    const result = extractFirstUserMessageWithHeartbeatFilter(messages);\n    \n    // Should skip the heartbeat message and use the real user message\n    expect(result).toBe(\"Help me analyze this data\");\n    expect(result).not.toContain(\"HEARTBEAT\");\n  });\n\n  it(\"preserves non-heartbeat content in messages with heartbeat tokens\", () => {\n    const messages = [\n      { role: \"user\", content: \"Check status and send HEARTBEAT_OK when done\" },\n      { role: \"assistant\", content: \"Status checked.\" },\n    ];\n\n    const result = extractFirstUserMessageWithHeartbeatFilter(messages);\n    \n    // Should preserve the meaningful content even if it mentions heartbeat\n    expect(result).toBe(\"Check status and send when done\");\n  });\n\n  it(\"handles sessions with only heartbeat messages gracefully\", () => {\n    const messages = [\n      { role: \"user\", content: \"HEARTBEAT_OK\" },\n      { role: \"assistant\", content: \"HEARTBEAT_OK\" },\n      { role: \"user\", content: \"**HEARTBEAT_OK**\" }, // Markdown wrapped\n    ];\n\n    const result = extractFirstUserMessageWithHeartbeatFilter(messages);\n    \n    // Should return undefined when all messages are heartbeat content\n    expect(result).toBeUndefined();\n  });\n\n  it(\"preserves normal conversation content without heartbeat tokens\", () => {\n    const messages = [\n      { role: \"user\", content: \"What's the weather like today?\" },\n      { role: \"assistant\", content: \"It's sunny and 75°F.\" },\n    ];\n\n    const result = extractFirstUserMessageWithHeartbeatFilter(messages);\n    \n    // Should work normally for non-heartbeat content\n    expect(result).toBe(\"What's the weather like today?\");\n  });\n});\n"