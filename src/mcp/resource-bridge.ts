/**
 * Resource bridge — discovers and injects MCP resources into agent context.
 *
 * Implementation in Phase 4.
 */

// Placeholder export to satisfy no-empty-file lint rule
export const RESOURCE_BRIDGE_VERSION = "0.0.0";
