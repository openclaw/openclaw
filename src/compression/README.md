# Compression Module (Skeleton)

Planned files:
- `index.ts` (feature flag + orchestration)
- `simple_compress.py` (optional runtime helper)
- adapter integration in session prompt pipeline

Status: skeleton only (no runtime changes yet).
