// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               unknown
// source: envd/v1/browser.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "envd.v1";

export interface LaunchRequest {
  /** Whether to run in headless mode (default true). */
  headless: boolean;
  /** Viewport width in pixels (default 1280). */
  viewportWidth: number;
  /** Viewport height in pixels (default 720). */
  viewportHeight: number;
}

export interface LaunchResponse {
  /** Unique identifier for the browser session. */
  sessionId: string;
  /** VNC info populated when headless=false and desktop environment is available. */
  vncInfo?: VNCInfo | undefined;
}

export interface NavigateRequest {
  sessionId: string;
  url: string;
  /** Navigation timeout in milliseconds (0 = default). */
  timeoutMs: number;
}

export interface NavigateResponse {
  url: string;
  title: string;
}

export interface ClickRequest {
  sessionId: string;
  /** CSS selector for the element to click. */
  selector: string;
}

export interface ClickResponse {}

export interface TypeRequest {
  sessionId: string;
  /** CSS selector for the element to type into. */
  selector: string;
  /** Text to type. */
  text: string;
}

export interface TypeResponse {}

export interface ScreenshotRequest {
  sessionId: string;
  /** Capture the full scrollable page (default false, viewport only). */
  fullPage: boolean;
  /** JPEG quality 1-100. 0 means PNG format. */
  quality: number;
}

export interface ScreenshotResponse {
  /** Image data chunk. */
  data: Buffer;
  /** True when this is the final chunk. */
  eof: boolean;
}

export interface EvaluateJSRequest {
  sessionId: string;
  /** JavaScript expression to evaluate. */
  expression: string;
}

export interface EvaluateJSResponse {
  /** JSON-serialized result of the expression. */
  result: string;
}

export interface ExtractContentRequest {
  sessionId: string;
  /** CSS selector for the element to extract content from. */
  selector: string;
}

export interface ExtractContentResponse {
  /** Text content of the element. */
  text: string;
  /** Inner HTML of the element. */
  html: string;
}

export interface WaitForSelectorRequest {
  sessionId: string;
  /** CSS selector to wait for. */
  selector: string;
  /** Timeout in milliseconds (0 = default). */
  timeoutMs: number;
}

export interface WaitForSelectorResponse {
  /** Whether the selector was found before timeout. */
  found: boolean;
}

export interface GetPageInfoRequest {
  sessionId: string;
}

export interface GetPageInfoResponse {
  title: string;
  url: string;
}

export interface CloseRequest {
  sessionId: string;
}

export interface CloseResponse {}

/** VNCInfo contains VNC connection details for desktop environments. */
export interface VNCInfo {
  /** VNC password for authentication. */
  password: string;
  /** X display number (e.g., 99 for :99). */
  display: number;
  /** Whether the desktop environment is available. */
  available: boolean;
}

/** GetVNCInfoRequest is empty -- VNC info is system-level, not session-specific. */
export interface GetVNCInfoRequest {}

export interface GetVNCInfoResponse {
  vncInfo?: VNCInfo | undefined;
}

function createBaseLaunchRequest(): LaunchRequest {
  return { headless: false, viewportWidth: 0, viewportHeight: 0 };
}

export const LaunchRequest: MessageFns<LaunchRequest> = {
  encode(message: LaunchRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.headless !== false) {
      writer.uint32(8).bool(message.headless);
    }
    if (message.viewportWidth !== 0) {
      writer.uint32(16).uint32(message.viewportWidth);
    }
    if (message.viewportHeight !== 0) {
      writer.uint32(24).uint32(message.viewportHeight);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LaunchRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLaunchRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.headless = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.viewportWidth = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.viewportHeight = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LaunchRequest {
    return {
      headless: isSet(object.headless) ? globalThis.Boolean(object.headless) : false,
      viewportWidth: isSet(object.viewportWidth)
        ? globalThis.Number(object.viewportWidth)
        : isSet(object.viewport_width)
          ? globalThis.Number(object.viewport_width)
          : 0,
      viewportHeight: isSet(object.viewportHeight)
        ? globalThis.Number(object.viewportHeight)
        : isSet(object.viewport_height)
          ? globalThis.Number(object.viewport_height)
          : 0,
    };
  },

  toJSON(message: LaunchRequest): unknown {
    const obj: any = {};
    if (message.headless !== false) {
      obj.headless = message.headless;
    }
    if (message.viewportWidth !== 0) {
      obj.viewportWidth = Math.round(message.viewportWidth);
    }
    if (message.viewportHeight !== 0) {
      obj.viewportHeight = Math.round(message.viewportHeight);
    }
    return obj;
  },

  create(base?: DeepPartial<LaunchRequest>): LaunchRequest {
    return LaunchRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<LaunchRequest>): LaunchRequest {
    const message = createBaseLaunchRequest();
    message.headless = object.headless ?? false;
    message.viewportWidth = object.viewportWidth ?? 0;
    message.viewportHeight = object.viewportHeight ?? 0;
    return message;
  },
};

function createBaseLaunchResponse(): LaunchResponse {
  return { sessionId: "", vncInfo: undefined };
}

export const LaunchResponse: MessageFns<LaunchResponse> = {
  encode(message: LaunchResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.vncInfo !== undefined) {
      VNCInfo.encode(message.vncInfo, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LaunchResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLaunchResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.vncInfo = VNCInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LaunchResponse {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
          ? globalThis.String(object.session_id)
          : "",
      vncInfo: isSet(object.vncInfo)
        ? VNCInfo.fromJSON(object.vncInfo)
        : isSet(object.vnc_info)
          ? VNCInfo.fromJSON(object.vnc_info)
          : undefined,
    };
  },

  toJSON(message: LaunchResponse): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.vncInfo !== undefined) {
      obj.vncInfo = VNCInfo.toJSON(message.vncInfo);
    }
    return obj;
  },

  create(base?: DeepPartial<LaunchResponse>): LaunchResponse {
    return LaunchResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<LaunchResponse>): LaunchResponse {
    const message = createBaseLaunchResponse();
    message.sessionId = object.sessionId ?? "";
    message.vncInfo =
      object.vncInfo !== undefined && object.vncInfo !== null
        ? VNCInfo.fromPartial(object.vncInfo)
        : undefined;
    return message;
  },
};

function createBaseNavigateRequest(): NavigateRequest {
  return { sessionId: "", url: "", timeoutMs: 0 };
}

export const NavigateRequest: MessageFns<NavigateRequest> = {
  encode(message: NavigateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.url !== "") {
      writer.uint32(18).string(message.url);
    }
    if (message.timeoutMs !== 0) {
      writer.uint32(24).uint32(message.timeoutMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NavigateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNavigateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.timeoutMs = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NavigateRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
          ? globalThis.String(object.session_id)
          : "",
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      timeoutMs: isSet(object.timeoutMs)
        ? globalThis.Number(object.timeoutMs)
        : isSet(object.timeout_ms)
          ? globalThis.Number(object.timeout_ms)
          : 0,
    };
  },

  toJSON(message: NavigateRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.timeoutMs !== 0) {
      obj.timeoutMs = Math.round(message.timeoutMs);
    }
    return obj;
  },

  create(base?: DeepPartial<NavigateRequest>): NavigateRequest {
    return NavigateRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<NavigateRequest>): NavigateRequest {
    const message = createBaseNavigateRequest();
    message.sessionId = object.sessionId ?? "";
    message.url = object.url ?? "";
    message.timeoutMs = object.timeoutMs ?? 0;
    return message;
  },
};

function createBaseNavigateResponse(): NavigateResponse {
  return { url: "", title: "" };
}

export const NavigateResponse: MessageFns<NavigateResponse> = {
  encode(message: NavigateResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.url !== "") {
      writer.uint32(10).string(message.url);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NavigateResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNavigateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NavigateResponse {
    return {
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
    };
  },

  toJSON(message: NavigateResponse): unknown {
    const obj: any = {};
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    return obj;
  },

  create(base?: DeepPartial<NavigateResponse>): NavigateResponse {
    return NavigateResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<NavigateResponse>): NavigateResponse {
    const message = createBaseNavigateResponse();
    message.url = object.url ?? "";
    message.title = object.title ?? "";
    return message;
  },
};

function createBaseClickRequest(): ClickRequest {
  return { sessionId: "", selector: "" };
}

export const ClickRequest: MessageFns<ClickRequest> = {
  encode(message: ClickRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.selector !== "") {
      writer.uint32(18).string(message.selector);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClickRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClickRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.selector = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClickRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
          ? globalThis.String(object.session_id)
          : "",
      selector: isSet(object.selector) ? globalThis.String(object.selector) : "",
    };
  },

  toJSON(message: ClickRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.selector !== "") {
      obj.selector = message.selector;
    }
    return obj;
  },

  create(base?: DeepPartial<ClickRequest>): ClickRequest {
    return ClickRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ClickRequest>): ClickRequest {
    const message = createBaseClickRequest();
    message.sessionId = object.sessionId ?? "";
    message.selector = object.selector ?? "";
    return message;
  },
};

function createBaseClickResponse(): ClickResponse {
  return {};
}

export const ClickResponse: MessageFns<ClickResponse> = {
  encode(_: ClickResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClickResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClickResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ClickResponse {
    return {};
  },

  toJSON(_: ClickResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<ClickResponse>): ClickResponse {
    return ClickResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ClickResponse>): ClickResponse {
    const message = createBaseClickResponse();
    return message;
  },
};

function createBaseTypeRequest(): TypeRequest {
  return { sessionId: "", selector: "", text: "" };
}

export const TypeRequest: MessageFns<TypeRequest> = {
  encode(message: TypeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.selector !== "") {
      writer.uint32(18).string(message.selector);
    }
    if (message.text !== "") {
      writer.uint32(26).string(message.text);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TypeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTypeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.selector = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.text = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TypeRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
          ? globalThis.String(object.session_id)
          : "",
      selector: isSet(object.selector) ? globalThis.String(object.selector) : "",
      text: isSet(object.text) ? globalThis.String(object.text) : "",
    };
  },

  toJSON(message: TypeRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.selector !== "") {
      obj.selector = message.selector;
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    return obj;
  },

  create(base?: DeepPartial<TypeRequest>): TypeRequest {
    return TypeRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TypeRequest>): TypeRequest {
    const message = createBaseTypeRequest();
    message.sessionId = object.sessionId ?? "";
    message.selector = object.selector ?? "";
    message.text = object.text ?? "";
    return message;
  },
};

function createBaseTypeResponse(): TypeResponse {
  return {};
}

export const TypeResponse: MessageFns<TypeResponse> = {
  encode(_: TypeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TypeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTypeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): TypeResponse {
    return {};
  },

  toJSON(_: TypeResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<TypeResponse>): TypeResponse {
    return TypeResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<TypeResponse>): TypeResponse {
    const message = createBaseTypeResponse();
    return message;
  },
};

function createBaseScreenshotRequest(): ScreenshotRequest {
  return { sessionId: "", fullPage: false, quality: 0 };
}

export const ScreenshotRequest: MessageFns<ScreenshotRequest> = {
  encode(message: ScreenshotRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.fullPage !== false) {
      writer.uint32(16).bool(message.fullPage);
    }
    if (message.quality !== 0) {
      writer.uint32(24).uint32(message.quality);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScreenshotRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScreenshotRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.fullPage = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.quality = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScreenshotRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
          ? globalThis.String(object.session_id)
          : "",
      fullPage: isSet(object.fullPage)
        ? globalThis.Boolean(object.fullPage)
        : isSet(object.full_page)
          ? globalThis.Boolean(object.full_page)
          : false,
      quality: isSet(object.quality) ? globalThis.Number(object.quality) : 0,
    };
  },

  toJSON(message: ScreenshotRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.fullPage !== false) {
      obj.fullPage = message.fullPage;
    }
    if (message.quality !== 0) {
      obj.quality = Math.round(message.quality);
    }
    return obj;
  },

  create(base?: DeepPartial<ScreenshotRequest>): ScreenshotRequest {
    return ScreenshotRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ScreenshotRequest>): ScreenshotRequest {
    const message = createBaseScreenshotRequest();
    message.sessionId = object.sessionId ?? "";
    message.fullPage = object.fullPage ?? false;
    message.quality = object.quality ?? 0;
    return message;
  },
};

function createBaseScreenshotResponse(): ScreenshotResponse {
  return { data: Buffer.alloc(0), eof: false };
}

export const ScreenshotResponse: MessageFns<ScreenshotResponse> = {
  encode(message: ScreenshotResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data.length !== 0) {
      writer.uint32(10).bytes(message.data);
    }
    if (message.eof !== false) {
      writer.uint32(16).bool(message.eof);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScreenshotResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScreenshotResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = Buffer.from(reader.bytes());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.eof = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScreenshotResponse {
    return {
      data: isSet(object.data) ? Buffer.from(bytesFromBase64(object.data)) : Buffer.alloc(0),
      eof: isSet(object.eof) ? globalThis.Boolean(object.eof) : false,
    };
  },

  toJSON(message: ScreenshotResponse): unknown {
    const obj: any = {};
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    if (message.eof !== false) {
      obj.eof = message.eof;
    }
    return obj;
  },

  create(base?: DeepPartial<ScreenshotResponse>): ScreenshotResponse {
    return ScreenshotResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ScreenshotResponse>): ScreenshotResponse {
    const message = createBaseScreenshotResponse();
    message.data = object.data ?? Buffer.alloc(0);
    message.eof = object.eof ?? false;
    return message;
  },
};

function createBaseEvaluateJSRequest(): EvaluateJSRequest {
  return { sessionId: "", expression: "" };
}

export const EvaluateJSRequest: MessageFns<EvaluateJSRequest> = {
  encode(message: EvaluateJSRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.expression !== "") {
      writer.uint32(18).string(message.expression);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EvaluateJSRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEvaluateJSRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.expression = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EvaluateJSRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
          ? globalThis.String(object.session_id)
          : "",
      expression: isSet(object.expression) ? globalThis.String(object.expression) : "",
    };
  },

  toJSON(message: EvaluateJSRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.expression !== "") {
      obj.expression = message.expression;
    }
    return obj;
  },

  create(base?: DeepPartial<EvaluateJSRequest>): EvaluateJSRequest {
    return EvaluateJSRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EvaluateJSRequest>): EvaluateJSRequest {
    const message = createBaseEvaluateJSRequest();
    message.sessionId = object.sessionId ?? "";
    message.expression = object.expression ?? "";
    return message;
  },
};

function createBaseEvaluateJSResponse(): EvaluateJSResponse {
  return { result: "" };
}

export const EvaluateJSResponse: MessageFns<EvaluateJSResponse> = {
  encode(message: EvaluateJSResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.result !== "") {
      writer.uint32(10).string(message.result);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EvaluateJSResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEvaluateJSResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.result = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EvaluateJSResponse {
    return { result: isSet(object.result) ? globalThis.String(object.result) : "" };
  },

  toJSON(message: EvaluateJSResponse): unknown {
    const obj: any = {};
    if (message.result !== "") {
      obj.result = message.result;
    }
    return obj;
  },

  create(base?: DeepPartial<EvaluateJSResponse>): EvaluateJSResponse {
    return EvaluateJSResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EvaluateJSResponse>): EvaluateJSResponse {
    const message = createBaseEvaluateJSResponse();
    message.result = object.result ?? "";
    return message;
  },
};

function createBaseExtractContentRequest(): ExtractContentRequest {
  return { sessionId: "", selector: "" };
}

export const ExtractContentRequest: MessageFns<ExtractContentRequest> = {
  encode(message: ExtractContentRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.selector !== "") {
      writer.uint32(18).string(message.selector);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExtractContentRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExtractContentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.selector = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExtractContentRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
          ? globalThis.String(object.session_id)
          : "",
      selector: isSet(object.selector) ? globalThis.String(object.selector) : "",
    };
  },

  toJSON(message: ExtractContentRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.selector !== "") {
      obj.selector = message.selector;
    }
    return obj;
  },

  create(base?: DeepPartial<ExtractContentRequest>): ExtractContentRequest {
    return ExtractContentRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ExtractContentRequest>): ExtractContentRequest {
    const message = createBaseExtractContentRequest();
    message.sessionId = object.sessionId ?? "";
    message.selector = object.selector ?? "";
    return message;
  },
};

function createBaseExtractContentResponse(): ExtractContentResponse {
  return { text: "", html: "" };
}

export const ExtractContentResponse: MessageFns<ExtractContentResponse> = {
  encode(message: ExtractContentResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.html !== "") {
      writer.uint32(18).string(message.html);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExtractContentResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExtractContentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.html = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExtractContentResponse {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      html: isSet(object.html) ? globalThis.String(object.html) : "",
    };
  },

  toJSON(message: ExtractContentResponse): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.html !== "") {
      obj.html = message.html;
    }
    return obj;
  },

  create(base?: DeepPartial<ExtractContentResponse>): ExtractContentResponse {
    return ExtractContentResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ExtractContentResponse>): ExtractContentResponse {
    const message = createBaseExtractContentResponse();
    message.text = object.text ?? "";
    message.html = object.html ?? "";
    return message;
  },
};

function createBaseWaitForSelectorRequest(): WaitForSelectorRequest {
  return { sessionId: "", selector: "", timeoutMs: 0 };
}

export const WaitForSelectorRequest: MessageFns<WaitForSelectorRequest> = {
  encode(message: WaitForSelectorRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.selector !== "") {
      writer.uint32(18).string(message.selector);
    }
    if (message.timeoutMs !== 0) {
      writer.uint32(24).uint32(message.timeoutMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WaitForSelectorRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWaitForSelectorRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.selector = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.timeoutMs = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WaitForSelectorRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
          ? globalThis.String(object.session_id)
          : "",
      selector: isSet(object.selector) ? globalThis.String(object.selector) : "",
      timeoutMs: isSet(object.timeoutMs)
        ? globalThis.Number(object.timeoutMs)
        : isSet(object.timeout_ms)
          ? globalThis.Number(object.timeout_ms)
          : 0,
    };
  },

  toJSON(message: WaitForSelectorRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.selector !== "") {
      obj.selector = message.selector;
    }
    if (message.timeoutMs !== 0) {
      obj.timeoutMs = Math.round(message.timeoutMs);
    }
    return obj;
  },

  create(base?: DeepPartial<WaitForSelectorRequest>): WaitForSelectorRequest {
    return WaitForSelectorRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WaitForSelectorRequest>): WaitForSelectorRequest {
    const message = createBaseWaitForSelectorRequest();
    message.sessionId = object.sessionId ?? "";
    message.selector = object.selector ?? "";
    message.timeoutMs = object.timeoutMs ?? 0;
    return message;
  },
};

function createBaseWaitForSelectorResponse(): WaitForSelectorResponse {
  return { found: false };
}

export const WaitForSelectorResponse: MessageFns<WaitForSelectorResponse> = {
  encode(
    message: WaitForSelectorResponse,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.found !== false) {
      writer.uint32(8).bool(message.found);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WaitForSelectorResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWaitForSelectorResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.found = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WaitForSelectorResponse {
    return { found: isSet(object.found) ? globalThis.Boolean(object.found) : false };
  },

  toJSON(message: WaitForSelectorResponse): unknown {
    const obj: any = {};
    if (message.found !== false) {
      obj.found = message.found;
    }
    return obj;
  },

  create(base?: DeepPartial<WaitForSelectorResponse>): WaitForSelectorResponse {
    return WaitForSelectorResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WaitForSelectorResponse>): WaitForSelectorResponse {
    const message = createBaseWaitForSelectorResponse();
    message.found = object.found ?? false;
    return message;
  },
};

function createBaseGetPageInfoRequest(): GetPageInfoRequest {
  return { sessionId: "" };
}

export const GetPageInfoRequest: MessageFns<GetPageInfoRequest> = {
  encode(message: GetPageInfoRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPageInfoRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPageInfoRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPageInfoRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
          ? globalThis.String(object.session_id)
          : "",
    };
  },

  toJSON(message: GetPageInfoRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    return obj;
  },

  create(base?: DeepPartial<GetPageInfoRequest>): GetPageInfoRequest {
    return GetPageInfoRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetPageInfoRequest>): GetPageInfoRequest {
    const message = createBaseGetPageInfoRequest();
    message.sessionId = object.sessionId ?? "";
    return message;
  },
};

function createBaseGetPageInfoResponse(): GetPageInfoResponse {
  return { title: "", url: "" };
}

export const GetPageInfoResponse: MessageFns<GetPageInfoResponse> = {
  encode(message: GetPageInfoResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.title !== "") {
      writer.uint32(10).string(message.title);
    }
    if (message.url !== "") {
      writer.uint32(18).string(message.url);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPageInfoResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPageInfoResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.url = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPageInfoResponse {
    return {
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      url: isSet(object.url) ? globalThis.String(object.url) : "",
    };
  },

  toJSON(message: GetPageInfoResponse): unknown {
    const obj: any = {};
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    return obj;
  },

  create(base?: DeepPartial<GetPageInfoResponse>): GetPageInfoResponse {
    return GetPageInfoResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetPageInfoResponse>): GetPageInfoResponse {
    const message = createBaseGetPageInfoResponse();
    message.title = object.title ?? "";
    message.url = object.url ?? "";
    return message;
  },
};

function createBaseCloseRequest(): CloseRequest {
  return { sessionId: "" };
}

export const CloseRequest: MessageFns<CloseRequest> = {
  encode(message: CloseRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CloseRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCloseRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CloseRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
          ? globalThis.String(object.session_id)
          : "",
    };
  },

  toJSON(message: CloseRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    return obj;
  },

  create(base?: DeepPartial<CloseRequest>): CloseRequest {
    return CloseRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CloseRequest>): CloseRequest {
    const message = createBaseCloseRequest();
    message.sessionId = object.sessionId ?? "";
    return message;
  },
};

function createBaseCloseResponse(): CloseResponse {
  return {};
}

export const CloseResponse: MessageFns<CloseResponse> = {
  encode(_: CloseResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CloseResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCloseResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): CloseResponse {
    return {};
  },

  toJSON(_: CloseResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<CloseResponse>): CloseResponse {
    return CloseResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<CloseResponse>): CloseResponse {
    const message = createBaseCloseResponse();
    return message;
  },
};

function createBaseVNCInfo(): VNCInfo {
  return { password: "", display: 0, available: false };
}

export const VNCInfo: MessageFns<VNCInfo> = {
  encode(message: VNCInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.password !== "") {
      writer.uint32(10).string(message.password);
    }
    if (message.display !== 0) {
      writer.uint32(16).uint32(message.display);
    }
    if (message.available !== false) {
      writer.uint32(24).bool(message.available);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VNCInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVNCInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.password = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.display = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.available = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VNCInfo {
    return {
      password: isSet(object.password) ? globalThis.String(object.password) : "",
      display: isSet(object.display) ? globalThis.Number(object.display) : 0,
      available: isSet(object.available) ? globalThis.Boolean(object.available) : false,
    };
  },

  toJSON(message: VNCInfo): unknown {
    const obj: any = {};
    if (message.password !== "") {
      obj.password = message.password;
    }
    if (message.display !== 0) {
      obj.display = Math.round(message.display);
    }
    if (message.available !== false) {
      obj.available = message.available;
    }
    return obj;
  },

  create(base?: DeepPartial<VNCInfo>): VNCInfo {
    return VNCInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<VNCInfo>): VNCInfo {
    const message = createBaseVNCInfo();
    message.password = object.password ?? "";
    message.display = object.display ?? 0;
    message.available = object.available ?? false;
    return message;
  },
};

function createBaseGetVNCInfoRequest(): GetVNCInfoRequest {
  return {};
}

export const GetVNCInfoRequest: MessageFns<GetVNCInfoRequest> = {
  encode(_: GetVNCInfoRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetVNCInfoRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetVNCInfoRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetVNCInfoRequest {
    return {};
  },

  toJSON(_: GetVNCInfoRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<GetVNCInfoRequest>): GetVNCInfoRequest {
    return GetVNCInfoRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<GetVNCInfoRequest>): GetVNCInfoRequest {
    const message = createBaseGetVNCInfoRequest();
    return message;
  },
};

function createBaseGetVNCInfoResponse(): GetVNCInfoResponse {
  return { vncInfo: undefined };
}

export const GetVNCInfoResponse: MessageFns<GetVNCInfoResponse> = {
  encode(message: GetVNCInfoResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.vncInfo !== undefined) {
      VNCInfo.encode(message.vncInfo, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetVNCInfoResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetVNCInfoResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.vncInfo = VNCInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetVNCInfoResponse {
    return {
      vncInfo: isSet(object.vncInfo)
        ? VNCInfo.fromJSON(object.vncInfo)
        : isSet(object.vnc_info)
          ? VNCInfo.fromJSON(object.vnc_info)
          : undefined,
    };
  },

  toJSON(message: GetVNCInfoResponse): unknown {
    const obj: any = {};
    if (message.vncInfo !== undefined) {
      obj.vncInfo = VNCInfo.toJSON(message.vncInfo);
    }
    return obj;
  },

  create(base?: DeepPartial<GetVNCInfoResponse>): GetVNCInfoResponse {
    return GetVNCInfoResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetVNCInfoResponse>): GetVNCInfoResponse {
    const message = createBaseGetVNCInfoResponse();
    message.vncInfo =
      object.vncInfo !== undefined && object.vncInfo !== null
        ? VNCInfo.fromPartial(object.vncInfo)
        : undefined;
    return message;
  },
};

/** BrowserService provides browser automation inside the MicroVM via Chromium. */
export type BrowserServiceDefinition = typeof BrowserServiceDefinition;
export const BrowserServiceDefinition = {
  name: "BrowserService",
  fullName: "envd.v1.BrowserService",
  methods: {
    /** Launch starts a new browser session. */
    launch: {
      name: "Launch",
      requestType: LaunchRequest as typeof LaunchRequest,
      requestStream: false,
      responseType: LaunchResponse as typeof LaunchResponse,
      responseStream: false,
      options: {},
    },
    /** Navigate loads a URL in the browser. */
    navigate: {
      name: "Navigate",
      requestType: NavigateRequest as typeof NavigateRequest,
      requestStream: false,
      responseType: NavigateResponse as typeof NavigateResponse,
      responseStream: false,
      options: {},
    },
    /** Click clicks an element matching a CSS selector. */
    click: {
      name: "Click",
      requestType: ClickRequest as typeof ClickRequest,
      requestStream: false,
      responseType: ClickResponse as typeof ClickResponse,
      responseStream: false,
      options: {},
    },
    /** Type types text into an element matching a CSS selector. */
    type: {
      name: "Type",
      requestType: TypeRequest as typeof TypeRequest,
      requestStream: false,
      responseType: TypeResponse as typeof TypeResponse,
      responseStream: false,
      options: {},
    },
    /** Screenshot captures the current page as a streamed image. */
    screenshot: {
      name: "Screenshot",
      requestType: ScreenshotRequest as typeof ScreenshotRequest,
      requestStream: false,
      responseType: ScreenshotResponse as typeof ScreenshotResponse,
      responseStream: true,
      options: {},
    },
    /** EvaluateJS executes a JavaScript expression and returns the result. */
    evaluateJS: {
      name: "EvaluateJS",
      requestType: EvaluateJSRequest as typeof EvaluateJSRequest,
      requestStream: false,
      responseType: EvaluateJSResponse as typeof EvaluateJSResponse,
      responseStream: false,
      options: {},
    },
    /** ExtractContent extracts text and HTML from an element. */
    extractContent: {
      name: "ExtractContent",
      requestType: ExtractContentRequest as typeof ExtractContentRequest,
      requestStream: false,
      responseType: ExtractContentResponse as typeof ExtractContentResponse,
      responseStream: false,
      options: {},
    },
    /** WaitForSelector waits for an element to appear in the DOM. */
    waitForSelector: {
      name: "WaitForSelector",
      requestType: WaitForSelectorRequest as typeof WaitForSelectorRequest,
      requestStream: false,
      responseType: WaitForSelectorResponse as typeof WaitForSelectorResponse,
      responseStream: false,
      options: {},
    },
    /** GetPageInfo returns the current page title and URL. */
    getPageInfo: {
      name: "GetPageInfo",
      requestType: GetPageInfoRequest as typeof GetPageInfoRequest,
      requestStream: false,
      responseType: GetPageInfoResponse as typeof GetPageInfoResponse,
      responseStream: false,
      options: {},
    },
    /** Close terminates a browser session. */
    close: {
      name: "Close",
      requestType: CloseRequest as typeof CloseRequest,
      requestStream: false,
      responseType: CloseResponse as typeof CloseResponse,
      responseStream: false,
      options: {},
    },
    /** GetVNCInfo returns VNC connection details when desktop environment is available. */
    getVNCInfo: {
      name: "GetVNCInfo",
      requestType: GetVNCInfoRequest as typeof GetVNCInfoRequest,
      requestStream: false,
      responseType: GetVNCInfoResponse as typeof GetVNCInfoResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
