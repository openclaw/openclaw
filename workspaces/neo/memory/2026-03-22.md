# 2026-03-22 — Git History Research

## Active Branches

### Main Branch (current)

- **Status:** 3 commits ahead of origin/main
- **Last commit:** `61f89da164` — auto-improve: add mandatory write-back + retrieval triggers to trinity AGENTS.md
- **Uncommitted changes:** `.stellar-powers/workflow.jsonl` (M), `.claude/settings.local.json` (untracked)

### Local Branches (by recency)

| Branch                            | Last Activity | Notes                                 |
| --------------------------------- | ------------- | ------------------------------------- |
| `main`                            | Today         | Active development                    |
| `feature/onboarding-phase3`       | 5 days ago    | Onboarding GUI work                   |
| `feature/onboarding-phase2-fixes` | 5 days ago    | Onboarding fixes                      |
| `feature/onboarding-gui`          | 5 days ago    | GUI onboarding feature                |
| `docs/rebrand-readme-operator1`   | 5 days ago    | Documentation                         |
| `sync/v2026.3.13-*` (6 branches)  | 6 days ago    | Sync branches for beta.1 release      |
| `sync/v2026.3.12-*` (5 branches)  | ~1 week ago   | Prior sync branches                   |
| `backup/*` (3 branches)           | Older         | Backup branches before upstream syncs |
| `merge/upstream-v2026.2.12`       | Older         | Upstream merge                        |

### Remote Tracking

- **origin:** Main fork (Rohit's repo)
- **paperclip:** Paperclip Games fork — many feature branches including worktree support, dotta, UI fixes
- **upstream:** OpenClaw upstream — extensive branch list with many codex/_, feat/_, fix/\* branches

---

## Recent Work (Last 2 Weeks)

### March 22 (Today)

**Theme: Auto-Improve Agent & Memory System**

Major commits:

1. `61f89da164` — auto-improve: add mandatory write-back + retrieval triggers to trinity AGENTS.md
2. `b772cbbc24` — auto-improve: add mandatory write-back checklist to morpheus AGENTS.md
3. `476ded2f32` — auto-improve: strengthen memory retrieval triggers in operator1 AGENTS.md
4. `71d57a8a88` — feat(agent): add memory metrics to auto-improve, configure cross-agent memory access
5. `f32a140cfc` — **feat: move agent workspaces into repo — operator1, neo, morpheus, trinity**
6. `e47963e2d7` — feat(agent): auto-improve monitors all 4 agents — Operator1, Neo, Morpheus, Trinity
7. `73a6cf8cc3` — **feat: add auto-improve agent and eval harness skill**
8. `76b1e60876` — feat(agent): add channel-aware compaction prompt and post-compaction re-injection
9. `394aa8f41d` — feat(agent): add delegation nudge when agent uses too many direct tools
10. `a00be5e061` — feat(agent): add memory pre-fetch for context-referencing messages
11. `3f8dc8ebbf` — feat(agent): add channel section to system prompt, move memory recall higher

**Key Pattern:** Building out the auto-improve evaluation harness and memory system for multi-agent coordination.

### March 19

**Theme: YouTube Analysis Skill**

- Full youtube-analysis skill implementation with:
  - `fetch_video.py`, `parse_transcript.py`, `save_analysis.py`
  - SKILL.md with yt-dlp reference docs
  - Fixes to prioritize youtube-analysis over summarize skill
  - `--cookies-from-browser` flag for anti-bot bypass

### March 20

- Spec status updates and SKILL.md formatting

---

## Patterns Observed

### 1. Auto-Improve Evaluation Harness

- New system for monitoring and evaluating agent performance
- Monitors all 4 agents: Operator1, Neo (CTO), Morpheus (CMO), Trinity (CFO)
- Includes memory metrics and cross-agent memory access
- Gateway restart handling after workspace file changes

### 2. Workspace Consolidation

- Agent workspaces moved into repo: `workspaces/operator1`, `workspaces/neo`, `workspaces/morpheus`, `workspaces/trinity`
- Templates managed in `docs/reference/templates` (lazy-loaded)

### 3. Memory System Enhancements

- Memory pre-fetch for context-referencing messages
- Channel-aware compaction with post-compaction re-injection
- Mandatory write-back triggers added to agent AGENTS.md files

### 4. Delegation Architecture

- Delegation nudge when agents use too many direct tools
- Encourages proper delegation to subagents

### 5. Sync Branch Strategy

- Organized sync branches by version: `sync/v2026.3.13-beta.1-{core,features,integration,platform,security,ui-reference}`
- Suggests structured release process with categorized merges

---

## Key Files/Directories

| Path                        | Purpose                                                         |
| --------------------------- | --------------------------------------------------------------- |
| `skills/auto-improve/`      | Evaluation harness skill                                        |
| `skills/youtube-analysis/`  | YouTube video analysis                                          |
| `extensions/acpx/`          | ACP runtime extension                                           |
| `workspaces/`               | Agent workspace directories (neo, morpheus, trinity, operator1) |
| `docs/reference/templates/` | Workspace templates (lazy-loaded)                               |

---

## Notable Technical Details

1. **Gateway restart handling:** Multiple iterations (pkill -9 → openclaw gateway restart) for reliable WebSocket handling
2. **Memory pre-fetch:** Dynamic import with runtime boundary for memory prefetch
3. **Tool execution metrics:** Added subagent tool execution metric to auto-improve
4. **Active session check:** Before gateway restart to prevent tool execution failure

---

## Upstream Activity (from branch list)

The upstream OpenClaw repo shows extensive activity:

- Many `codex/*` branches (Codex AI agent features)
- `feat/*` branches for new features (web search, telegram, slack, etc.)
- `fix/*` branches for bug fixes across channels
- Active dependabot PRs
- Security hardening work (`security/*` branches)

---

## Action Items / Follow-ups

- [ ] Push 3 local commits to origin/main
- [ ] Review `feature/onboarding-phase3` for merge readiness
- [ ] Clean up old backup branches if no longer needed
- [ ] Consider consolidating sync branches after release
