# 2026-02-13

## Session Notes

- User asked how multiple LLM providers route calls — explained it's deterministic (primary → fallbacks in order), not random.
- User called out overly verbose responses. Updated `SOUL.md` with explicit conciseness directive: "Keep answers concise. Don't over-explain unless explicitly asked. If it can be said in two lines, don't use ten."
- Current config: single provider `google-antigravity/claude-opus-4-6-thinking`, no fallbacks configured yet. User was exploring adding a second provider.
- User is locally developing OpenClaw with their own version (`ui-next` frontend). Proposed 21 GUI features for ui-next including: onboard wizard, agent management, model picker, auth manager, channel manager, plugin toggles, skill browser, session list/inspector, cron job manager, gateway controls, config editor, node manager, memory/workspace file browser. User explicitly said **don't start coding** — a separate agent handles that.
- Bootstrap files (BOOTSTRAP.md, IDENTITY.md, USER.md) still exist — first-run setup hasn't been completed yet.
