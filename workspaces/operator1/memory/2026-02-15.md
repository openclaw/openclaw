# 2026-02-15

## QMD Memory Search — Setup Completed

- Installed QMD v1.0.0 via Bun (`/Users/rohits/.bun/bin/qmd`)
- Bun 1.3.8 at `/Users/rohits/Library/PhpWebStudy/app/bun/1.3.8/bun`, symlinked to `/Users/rohits/.bun/bin/bun`
- 3 GGUF models downloaded to `~/.cache/qmd/models/` (~2.2GB total):
  - embeddinggemma-300M (313MB)
  - qmd-query-expansion-1.7B (1.28GB)
  - qwen3-reranker-0.6b (639MB)
- Config: `memory.backend: "qmd"` with `limits.timeoutMs: 15000` (default 4s was too short for cold model loading)
- **Critical fix:** Added `PATH=/Users/rohits/.bun/bin:${PATH}` to `~/.openclaw/.env` — gateway doesn't inherit `~/.zshrc`
- Without both the `.env` PATH fix AND `memory.qmd.command` full path, you get `spawn qmd ENOENT` errors
- Once QMD fails on first query, it permanently falls back to builtin for that boot cycle (needs restart to recover)
- `memory_search` confirmed working: `provider: "qmd"`, score 0.88 on test query
- Full setup log written to `/Users/rohits/dev/operator1/Project-tasks/local-embeddings-memory-proposal.md`

## Memory UI Feature Discussion

- User wants a Memory tab under Agents in ui-next dashboard
- Discussed 4 memory mechanisms: explicit writes, compaction flush, heartbeat review, QMD indexing
- Heartbeat–memory link: heartbeats serve as agent reflection time for curating MEMORY.md (behavioral, not infrastructure)
- Designed 5 panels: Memory Files, Search, Index Status, Activity Log, Maintenance Panel
- 3 new gateway RPCs proposed: `memory.status`, `memory.search`, `memory.reindex`
- Full UI spec + Zustand store shape added to `local-embeddings-memory-proposal.md`
- P0 features: Memory file browser + Search UI
- Key insight: Activity Log (P2) parses tool calls from session transcripts to show when/why memory was written/read

## Matrix Multi-Agent System

- Implementation guide at `/Users/rohits/dev/operator1/Project-tasks/matrix-multi-agent-implementation.md` (~37KB)
- Big Five C-suite: CEO=User, COO=Operator1(main), CTO=Neo, CMO=Morpheus, CFO=Trinity
- Two gateway patterns: Collocated (port 18789) and Independent (Sati 19789, Link 20789)
- UI integration plan ("The Construct") added as Section 11
- Sessions Task Manager, Cron Dashboard, Cost Tracker already exist in ui-next — not duplicated

## Key File Locations

- OpenClaw config: `~/.openclaw/openclaw.json`
- QMD state: `~/.openclaw/agents/main/qmd/`
- QMD models: `~/.cache/qmd/models/`
- Implementation guide: `/Users/rohits/dev/operator1/Project-tasks/matrix-multi-agent-implementation.md`
- Memory/QMD proposal: `/Users/rohits/dev/operator1/Project-tasks/local-embeddings-memory-proposal.md`
- UI proposals: `/Users/rohits/dev/operator1/Project-tasks/ui-next-feature-proposals.md`

## User Preferences

- Prefers self-hosted/local solutions over cloud APIs
- Wants documentation-first approach before making code/config changes
- Uses Google Antigravity OAuth (no OpenAI/Anthropic API keys)
- Channel: BlueBubbles (iMessage bridge) on `http://192.168.1.19:1234`
