# 2026-02-27

## MAIBOT / Claude Code cooldown 안정화 후속 점검

- 사용자 요청: Claude Code cooldown 대규모 업데이트 점검 + 즉시 보완 적용
- 반영 작업:
  1. Claude Code 업데이트: `2.1.56 -> 2.1.59`
  2. 플러그인 오류 복구: `dev-workflows@claude-code-workflows`의 누락된 `design-sync.md` 경로 복원 후 로드 정상화
  3. OpenClaw fallback 순서 조정:
     - `anthropic/claude-opus-4-6` (primary)
     - `openai-codex/gpt-5.3-codex`
     - `anthropic/claude-sonnet-4-6`
     - `google/gemini-2.5-flash`
  4. 게이트웨이 재시작 및 상태 확인 (`openclaw gateway status`: running, RPC probe ok)
  5. Claude Code 자동 업데이트 활성화 (`C:\Users\jini9\.claude.json`의 `autoUpdates: true`)

- 검증 결과:
  - `claude --version` => `2.1.59 (Claude Code)`
  - `claude plugin list` => `dev-workflows` 상태 `enabled`
  - `claude doctor` => 기존 plugin path 오류 미재현
  - `openclaw config get agents.defaults.model --json` => fallbacks 적용 확인

---

## MAIBOTALKS App Store 재제출 후속 (IAP 보완 + 빌드 6)

- 사용자 요청: BOTALKS Apple 배포 현황 재점검 및 재제출 조치 계속 진행
- 확인/조치:
  1. App Store Connect 심사 거절 원인 재확인
     - 이슈: IAP 미제출 + IAP 동작 검증 실패 (2.1.0)
  2. 구독 메타데이터 보완
     - Monthly/Annual 모두 심사 스크린샷 등록 상태 확인
     - 구독 그룹 로컬라이제이션(`BOTALKS Premium`) 추가
     - 구독 상태: `메타데이터 누락됨` → `제출 준비 완료`
  3. iOS 1.0 심사 버전에 IAP 2종 연결
     - `botalks_monthly`, `botalks_annual` 선택 반영
  4. 신규 빌드/제출 실행
     - EAS iOS production build: `1.0.0 (6)` 생성 완료
     - Build ID: `624456a2-a0ee-4e0d-b347-f1ca0dc7badb`
     - EAS submission 생성: `7d5cd291-f0cf-4bb3-b688-09cb45c68db1`
     - 현재 상태: `Queued (Free Tier Queue)`

- 현재 대기 항목:
  - EAS submission이 처리 시작/완료될 때까지 대기
  - App Store Connect TestFlight에 build 6 반영 확인 후 심사 업데이트 상태 재확인
