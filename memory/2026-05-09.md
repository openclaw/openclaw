# Memory — 2026-05-09

## daily-openclaw-bugfix-pr / issue #79066
- Cron `60860a3a-d5db-4bc9-8276-c6c60386a85f` ran against `openclaw/openclaw` bug issues and selected issue `#79066` (`https://github.com/openclaw/openclaw/issues/79066`).
- Spawned fix subagent `agent:main:subagent:d4bd76ac-3d59-4d63-beba-c9d0fed5c6fc` with run id `c868ab39-1c48-4795-b9b5-74a745003cd3`.
- Because `/data/.clawdbot` was unavailable in this runtime, the run used fallback state under `/tmp/openclaw-gh-issues-state/`, including:
  - `/tmp/openclaw-gh-issues-state/gh-issues-claims.json`
  - `/tmp/openclaw-gh-issues-state/gh-issues-cursor-openclaw-openclaw.json`
- Freed disk pressure before the fix by deleting large temporary repo dirs under `/tmp` and improving free space from about `123Mi` to `1.8Gi`.
- Fix work happened in worktree `/tmp/oc-79066` on branch `fix/issue-79066` after fetching `origin/main` to `af0c273d28`.
- Local fix outcome:
  - commit `08c78f1370` — `fix: share gateway missing-scope responses`
  - added shared helper `sendMissingScopeForbidden`
  - replaced duplicated Gateway HTTP 403 missing-scope payload handling across 6 callsites
- Verification passed:
  - `pnpm test src/gateway/http-common.test.ts src/gateway/http-endpoint-helpers.test.ts src/gateway/http-auth-utils.test.ts src/gateway/models-http.test.ts src/gateway/session-kill-http.test.ts src/gateway/control-ui.http.test.ts`
- Push/PR failed because GitHub denied write access for token user `yfge` (`403` on push to `openclaw/openclaw`), so no PR was created.
- Cleared the temporary claim / in-progress state afterward so later cron runs would not stay blocked.

## Ada (`openclaw-team`) gateway bring-up
- Confirmed Ada is the local macOS user `openclaw-team`; Gateway service file is `/Users/openclaw-team/Library/LaunchAgents/ai.openclaw.gateway.plist` with label `ai.openclaw.gateway` and loopback port `18790`.
- Observed Ada service state before recovery:
  - Gateway not listening on `127.0.0.1:18790`
  - LaunchAgent installed but `not loaded`
  - service config/version looked stale (`v2026.4.2`)
  - log path referenced by status: `/tmp/openclaw-502/openclaw-2026-05-08.log`
- Normal service start failed with launchd domain errors:
  - `launchctl kickstart` failed with code `125`
  - `launchctl bootstrap gui/502` also failed; user domain only showed a background session
- Worked around launchd by writing helper script `/tmp/start-ada-gateway.py`, then running it as `openclaw-team` via `/usr/bin/python3`.
- Manual start succeeded with process `pid 4032`.
- Verification after bring-up:
  - `http://127.0.0.1:18790/` returned `200 OK`
  - gateway probe became `ok`
  - capability reported `admin-capable`
- Important caveat: Ada is running from a manual background start, not from a healthy launchd load; LaunchAgent still shows `not loaded` and needs later repair for durable operation.

## daily-openclaw-bugfix-pr / issue #79490
- Later the same cron `60860a3a-d5db-4bc9-8276-c6c60386a85f` selected issue `#79490` (`https://github.com/openclaw/openclaw/issues/79490`) about `SPAWN_ALLOWLIST` being ignored for `sessions_spawn`.
- Because the main workspace was dirty and a full clone hit disk-space limits, the run switched to clean worktree `.gh-issues-worktree/` at `origin/main` commit `995d2af0364d457515e0dbcab536c434cf48fb44`.
- Because `/data/.clawdbot` was unavailable/read-only in this runtime, claim/cursor state used workspace-local files instead:
  - `.gh-issues-state/gh-issues-claims.json`
  - `.gh-issues-state/gh-issues-cursor-openclaw-openclaw.json`
- Spawned fix subagent `agent:main:subagent:3f8bc985-5394-4cb6-b633-b9963ff0255a` for branch `fix/issue-79490`.
- Local fix completed but PR creation failed:
  - local commit `7f25c06bb7`
  - changed files `src/config/io.ts`, `src/config/io.compat.test.ts`
  - targeted config/agent allowlist tests passed (`29` tests)
  - push to `openclaw/openclaw` was rejected with `403` because token user `yfge` lacked upstream push permission
- Cleared the temporary in-progress state afterward so later cron runs would not remain blocked.

## daily bugfix cron diagnosis
- Follow-up diagnosis found the daily bugfix巡检 should use fork mode (`yfge/openclaw`) instead of trying to push branches directly to `openclaw/openclaw`; upstream repo permission for `yfge` is pull-only while the fork exists and is writable.
- Also noted existing open-PR backlog for `yfge` in `openclaw/openclaw`; failing items called out were `#77653`, `#77639`, and `#75970`, while `#78809` and `#74913` looked mostly green. Recommendation in chat was to prioritize PR巡检 before opening more new bugfix PRs.
