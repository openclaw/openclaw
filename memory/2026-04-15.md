- 2026-04-15: OpenClaw Ollama reasoning-only recovery fix was implemented and published as PR `https://github.com/openclaw/openclaw/pull/66552` on branch `fix/ollama-reasoning-only-recovery`, commit `c3e1862b88`. The fix broadened incomplete-turn recovery so `provider: ollama` can reuse reasoning-only / empty-response retry handling, with targeted coverage added in `src/agents/execution-contract.ts` and `src/agents/pi-embedded-runner/run.incomplete-turn.test.ts`; verification passed with `pnpm exec vitest run src/agents/pi-embedded-runner/run.incomplete-turn.test.ts`.
- 2026-04-15: The skill repo push for `git@github.com:yfge/video-pipeline.git` landed on branch `import-video-pipeline-skill` instead of `main` because the remote already had existing content. Durable refs: PR creation URL `https://github.com/yfge/video-pipeline/pull/new/import-video-pipeline-skill`, commits `eafffaa` (initial skill push), `4c90493` (added related skills and bundled `references/` + `scripts/`), and `2c810db` (filled current `video-clipper` script resources).
- 2026-04-15: The pushed repo content was expanded beyond `video-pipeline` to include `skills/video-pipeline/`, `skills/video-to-text/`, `skills/insight-extractor/`, `skills/video-clipper/`, and `skills/article-forge/`. The follow-up specifically corrected an omission where related skill files were initially not included, then synced the current `video-clipper` implementation resources such as `run-full-clipper.sh`, `clip-postcheck.py`, `clip-subtitles.py`, `smart-silence.py`, `stutter-skip-gen.py`, `drag-skip-gen.py`, `llm-skip-apply.py`, `llm-skip-apply-v3.py`, `fix-av-sync.py`, `iterate-until-clean.py`, `gate-clips.sh`, and `clip-content-gate.py`.
