# Memory — 2026-05-10

## daily-openclaw-bugfix-pr / issue #79804
- Cron `60860a3a-d5db-4bc9-8276-c6c60386a85f` ran against `openclaw/openclaw` bug issues and identified issue `#79804` as the best candidate.
- No safe fix was attempted because the required latest-code prep failed:
  - `git fetch origin main` failed with `fatal: write error: No space left on device`
  - disk state at the time was about `120Mi` free on `/System/Volumes/Data`
- gh-issues state paths under `/data/.clawdbot/...` were unavailable/read-only in this runtime, so standard claim/cursor persistence was blocked.
- Outcome recorded in chat: skip the fix rather than violate the skill requirement to work from freshly updated code.

## PR巡检 skill hardening for author-list URLs
- Updated PR巡检 guidance to avoid treating GitHub author-list URLs as single PRs.
- Modified files:
  - `.agents/skills/openclaw-pr-maintainer/SKILL.md`
  - `skills/github/SKILL.md`
- Key rule added: parse `https://github.com/openclaw/openclaw/pulls/yfge` as an author-filtered PR list and use `gh pr list --repo openclaw/openclaw --author yfge --state open`, not `gh pr view`.
- Also documented that `gh pr view` is only for `/pull/<number>` or numeric PR refs, and PR巡检 summaries should include mergeability, non-green checks, review state, conflicts, and recommended next action.

## yfge open PR sweep
- Reviewed open PRs for author `yfge` in `openclaw/openclaw` from `https://github.com/openclaw/openclaw/pulls/yfge`.
- Safe-to-land candidates identified:
  - `#78809` — CLEAN / MERGEABLE / checks green except CodeQL neutral
  - `#74913` — CLEAN / MERGEABLE / checks green except CodeQL neutral
- Blocked PRs identified:
  - `#77653` — CONFLICTING, `Real behavior proof` failed
  - `#77639` — CONFLICTING, `Real behavior proof` failed
  - `#75970` — MERGEABLE but UNSTABLE, `Real behavior proof` and `macos-swift` failed
- Attempt to rerun the failing macOS Swift job hit a permissions boundary: GitHub returned `Must have admin rights to Repository`.
- Final recommendation in chat: only `#78809` and `#74913` were ready for explicit merge approval; the others needed proof and/or conflict resolution first.
