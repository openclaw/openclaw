---
type: session-log
project: MAIBOT
date: 2026-02-15
tags: [discord, voice-message]
---

# 2026-02-15 메모리

## Discord Voice Message 기능 시도

- 지니님이 OpenClaw 최근 업데이트에 디스코드 보이스 메시징이 있다고 질문
- 현재 로컬 버전(2026.2.13, 942커밋 뒤처짐)에는 미구현
- upstream에 "Discord voice message 지원" 이미 포함됨
- **업데이트 후 사용 가능** — 지니님이 처음부터 이걸 알고 계셨음 ㅋㅋ

## 설치/설정 완료 항목

- **ffmpeg 8.0.1** 설치 완료 (winget install Gyan.FFmpeg)
- **TTS 설정** 추가: `messages.tts.provider=openai`, `openai.model=gpt-4o-mini-tts`
- TTS 파일이 0바이트로 생성되는 문제 발생 — 원인 미확인, 추가 디버깅 필요

## 업데이트 필요성 확인

- 데일리 노트 `00.DAILY/2026-02-15_Moltbot Update Check.md`에 상세 내역 있음
- 942커밋 미적용, 보안 패치 10건+, 브레이킹 체인지 3건
- `upstream-sync` 스킬로 안전 머지 가능
- 지니님에게 업데이트 진행 여부 질문한 상태 — 아직 답변 없음

## 업데이트 완료

- upstream-sync 서브에이전트로 실행, 6분 소요
- **1,111커밋 머지** 성공
- 충돌 4개: `.gitignore`, `src/agents/skills/config.ts`, `src/hooks/config.ts`, `src/process/exec.ts` → 모두 theirs 수용
- 빌드 ✅, 테스트 4,862 통과 / 3 실패 (upstream 기존 이슈)
- git push 완료 (`ca8352d55..c8b04683f`)
- 게이트웨이 재시작 완료
- `commands.restart=true` 설정 추가함

## TTS 0바이트 문제 (미해결)

- `messages.tts` 설정 추가했으나 TTS 파일이 계속 0바이트로 생성됨
- OpenAI API 키로 TTS 호출은 되지만 파일이 비어있음
- 추후 디버깅 필요

## Discord Voice Message — 버그 발견 & 이슈 등록

- 업데이트 후에도 asVoice=true로 보이스 메시지 전송 불가
- **근본 원인**: tool call 인프라가 tool execute 함수 실행 전에 파라미터 검증 → `message` 없이 `send` 시 "content required" 에러
- debug logging으로 확인: `createMessageTool.execute`도, `handleSendAction`도 호출되지 않음
- Discord voice message 코드 자체(voice-message.ts, discord-actions-messaging.ts)는 완벽 구현됨
- **OpenClaw upstream 이슈 등록**: https://github.com/openclaw/openclaw/issues/16688
- handle-action.ts에 로컬 수정 적용됨 (asVoiceEarly 체크) — 하지만 tool 인프라 문제라 효과 없음
- dist에 debug logging 넣은 상태 → 다음 빌드 시 덮어쓰면 됨

## dist 디버그 로그 정리 필요

- `dist/pi-embedded-CjtyoaXQ.js`, `dist/pi-embedded-DqB5nY_d.js`, `dist/reply-DvThn52S.js`에 VOICE-DEBUG 로그 삽입됨
- 다음 `pnpm build` 시 자동 덮어씀 — 별도 정리 불필요

## MAIBOTALKS (BOTALKS) 프로젝트 시작 — 2026-02-15

- **컨셉**: 마이봇과 다이렉트 음성대화하는 심플한 모바일 앱
- **브랜드**: BOTALKS (Bot + Talks, 보톡스 💉)
- **플랫폼**: 모바일 앱 (지니님 명시 — 웹앱 아님)
- **프로젝트 세팅 완료**: 로컬, Obsidian(10.MAIBOTALKS), GitHub(jini92/MAIBOTALKS), 메모리
- **PRD 팀 에이전트 3명 완료**:
  - 리서치: `docs/A-market-research.md`
  - PRD: `docs/D-PRD.md` (웹앱 기준 → 모바일 수정 필요)
  - 기술검증: `docs/A-technical-feasibility.md`
- **핵심 결론**:
  - 자체 파이프라인 월 $3~5 vs OpenAI Realtime $270 → 자체 압승
  - 지연시간 5-7초 (스트리밍 시 3-5초) — 실용적
  - 권장 스택: React Native(Expo) + Whisper STT + Edge TTS(무료) + OpenClaw API
  - MVP 2주 가능
- **PRD v2.0 완료** (모바일 앱 + 수익화):
  - React Native + Expo (EAS Build) → iOS/Android 동시
  - 수익화: $6.99/월 구독 (Free/Premium 티어) + RevenueCat
  - 무료=on-device STT + Edge TTS / 프리미엄=Whisper + OpenAI TTS
  - 초기 고정비 ~$10/월
  - 앱스토어 30% 후 유저당 마진 ~$2-3
  - 2주 MVP → 1개월 수익 검증, 피벗 기준 명시
  - ASO 키워드, 리뷰 가이드라인 포함
- **전략**: 가볍고 빠르게 치고 빠지기 — 지니님 명시
- **다음**: 지니님 PRD 검토 후 개발 시작

## BOTALKS 구현 진행 (오후)

### Sprint 1 — 팀 에이전트 3개로 병렬 구현 완료

- **아키텍트**: `D-technical-design.md` (미들웨어 서버 구조, 오디오 파이프라인, Auth 설계)
- **PM**: `D-development-plan.md` (2주 스프린트, 20개 태스크)
- **개발자**: Expo SDK 54 프로젝트 초기화 + 전체 구조 세팅

### 구현팀 3개 — 코어 기능 구현

- **UI팀**: 채팅화면, MicButton(PTT+펄스 애니메이션), 설정, 온보딩, 구독 화면
- **오디오팀**: expo-av 녹음, STT(Whisper+on-device), TTS(Edge+OpenAI), SSE API 클라이언트, useVoiceChat 훅
- **상태팀**: Zustand 스토어 5개 (chat, auth, subscription, settings + barrel), AsyncStorage 영속화
- **통합 에이전트**: 3팀 코드 머지, TypeScript 0 에러, git push 완료

### Sprint 2 — Week 2 태스크

- **폴리시팀**: 햅틱, 애니메이션, 에러 토스트, 스켈레톤 로더, 타이핑 인디케이터
- **미들웨어팀**: Hono 프록시 서버 (Chat/STT/TTS 프록시, Auth, rate limit) `server/`
- **설정팀**: app.config.ts, EAS 빌드설정, 개인정보처리방침(한/영), 이용약관, README

### 인프라 설정

- Gateway `chatCompletions` API 엔드포인트 활성화
- `server/.env` 생성 (Gateway 토큰 + OpenAI API 키)
- OpenAI API 키 교체 (구 키 노출 → 새 키 발급)
- ffmpeg PATH 등록 (winget 설치 경로 → User PATH)
- Discord voice message 소스 수정 빌드 완료 (asVoiceEarly fix)

### 실기기 테스트 진행 중

- Expo Go 터널 모드로 iPhone 접속 성공
- 온보딩 3단계 정상 표시 확인!
- **크래시 이슈 수정 중**:
  - `expo-haptics` import → optional require로 변경
  - Zustand `getSnapshot` 무한루프 → 안정적 셀렉터(컴포넌트 외부 정의)로 수정
  - `selectMessages`의 `?? []` → 상수 빈 배열로 캐싱
  - 아직 완전히 해결 안 됨 — 추가 디버깅 필요

### OneDrive 심볼릭 링크 문제 해결

- `11.MAIBOTALKS/docs` 심볼릭 링크 → `fsutil reparsepoint delete` + robocopy 실제 파일 복사
- `09.MAICON/docs`는 이미 정상

### GitHub: https://github.com/jini92/MAIBOTALKS

- 총 53개 파일, 10+ 커밋

## 교훈

- 지니님 말을 더 잘 들을 것 — "업데이트 후 해야하는거죠?" → 맞았음
- 데일리 노트를 먼저 확인할 것 — 이미 정보가 정리되어 있었음
- 지니님 요청사항(모바일앱)을 서브에이전트 투입 전에 반영할 것
- Zustand 셀렉터는 반드시 컴포넌트 외부에 정의 (인라인 → 무한루프)
- `?? []`같은 폴백은 상수로 캐싱해야 React 무한 리렌더 방지
- 팀 에이전트 병렬 작업 시 타입/config 파일 충돌 주의
