# 2026-04-07

- Mert yeni bir davranış kuralı istedi ve netleştirdi: ACP/subagent işleri verildikten sonra ara durum istenmiyorsa bağlantıyı/turn'ü açık tutup işlerin bitmesini beklemem, sonuçları tek final mesajda vermem ve ancak sonra kapanmam gerekiyor.
- Mevcut davranışta bunu birkaç kez ihlal ettim; kullanıcı özellikle erken kapanma/final bildiriminin kaçması sorununu vurguladı.
- Mert bu sorunun sadece davranışsal değil, gateway tarafında altyapısal olarak da ele alınmasını istedi; artık yapısal çözüm yönüne geçtik.
- Bu doğrultuda `gateway-followup-reliability-notes.md` oluşturuldu. Temel bulgu: child completion ve WhatsApp reconnect/disconnect gibi olaylar sistemde bilinse de final kullanıcı bildirimi yeterince durable/parent-owned değil.
- Doğrulanan teknik nokta: WhatsApp gateway connect/disconnect olayları system event olarak enqueue ediliyor; system event'ler ephemeral ve genelde ancak sonraki reply run sırasında prompt'a ekleniyor, bu yüzden garanti kullanıcı bildirimi sağlamıyor.
- Önerilen mimari yön netleşti: ephemeral system events yerine durable, parent-owned `pending completion delivery` / follow-up obligation kuyruğu gerekiyor; child run bitince parent session için kalıcı final delivery kaydı oluşmalı ve reconnect/uygun ilk anda retry ile kullanıcıya ulaştırılmalı.
- Ürün/çalışma yönü de netleşti: SketchUp PoC ve Watch Ceviz iki eşit ağırlıklı track olarak paralel yürütülüyor; user bu dengeyi özellikle istedi.
- Mert artık gateway follow-up reliability için sadece plan değil, doğrudan plan + implementasyon başlamamı ve bunun için ayrıca onay beklemememi söyledi.
- Bu doğrultuda gerçek source checkout üzerinde ilk patch başlatıldı: `/tmp/openclaw-src/src/agents/subagent-final-delivery.ts` eklendi ve `/tmp/openclaw-src/src/agents/subagent-registry-lifecycle.ts` içinde child completion sonrası parent-owned pending final delivery akışına ilk entegrasyon yapıldı.
- Patch'in amacı: child iş bitince ephemeral system event'e güvenmek yerine requester session için `pending final delivery` kaydı oluşturmak; doğrudan deliver mümkünse göndermek, değilse session queue + heartbeat wake ile follow-up zorlamak.
- Kaynak ağaçta test çalıştırma denemesi yapıldı ama environment eksik çıktı; `vitest/package.json` bulunamadığı için doğrulama o noktada bloklandı.
- Agent dağıtımı tarafında boşta kalan Gemini'ye yeni Watch işi verildi: `watch-ceviz-next-after-monitoring-2` (monitoring'den sonraki ince intervention/write slice: job detail / summarize progress / cancel stub yönü).
