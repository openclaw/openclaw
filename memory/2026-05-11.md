# Memory — 2026-05-11

## Ada 实例状态 / memorySearch 检查受限
- 验证 Ada Gateway 在线：`http://127.0.0.1:18790/` 返回 `200 OK`。
- 监听进程为 `openclaw-team` 用户下的 `openclaw`，当时 PID `22176`；监听地址包含 `127.0.0.1:18790` 和 `::1:18790`。
- 但无法直接确认 Ada 是否启用了 `bge-m3` 记忆检索：
  - 读取 `/Users/openclaw-team/.openclaw/openclaw.json` 遇到权限拒绝
  - `launchctl print gui/.../ai.openclaw.gateway` 也无权限
- 结论：Ada 实例在线，但 `memorySearch` 配置是否启用 `bge-m3` 仍未确认，需要带权限读取后再查。

## 大麦抢票请求的处理边界
- 明确拒绝帮助构建或改造自动抢票/自动下单/绕过验证脚本，即使是自用场景也不提供此类自动化。
- 用户给出的相关仓库：`https://github.com/Guyungy/damaihelper`。
- 可接受替代方案只限于合规辅助：开票提醒、打开活动页、检查实名/票档/支付准备、人工操作提示，不做自动点击、自动提交或自动支付。

## X 推文每日发布 cron 恢复并加发布前门禁
- 已恢复 cron `efd191f5-488f-4990-9cf3-fed54c47c632`（`X推文每日发布`），当前 `enabled=true`。
- 调度为 `0 9,12,15,18 * * *`（Asia/Shanghai）。
- payload 改为读取绝对路径：`/Users/geyunfei/.openclaw/skills/x-daily/SKILL.md`，避免相对路径失效。
- 发布前新增两道门禁：
  1. 去 AI 味检查：扫描禁用表达、八股总结、过度工整、空洞升华，命中则定向改写，不通过不发。
  2. 主题完整性检查：确认钩子、证据、立场、收口/互动、配图齐全，且删掉末句后主题仍完整。
- 检查时该任务最近一次运行状态为 `ok`，下一次触发是次日白天档位。
