# 2026-03-30

- Mert ile Windows base OS üzerinde çalışan bir executor agent + Ceviz orchestrator modeli netleştirildi.
- Başta ACP/Codex delegasyonu çalışmıyordu; kök neden `acpx` runtime plugin'inin disabled olmasıydı. Plugin enable edildi, ilgili config ayarları yapıldı ve gateway restart sonrası ACP spawn çalışır hale geldi.
- İlk discovery denemesi bağlı Codex ajanının yalnızca WSL/Linux ortamını gördüğünü gösterdi. Daha sonra TOOLS.md içine Windows host access köprüleri eklendi:
  - `pwsh` wrapper
  - `dotnet` wrapper
  - `browser-launch`
  - `/mnt/c` vb. Windows filesystem erişimi
- Sonraki teşhislerde kritik ayrım bulundu:
  - sandboxed ACP lane içinde Windows binary launch denemeleri WSL/vsock hataları verebiliyor
  - escalated / unsandboxed lane içinde aynı Windows binary'ler gerçekten çalışıyor
- Bu ayrım kullanıcı gözlemiyle doğrulandı: Windows desktop'ta browser açılması gibi side effect'ler görüldü.
- Reconciliation aşamasında doğrulanmış Windows-side marker dosyaları üretildi. Sonuç: sorun "Windows interop tamamen bozuk" değil; sorun kullanılan execution lane.
- Çıkarılan operasyonel politika:
  - sandboxed lane = Linux/WSL planning, workspace işleri, rapor yazımı
  - escalated Windows-capable lane = `cmd.exe`, `pwsh.exe`, `dotnet.exe`, browser launch, Windows-side artifact üretimi
  - approval-heavy Windows görevleri unattended kullanım için riskli olabilir
- Bootstrap Phase 1 tamamlandı:
  - `windows-bridge-bootstrap.md`
  - `windows-bridge-bootstrap/` klasörü
  - probe notları
  - `scripts/win-capability-probe.ps1`
  - future bridge direction notu
- Phase 2 başlangıcı tamamlandı:
  - `windows-bridge-phase2.md`
  - Windows capability probe script'i Windows PowerShell 7 üzerinden gerçekten çalıştırıldı
  - doğrulanmış JSON artifact üretildi:
    - Windows path: `C:\Users\mertb\Desktop\windows-bridge-capability-probe-20260330-0117.json`
    - WSL path: `/mnt/c/Users/mertb/Desktop/windows-bridge-capability-probe-20260330-0117.json`
  - doğrulanan içerik:
    - computerName: `KOLTIGINMASAUST`
    - pwshVersion: `7.6.0`
    - dotnetOk: `true`
    - dotnetVersion: `10.0.103`
  - queue skeleton oluşturuldu:
    - `windows-bridge-bootstrap/queue/inbound`
    - `windows-bridge-bootstrap/queue/outbound`
    - `windows-bridge-bootstrap/queue/archive`
  - example request ve helper consumption note yazıldı
- Phase 3 tamamlandı:
  - `windows-helper/runner.ps1` eklendi
  - allowlist handler routing kuruldu
  - `capability-probe` ve `dotnet-info` handler'ları eklendi
  - gerçek RunOnce testi geçti; outbound/archive/artifact zinciri doğrulandı
  - iki düzeltme yapıldı:
    - tek dosya durumunda PowerShell collection Count hatası düzeltildi
    - QueueRoot path çözümlemesi absolute/relative durumda düzeltildi
- Phase 4 tamamlandı:
  - `scripts/enqueue-bridge-request.py` eklendi
  - WSL tarafında request üretme ve opsiyonel wait desteği geldi
- Phase 5 tamamlandı:
  - `scripts/run-bridge-request.py` eklendi
  - tek komutluk enqueue + RunOnce + wait akışı doğrulandı
  - gerçek dotnet-info Phase 5 testi başarıyla geçti
- Phase 6 başlangıcında Outlook COM tabanlı `outlook-job-signal-scan` handler denendi ama başarısız oldu:
  - hata: `REGDB_E_CLASSNOTREG (0x80040154)`
  - sonuç: bu makinede ana yol olarak Outlook COM yerine Microsoft Graph daha mantıklı
- Graph yönüne geçildi:
  - `graph-auth-status`, `graph-auth-login`, `graph-mail-job-signal-scan` handler'ları yazıldı
  - `Microsoft.Graph.Authentication` modülü Windows tarafında kuruldu (`2.36.1`)
- Önemli bulgu: Microsoft tarafında izin/onay verilmiş gibi görünmesine rağmen `Connect-MgGraph` delegated auth akışı bu ortamda tutarsız; tekrar eden timeout'lar nedeniyle güvenilir mailbox scan elde edilemedi.
- Mert, delegasyon/çok adımlı işlerde ara durum mesajları yerine sonucu bekleyip tek mesajda dönmemi özellikle istiyor; bundan sapmamam gerekiyor.
