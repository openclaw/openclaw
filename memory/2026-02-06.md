---
type: session-log
project: MAIBEAUTY
date: 2026-02-06
tags: [settings, testing]
---

# 2026-02-06 세션 기록

## MAIBEAUTY Settings 전체 연동 테스트 + 설정 완료

### 수행 작업

1. **n8n 연동 테스트** — n8n Cloud (`mai-n8n.app.n8n.cloud`) healthz OK, 워크플로우 5개 활성 확인, MAIBEAUTY API 시뮬레이션 (신규 리드 조회 → 상태 업데이트) 성공

2. **카카오톡 알림 서비스 테스트** — 텍스트(80ms)/피드(242ms)/커머스(244ms) 3/3 성공, 만료 토큰 자동 갱신 (Refresh Token) 정상 확인

3. **Settings 페이지 전체 연동 테스트 (15개 항목)** — 14/15 PASS (Ollama만 미실행이었음)
   - 인증(JWT 발급/Refresh), Google Sheets, n8n Cloud, Leads CRUD, Dashboard Stats, Contents, Campaigns, Pricing(5개 상품), Alerts, Copy Templates, AI Copy 생성, Frontend 7개 라우트 모두 정상

4. **Google Sheets Railway 연동 완료**
   - Railway 환경변수: `CRM_SPREADSHEET_ID` + `GOOGLE_SERVICE_ACCOUNT_JSON_B64` 설정
   - **PEM 파싱 문제 해결**: Railway 환경변수에 JSON 전달 시 `\n`이 실제 줄바꿈으로 해석되어 파싱 실패 → **Base64 인코딩**으로 해결
   - `sheets_sync.py`에 B64 디코딩 로직 추가 (`ac9bdc7`)
   - Push 11건 / Pull 성공 (양방향 동기화 검증 완료)
   - Node.js 유틸리티 스크립트 3종 추가 (`scripts/set-railway-env*.mjs`, `check-railway-env.mjs`)

5. **Ollama 연동 구현** (`10bd900`)
   - `services/ollama.py` — Ollama REST API 클라이언트 (health check, 모델 목록, AI 카피 생성)
   - `routers/ollama.py` — `GET /ollama/status` + `POST /ollama/test`
   - `/copy/generate` 업그레이드: **Ollama AI 우선 → 템플릿 폴백** 자동 전환
   - `OllamaCard` 프론트엔드 컴포넌트 — 실시간 상태, 모델 4개 표시, 생성 테스트 버튼
   - 로컬: qwen3:8b로 베트남어 마케팅 카피 3변형 AI 생성 성공
   - Railway: 로컬 Ollama 미접근 → template-fallback 자동 전환 정상

6. **문서화**
   - `docs/I022-Settings-Integration-Test.md` — Settings 전체 연동 테스트 리포트
   - `docs/STATUS-Development-Overview.md` — 인프라/테스트/일정/문서목록 업데이트
   - `memory/vietnam-beauty.md` — 2026-02-06 진행기록 추가

### 커밋 이력

| SHA       | 메시지                                                            |
| --------- | ----------------------------------------------------------------- |
| `ac9bdc7` | fix: support base64 Google SA JSON env var for Railway deployment |
| `bb10ef2` | docs: add I022 settings integration test report + update STATUS   |
| `10bd900` | feat: integrate Ollama LLM for AI copy generation and settings UI |

### Railway 환경변수 (신규 추가)

- `CRM_SPREADSHEET_ID` = `1r_CSTQDPdZtiPTqPdXjYvpRmtZe0oH6cGoMaoboBFO4`
- `GOOGLE_SERVICE_ACCOUNT_JSON_B64` = (Base64 인코딩된 SA JSON)

### 기술 메모

- Railway 환경변수에 JSON 전달 시 `\n` 이스케이프 문제: GraphQL API/CLI 모두 `\n`을 실제 줄바꿈으로 변환함 → **Base64가 유일한 안전한 방법**
- PowerShell에서 Railway CLI의 `--set` 옵션은 private key 공백으로 파싱 실패 → Node.js GraphQL 직접 호출로 해결
- Ollama는 로컬 GPU 전용이므로 Railway에서는 항상 template-fallback 사용

### 현재 Settings 연동 상태 (15/15)

- ✅ Auth (JWT + Refresh)
- ✅ Google Sheets 양방향 동기화
- ✅ n8n Cloud (5 워크플로우)
- ✅ Ollama (로컬 AI / 서버 폴백)
- ✅ 카카오톡 알림 (3 템플릿)
- ✅ Leads CRUD + Stats + Contents + Campaigns + Pricing + Alerts + Copy
- ✅ Frontend 7개 라우트
- ⏳ Zalo OA (베트남 +84 번호 확보 후)
