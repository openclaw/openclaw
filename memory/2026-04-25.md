## 03:36 TUI startup summary auto-bind

- Mert onayıyla TUI startup summary için display katmanından sonra auto-bind katmanı da eklendi.
- Yeni dosya: `openclaw-src/src/cli/tui-startup-summary.ts`
  - Varsayılan kaynak olarak `memory/2026-04-25-mobile-access-and-upstream-plan.md` okunuyor.
  - `## Konuşma özeti` bölümü çıkarılıp startup summary metni olarak çözülüyor.
- `openclaw-src/src/cli/tui-cli.ts` güncellendi:
  - `runTui(...)` öncesi summary çözülüyor.
  - Sonuç varsa `process.env.OPENCLAW_TUI_STARTUP_SUMMARY` set ediliyor; yoksa temizleniyor.
- Önceden eklenen `src/tui/tui.ts` tarafı bu env’yi okuyup history yüklendikten sonra chat log’da görünür startup summary bloğu basıyor.
- Ek test dosyası: `openclaw-src/src/cli/tui-startup-summary.test.ts`
- `openclaw-src/src/cli/program.smoke.test.ts` içine env yükleme davranışı için smoke assertion eklendi.
- `pnpm build` başarılı geçti.
- Dikkat: `openclaw-src` çalışma ağacında bu iş dışında önceden var olan başka değişiklikler de bulunduğu için bu turda commit/push yapılmadı; değişiklikler working tree’de kaldı.
