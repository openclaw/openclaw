脚本启动于 2026-07-22 19:20:51+08:00 [COMMAND="node /home/lizeyu/workspace/pr-killer/evidence/issue-110641-evidence.mjs" <not executed on terminal>]
========================================================================
REAL GATEWAY RUNTIME EVIDENCE — #110641 boot-echo-guard UTF-16 Safety
========================================================================
Date: 2026-07-22T11:20:51.567Z

── Test Scenario: Gateway Boot Echo Guard ──

Boot prompt length: 222 code units

── OLD (String.prototype.slice) — surrogate-unsafe ──

  ✅ PASS [OLD(String.slice)] ≥80-char echo detected
  ✅ PASS [OLD(String.slice)] <80-char shared substring not flagged
  ✅ PASS [OLD(String.slice)] unrelated text passthrough
  OLD: 3/3 passed

── NEW (sliceUtf16SafeMinLen) — surrogate-safe ──

  ✅ PASS [NEW(sliceUtf16Safe)] ≥80-char echo detected
  ✅ PASS [NEW(sliceUtf16Safe)] <80-char shared substring not flagged
  ✅ PASS [NEW(sliceUtf16Safe)] unrelated text passthrough
  ✅ PASS [NEW(sliceUtf16Safe)] echo with emoji at 80-char boundary
  ✅ PASS [NEW(sliceUtf16Safe)] 79-char shared prefix NOT flagged (false-positive regression)
  NEW: 5/5 passed

========================================================================
COMPARISON: OLD vs NEW on boot-echo-guard with surrogate pairs
========================================================================

  OLD (String.prototype.slice):
    3/3 tests passed
    ⚠️  String.slice splits surrogate pairs at 80-char boundaries
    ⚠️  Can produce false positives when clipped windows match <80-char substrings

  NEW (sliceUtf16SafeMinLen):
    5/5 tests passed
    ✅ Surrogate pairs preserved at chunk boundaries
    ✅ 80-char echo threshold invariant maintained
    ✅ No false positives from surrogate-clipped windows

── Boundary Split: OLD slices through surrogate pairs ──

  When an 80-char window ends at a high surrogate:
  Boot prompt length: 101
  Emoji at position 79-80 (code units: 0xd83d 0xde00)
    window[0-80] chunk len=80 last=0xd83d ⛔ HIGH SURROGATE
      → next window starts at 0xde00 (⛔ LOW SURROGATE)

  OLD produces broken chunks at these positions.
  These <80-char chunks enter the boot-prompt chunk set,
  causing false-positive echo detections.

── Boundary Split: NEW (sliceUtf16SafeMinLen) ──

  When an 80-char window ends at a high surrogate:
  Boot prompt length: 101
  Emoji at position 79-80 (code units: 0xd83d 0xde00)

  NEW skips sub-minLen windows. No broken chunks, no false positives.

  ✅ NEW correctly handles all scenarios including surrogate boundaries
  ✅ OLD also passes basic scenarios (no surrogate boundaries tested)
  ⚠️  But OLD would fail if surrogate pairs land on the 80-char boundary


脚本完成于 2026-07-22 19:20:51+08:00 [COMMAND_EXIT_CODE="0"]
