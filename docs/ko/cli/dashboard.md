---
summary: "`openclaw dashboard` (Control UI 열기)에 대한 CLI 참조"
read_when:
  - 현재 토큰으로 Control UI 를 열고자 할 때
  - 브라우저를 실행하지 않고 URL 을 출력하고자 할 때
title: "대시보드"
---

# `openclaw dashboard`

현재 인증을 사용하여 Control UI 를 엽니다.

```bash
openclaw dashboard
openclaw dashboard --no-open
```
