---
read_when:
    - 브로드캐스트 그룹 구성
    - WhatsApp에서 다중 에이전트 응답 디버깅
status: experimental
summary: 여러 상담원에게 WhatsApp 메시지 브로드캐스트
title: 방송 그룹
x-i18n:
    generated_at: "2026-02-08T15:48:09Z"
    model: gtx
    provider: google-translate
    source_hash: 25866bc0d519552d601ae27e5c3c97816f220f05de210e609f8b9f3712fababf
    source_path: channels/broadcast-groups.md
    workflow: 15
---

# 방송 그룹

**상태:** 실험적  
**버전:** 2026.1.9에 추가됨

## 개요

브로드캐스트 그룹을 사용하면 여러 에이전트가 동일한 메시지를 동시에 처리하고 응답할 수 있습니다. 이를 통해 하나의 전화번호를 사용하여 단일 WhatsApp 그룹 또는 DM에서 함께 작업하는 전문 상담원 팀을 만들 수 있습니다.

현재 범위: **WhatsApp 전용** (웹 채널).

브로드캐스트 그룹은 채널 허용 목록 및 그룹 활성화 규칙 후에 평가됩니다. WhatsApp 그룹에서 이는 OpenClaw가 일반적으로 응답할 때 브로드캐스트가 발생함을 의미합니다(예: 그룹 설정에 따라 언급 시).

## 사용 사례

### 1. 전문 에이전트 팀

원자적이고 집중적인 책임을 지닌 여러 에이전트를 배포합니다.

```
Group: "Development Team"
Agents:
  - CodeReviewer (reviews code snippets)
  - DocumentationBot (generates docs)
  - SecurityAuditor (checks for vulnerabilities)
  - TestGenerator (suggests test cases)
```

각 에이전트는 동일한 메시지를 처리하고 전문적인 관점을 제공합니다.

### 2. 다국어 지원

```
Group: "International Support"
Agents:
  - Agent_EN (responds in English)
  - Agent_DE (responds in German)
  - Agent_ES (responds in Spanish)
```

### 3. 품질 보증 워크플로

```
Group: "Customer Support"
Agents:
  - SupportAgent (provides answer)
  - QAAgent (reviews quality, only responds if issues found)
```

### 4. 작업 자동화

```
Group: "Project Management"
Agents:
  - TaskTracker (updates task database)
  - TimeLogger (logs time spent)
  - ReportGenerator (creates summaries)
```

## 구성

### 기본 설정

최상위 수준 추가 `broadcast` 섹션(옆에 `bindings`). 키는 WhatsApp 피어 ID입니다.

- 그룹 채팅: 그룹 JID(예: `120363403215116621@g.us`)
- DM: E.164 전화번호(예: `+15551234567`)

```json
{
  "broadcast": {
    "120363403215116621@g.us": ["alfred", "baerbel", "assistant3"]
  }
}
```

**결과:** OpenClaw가 이 채팅에 응답하면 세 에이전트가 모두 실행됩니다.

### 처리 전략

에이전트가 메시지를 처리하는 방법을 제어합니다.

#### 병렬(기본값)

모든 에이전트가 동시에 처리:

```json
{
  "broadcast": {
    "strategy": "parallel",
    "120363403215116621@g.us": ["alfred", "baerbel"]
  }
}
```

#### 잇달아 일어나는

에이전트는 순서대로 처리합니다(하나는 이전 작업이 완료될 때까지 기다림).

```json
{
  "broadcast": {
    "strategy": "sequential",
    "120363403215116621@g.us": ["alfred", "baerbel"]
  }
}
```

### 완전한 예

```json
{
  "agents": {
    "list": [
      {
        "id": "code-reviewer",
        "name": "Code Reviewer",
        "workspace": "/path/to/code-reviewer",
        "sandbox": { "mode": "all" }
      },
      {
        "id": "security-auditor",
        "name": "Security Auditor",
        "workspace": "/path/to/security-auditor",
        "sandbox": { "mode": "all" }
      },
      {
        "id": "docs-generator",
        "name": "Documentation Generator",
        "workspace": "/path/to/docs-generator",
        "sandbox": { "mode": "all" }
      }
    ]
  },
  "broadcast": {
    "strategy": "parallel",
    "120363403215116621@g.us": ["code-reviewer", "security-auditor", "docs-generator"],
    "120363424282127706@g.us": ["support-en", "support-de"],
    "+15555550123": ["assistant", "logger"]
  }
}
```

## 작동 방식

### 메시지 흐름

1. **수신 메시지** WhatsApp 그룹에 도착
2. **방송체크**: 시스템은 Peer ID가 있는지 확인합니다. `broadcast`
3. **방송 목록에 있는 경우**:
   - 나열된 모든 에이전트가 메시지를 처리합니다.
   - 각 에이전트에는 자체 세션 키와 격리된 컨텍스트가 있습니다.
   - 에이전트는 병렬(기본값) 또는 순차적으로 처리합니다.
4. **방송 목록에 없는 경우**:
   - 일반 라우팅 적용(첫 번째 일치하는 바인딩)

참고: 브로드캐스트 그룹은 채널 허용 목록이나 그룹 활성화 규칙(멘션/명령/등)을 우회하지 않습니다. 그들은 변할 뿐이야 _어떤 에이전트가 실행되는지_ 메시지가 처리에 적합한 경우.

### 세션 격리

브로드캐스트 그룹의 각 에이전트는 다음을 완전히 별도로 유지합니다.

- **세션 키** (`agent:alfred:whatsapp:group:120363...` 대 `agent:baerbel:whatsapp:group:120363...`)
- **대화 기록** (상담원은 다른 상담원의 메시지를 볼 수 없습니다)
- **작업공간** (구성된 경우 별도의 샌드박스)
- **도구 액세스** (다른 허용/거부 목록)
- **메모리/컨텍스트** (IDENTITY.md, SOUL.md 등 별도)
- **그룹 컨텍스트 버퍼** (컨텍스트에 사용된 최근 그룹 메시지)는 피어별로 공유되므로 모든 브로드캐스트 에이전트가 트리거될 때 동일한 컨텍스트를 볼 수 있습니다.

이를 통해 각 에이전트는 다음을 수행할 수 있습니다.

- 다양한 성격
- 다양한 도구 액세스(예: 읽기 전용 및 읽기-쓰기)
- 다양한 모델(예: Opus 대 Sonnet)
- 다양한 스킬이 설치되어 있습니다.

### 예: 격리된 세션

그룹 내 `120363403215116621@g.us` 에이전트와 함께 `["alfred", "baerbel"]`:

**알프레드의 상황:**

```
Session: agent:alfred:whatsapp:group:120363403215116621@g.us
History: [user message, alfred's previous responses]
Workspace: /Users/pascal/openclaw-alfred/
Tools: read, write, exec
```

**Bärbel의 맥락:**

```
Session: agent:baerbel:whatsapp:group:120363403215116621@g.us
History: [user message, baerbel's previous responses]
Workspace: /Users/pascal/openclaw-baerbel/
Tools: read only
```

## 모범 사례

### 1. 상담원의 집중력 유지

각 에이전트를 단일하고 명확한 책임으로 설계합니다.

```json
{
  "broadcast": {
    "DEV_GROUP": ["formatter", "linter", "tester"]
  }
}
```

✅ **좋은:** 각 에이전트에는 하나의 작업이 있습니다.  
❌ **나쁜:** 하나의 일반 "개발 도우미" 에이전트

### 2. 설명적인 이름을 사용하세요

각 에이전트가 수행하는 작업을 명확하게 합니다.

```json
{
  "agents": {
    "security-scanner": { "name": "Security Scanner" },
    "code-formatter": { "name": "Code Formatter" },
    "test-generator": { "name": "Test Generator" }
  }
}
```

### 3. 다양한 도구 액세스 구성

상담원에게 필요한 도구만 제공하세요.

```json
{
  "agents": {
    "reviewer": {
      "tools": { "allow": ["read", "exec"] } // Read-only
    },
    "fixer": {
      "tools": { "allow": ["read", "write", "edit", "exec"] } // Read-write
    }
  }
}
```

### 4. 성능 모니터링

상담원이 많은 경우 다음을 고려하세요.

- 사용 `"strategy": "parallel"` (기본값) 속도
- 브로드캐스트 그룹을 5~10명의 에이전트로 제한
- 더 간단한 에이전트를 위해 더 빠른 모델 사용

### 5. 실패를 우아하게 처리하기

에이전트는 독립적으로 실패합니다. 한 에이전트의 오류로 인해 다른 에이전트가 차단되지는 않습니다.

```
Message → [Agent A ✓, Agent B ✗ error, Agent C ✓]
Result: Agent A and C respond, Agent B logs error
```

## 호환성

### 공급자

브로드캐스트 그룹은 현재 다음과 함께 작업합니다.

- ✅ WhatsApp(구현됨)
- 🚧 텔레그램 (예정)
- 🚧 디스코드(예정)
- 🚧 Slack (예정)

### 라우팅

브로드캐스트 그룹은 기존 라우팅과 함께 작동합니다.

```json
{
  "bindings": [
    {
      "match": { "channel": "whatsapp", "peer": { "kind": "group", "id": "GROUP_A" } },
      "agentId": "alfred"
    }
  ],
  "broadcast": {
    "GROUP_B": ["agent1", "agent2"]
  }
}
```

- `GROUP_A`: alfred만 응답함(일반 라우팅)
- `GROUP_B`: 에이전트1 및 에이전트2 응답(브로드캐스트)

**상위:** `broadcast` 우선한다 `bindings`.

## 문제 해결

### 상담원이 응답하지 않음

**확인하다:**

1. 에이전트 ID가 다음 위치에 있습니다. `agents.list`
2. 피어 ID 형식이 정확합니다(예: `120363403215116621@g.us`)
3. 에이전트가 거부 목록에 없습니다.

**디버그:**

```bash
tail -f ~/.openclaw/logs/gateway.log | grep broadcast
```

### 상담원 한 명만 응답

**원인:** 피어 ID가 있을 수 있음 `bindings` 하지만 그렇지 않다 `broadcast`.

**고치다:** 브로드캐스트 구성에 추가하거나 바인딩에서 제거합니다.

### 성능 문제

**에이전트가 많아 속도가 느린 경우:**

- 그룹당 상담원 수 줄이기
- 더 가벼운 모델 사용(opus 대신 sonnet)
- 샌드박스 시작 시간 확인

## 예

### 예 1: 코드 검토 팀

```json
{
  "broadcast": {
    "strategy": "parallel",
    "120363403215116621@g.us": [
      "code-formatter",
      "security-scanner",
      "test-coverage",
      "docs-checker"
    ]
  },
  "agents": {
    "list": [
      {
        "id": "code-formatter",
        "workspace": "~/agents/formatter",
        "tools": { "allow": ["read", "write"] }
      },
      {
        "id": "security-scanner",
        "workspace": "~/agents/security",
        "tools": { "allow": ["read", "exec"] }
      },
      {
        "id": "test-coverage",
        "workspace": "~/agents/testing",
        "tools": { "allow": ["read", "exec"] }
      },
      { "id": "docs-checker", "workspace": "~/agents/docs", "tools": { "allow": ["read"] } }
    ]
  }
}
```

**사용자는 다음을 보냅니다:** 코드 조각  
**응답:**

- code-formatter: "고정된 들여쓰기 및 추가된 유형 힌트"
- security-scanner: "⚠️ 12번째 줄의 SQL 주입 취약점"
- test-coverage: "커버리지는 45%이며, 오류 사례에 대한 테스트가 누락되었습니다."
- docs-checker: "함수에 대한 문서 문자열이 누락되었습니다. `process_data`"

### 예 2: 다국어 지원

```json
{
  "broadcast": {
    "strategy": "sequential",
    "+15555550123": ["detect-language", "translator-en", "translator-de"]
  },
  "agents": {
    "list": [
      { "id": "detect-language", "workspace": "~/agents/lang-detect" },
      { "id": "translator-en", "workspace": "~/agents/translate-en" },
      { "id": "translator-de", "workspace": "~/agents/translate-de" }
    ]
  }
}
```

## API 참조

### 구성 스키마

```typescript
interface OpenClawConfig {
  broadcast?: {
    strategy?: "parallel" | "sequential";
    [peerId: string]: string[];
  };
}
```

### 전지

- `strategy` (선택): 에이전트 처리 방법
  - `"parallel"` (기본값): 모든 에이전트가 동시에 처리합니다.
  - `"sequential"`: 에이전트는 배열 순서로 처리합니다.
- `[peerId]`: WhatsApp 그룹 JID, E.164 번호 또는 기타 피어 ID
  - 값: 메시지를 처리해야 하는 에이전트 ID 배열

## 제한사항

1. **최대 상담원 수:** 엄격한 제한은 없지만 에이전트가 10개 이상이면 속도가 느릴 수 있습니다.
2. **공유 컨텍스트:** 상담원은 (설계상) 서로의 응답을 볼 수 없습니다.
3. **메시지 순서:** 병렬 응답은 어떤 순서로든 도착할 수 있습니다.
4. **비율 제한:** 모든 상담원은 WhatsApp 비율 제한에 포함됩니다.

## 향후 개선 사항

계획된 기능:

- [ ] 공유 컨텍스트 모드(에이전트가 서로의 응답을 볼 수 있음)
- [ ] 에이전트 조정(에이전트가 서로 신호를 보낼 수 있음)
- [ ] 동적 에이전트 선택(메시지 내용에 따라 에이전트 선택)
- [ ] 상담원 우선순위(일부 상담원이 다른 상담원보다 먼저 응답함)

## 참조

- [다중 에이전트 구성](/tools/multi-agent-sandbox-tools)
- [라우팅 구성](/channels/channel-routing)
- [세션 관리](/concepts/sessions)
