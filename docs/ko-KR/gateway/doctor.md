---
summary: "doctor 명령어로 시스템 진단"
read_when:
  - 시스템 문제를 진단할 때
title: "Doctor"
---

# Doctor

`openclaw doctor`는 시스템 상태를 진단하고 문제를 발견합니다.

## 기본 사용

```bash
openclaw doctor
```

## 출력 예시

```
🔍 OpenClaw Doctor

✅ Node.js 버전: v22.12.0
✅ OpenClaw 버전: 2024.2.0
✅ 설정 파일: 유효함
✅ API 키: Anthropic 설정됨

⚠️ 경고: WhatsApp 세션 만료 예정 (3일 후)

❌ 오류: Telegram 봇 토큰 누락

📊 시스템:
   - CPU: 4코어, 12% 사용
   - 메모리: 8GB, 42% 사용
   - 디스크: 256GB, 65% 사용

💡 권장사항:
   1. Telegram 봇 토큰을 설정하세요
   2. WhatsApp 세션을 갱신하세요
```

## 검사 항목

### 환경

| 항목     | 설명             |
| -------- | ---------------- |
| Node.js  | 버전 및 호환성   |
| npm/pnpm | 패키지 매니저    |
| OS       | 운영 체제 호환성 |
| 메모리   | 가용 메모리      |
| 디스크   | 저장 공간        |

### 설정

| 항목          | 설명             |
| ------------- | ---------------- |
| openclaw.json | 문법 및 유효성   |
| 스키마        | 설정 스키마 준수 |
| API 키        | 키 존재 및 형식  |
| 권한          | 파일 접근 권한   |

### 채널

| 항목     | 설명            |
| -------- | --------------- |
| WhatsApp | 세션 상태, 만료 |
| Telegram | 봇 토큰 유효성  |
| Discord  | 봇 토큰 및 권한 |
| Slack    | 앱/봇 토큰      |

### 네트워크

| 항목      | 설명                |
| --------- | ------------------- |
| 포트      | 포트 사용 가능 여부 |
| Tailscale | 연결 상태           |
| API 연결  | 모델 API 접근성     |

## 옵션

### 상세 모드

```bash
openclaw doctor --verbose
```

### 특정 영역만

```bash
openclaw doctor --check channels
openclaw doctor --check network
openclaw doctor --check config
```

### JSON 출력

```bash
openclaw doctor --json
```

### 자동 수정

```bash
openclaw doctor --fix
```

## 일반적인 문제

### Node.js 버전 낮음

```
❌ Node.js 버전 19.0.0 (최소 22.12.0 필요)
```

**해결**: Node.js 22+ 설치

### API 키 누락

```
❌ ANTHROPIC_API_KEY 누락
```

**해결**: 환경변수 설정

### 포트 사용 중

```
❌ 포트 18789 사용 중
```

**해결**: 다른 포트 사용 또는 충돌 프로세스 종료

### WhatsApp 세션 만료

```
⚠️ WhatsApp 세션 만료 예정
```

**해결**: `openclaw channels login --whatsapp`

## 정기 진단

### 크론으로 자동 실행

```bash
# 매일 진단 실행
0 6 * * * openclaw doctor --json >> ~/openclaw-health.log
```

### 알림 연동

```json5
{
  health: {
    check: {
      schedule: "0 6 * * *",
      notify: {
        onError: true,
        target: {
          channel: "telegram",
          to: "123456789",
        },
      },
    },
  },
}
```
