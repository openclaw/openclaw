# ClawTwin Platform API — moved

**Do not develop Nexus Platform HTTP / FastAPI code here.** This directory is intentionally thin so there is a single backend track.

## Canonical location

- **Repository:** `clawtwin-platform`
- **Package root:** `platform-api/` inside that repo (sibling checkout layout: `../clawtwin-platform/platform-api` next to your `openclaw` checkout).

Run `uvicorn`, `pytest`, and Alembic from that folder per its `README.md`.

## Design docs (unchanged)

Architecture and contracts remain under **`contrib/industrial-oilgas-skills/`** — start with [`DESIGN-FINAL-MASTER-INDEX.md`](../DESIGN-FINAL-MASTER-INDEX.md).
