---
summary: "重定向：flow命令位于`openclaw tasks flow`下"
read_when:
  - 您在旧文档或发行说明中遇到openclaw flows
title: "flows (重定向)"
---

# `openclaw tasks flow`

Flow命令是`openclaw tasks`的子命令，不是独立的`flows`命令。

```bash
openclaw tasks flow list [--json]
openclaw tasks flow show <lookup>
openclaw tasks flow cancel <lookup>
```

完整文档请参阅[Task Flow](/automation/taskflow)和[tasks CLI参考](/cli/index#tasks)。