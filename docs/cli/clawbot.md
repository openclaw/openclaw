---
summary: "CLI reference for `mullusi clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `mullusi clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `mullusi clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `mullusi clawbot qr` (same behavior as [`mullusi qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `mullusi clawbot qr` -> `mullusi qr`
