---
summary: "CLI reference for `smart-agent-neo uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `smart-agent-neo uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
smart-agent-neo uninstall
smart-agent-neo uninstall --all --yes
smart-agent-neo uninstall --dry-run
```
