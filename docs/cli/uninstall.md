---
summary: "CLI reference for `activi uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `activi uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
activi uninstall
activi uninstall --all --yes
activi uninstall --dry-run
```
