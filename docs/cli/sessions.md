---
summary: "CLI reference for `smart-agent-neo sessions` (list stored sessions + usage)"
read_when:
  - You want to list stored sessions and see recent activity
title: "sessions"
---

# `smart-agent-neo sessions`

List stored conversation sessions.

```bash
smart-agent-neo sessions
smart-agent-neo sessions --active 120
smart-agent-neo sessions --json
```
