# Zorg MemoryDB v1.2.55

## DB-first documentation maintenance

This release documents the current public-safe DB-first operating model for Zorg MemoryDB maintenance and recovery.

## Changed

- Added the root-markdown DB-first policy: root workspace markdown files are bootstrap pointers, while long-form rules and durable operating context belong in structured PostgreSQL recall.
- Added public-safe documentation for why Zorg MemoryDB exists, documentation maintenance, and release process expectations.
- Clarified that line-imported root markdown tables are bootstrap and repair surfaces rather than the canonical long-form rule store.

## Verification

- Queried structured DB rules and recent MemoryDB maintenance history before editing.
- Reviewed current `origin/main` in a clean worktree because the live workspace branch is divergent.
- Kept the update public-safe: no private database rows, dumps, transcripts, contacts, credentials, account data, emails, or operator-private context.
