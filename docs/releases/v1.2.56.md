# Zorg MemoryDB v1.2.56

## Docker release workflow repair

This release repairs the GHCR image name used by the Docker release workflow for mixed-case fork repositories.

## Fixed

- Lowercased the Docker image repository path before composing amd64, arm64, manifest, and attestation references.
- Preserved existing tag/version behavior while making the generated GHCR references valid Docker names.

## Verification

- Inspected the failed Docker release workflow logs for `v1.2.55`.
- Parsed `.github/workflows/docker-release.yml` as YAML after the workflow edit.
- Reused the existing docs gate from the preceding documentation release commit.
