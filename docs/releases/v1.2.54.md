# Zorg MemoryDB v1.2.54

## Documentation index maintenance

This release keeps the public documentation surfaces aligned with the current public-safe MemoryDB design after the v1.2.12 through v1.2.53 release catch-up.

## Fixed

- Repaired README LAN console screenshot references so they point at checked-in public-safe image assets.
- Refreshed the release index expectations so released MemoryDB work no longer sits only in transient local context.

## Verification

- Queried DB-backed documentation and release rules before editing.
- Reviewed recent commits and release-note coverage through `docs/releases/v1.2.53.md`.
- Kept the update public-safe: no private database rows, dumps, transcripts, contacts, credentials, account data, emails, or operator-private context.
