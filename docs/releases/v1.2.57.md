# Zorg MemoryDB v1.2.57

## Docker release build repair

This release repairs the Docker image build path after the package lifecycle scripts gained Zorg helper dependencies.

## Fixed

- Copied `zorg/check-node-version.cjs` and `zorg/postinstall-existing-upgrade.cjs` into the Docker dependency-install layer before `pnpm install`.
- Kept the existing package lifecycle scripts intact so Docker builds exercise the same preinstall/postinstall checks as package installs.

## Verification

- Inspected the failed Docker release workflow logs for `v1.2.56`.
- Confirmed the Dockerfile now provides the Zorg lifecycle helper paths before the `pnpm install --frozen-lockfile` layer.
