---
summary: "ایجنٹ رن ٹائم (ایمبیڈڈ pi-mono)، ورک اسپیس کنٹریکٹ، اور سیشن بوٹسٹریپ"
read_when:
  - ایجنٹ رن ٹائم، ورک اسپیس بوٹسٹریپ، یا سیشن کے رویّے میں تبدیلی کرتے وقت
title: "ایجنٹ رن ٹائم"
x-i18n:
  source_path: concepts/agent.md
  source_hash: 121103fda29a5481
  provider: openai
  model: gpt-5.2-chat-latest
  workflow: v1
  generated_at: 2026-02-08T10:47:18Z
---

# ایجنٹ رن ٹائم 🤖

OpenClaw ایک واحد ایمبیڈڈ ایجنٹ رن ٹائم چلاتا ہے جو **pi-mono** سے ماخوذ ہے۔

## Workspace (required)

OpenClaw ایک واحد ایجنٹ ورک اسپیس ڈائریکٹری (`agents.defaults.workspace`) استعمال کرتا ہے جو ٹولز اور سیاق کے لیے ایجنٹ کی **واحد** ورکنگ ڈائریکٹری (`cwd`) ہوتی ہے۔

سفارش کردہ: `openclaw setup` استعمال کریں تاکہ اگر موجود نہ ہو تو `~/.openclaw/openclaw.json` بنایا جائے اور ورک اسپیس فائلیں ابتدائی حالت میں سیٹ کی جائیں۔

مکمل ورک اسپیس لے آؤٹ + بیک اپ گائیڈ: [Agent workspace](/concepts/agent-workspace)

اگر `agents.defaults.sandbox` فعال ہو، تو غیر-مرکزی سیشن اس کو اووررائیڈ کر سکتے ہیں
اور `agents.defaults.sandbox.workspaceRoot` کے تحت فی-سیشن ورک اسپیسز استعمال کی جا سکتی ہیں (دیکھیں
[Gateway configuration](/gateway/configuration))۔

## Bootstrap files (injected)

`agents.defaults.workspace` کے اندر، OpenClaw ان صارف-قابلِ ترمیم فائلوں کی توقع کرتا ہے:

- `AGENTS.md` — عملی ہدایات + “میموری”
- `SOUL.md` — شخصیت، حدود، لہجہ
- `TOOLS.md` — صارف کی جانب سے برقرار رکھی گئی ٹول نوٹس (مثلاً `imsg`, `sag`, روایات)
- `BOOTSTRAP.md` — پہلی بار چلانے کا یک وقتی رسم (مکمل ہونے کے بعد حذف)
- `IDENTITY.md` — ایجنٹ کا نام/وائب/ایموجی
- `USER.md` — صارف پروفائل + ترجیحی مخاطبہ

نئے سیشن کے پہلے ٹرن پر، OpenClaw ان فائلوں کے مندرجات کو براہِ راست ایجنٹ سیاق میں داخل کرتا ہے۔

خالی فائلیں نظرانداز کر دی جاتی ہیں۔ بڑی فائلوں کو تراشا اور مختصر کیا جاتا ہے اور ایک نشان کے ساتھ کاٹا جاتا ہے تاکہ پرامپٹس مختصر رہیں (مکمل مواد کے لیے فائل پڑھیں)۔

اگر کوئی فائل موجود نہ ہو، تو OpenClaw ایک واحد “missing file” مارکر لائن داخل کرتا ہے (اور `openclaw setup` ایک محفوظ ڈیفالٹ ٹیمپلیٹ بنائے گا)۔

`BOOTSTRAP.md` صرف **بالکل نئی ورک اسپیس** کے لیے بنایا جاتا ہے (جب کوئی اور بوٹسٹریپ فائل موجود نہ ہو)۔ اگر رسم مکمل کرنے کے بعد آپ اسے حذف کر دیں، تو بعد کی ری اسٹارٹس پر اسے دوبارہ نہیں بنایا جانا چاہیے۔

بوٹسٹریپ فائل کی تخلیق کو مکمل طور پر غیر فعال کرنے کے لیے (پہلے سے سیڈ کی گئی ورک اسپیسز کے لیے)، یہ سیٹ کریں:

```json5
{ agent: { skipBootstrap: true } }
```

## Built-in tools

بنیادی ٹولز (read/exec/edit/write اور متعلقہ سسٹم ٹولز) ہمیشہ دستیاب ہوتے ہیں،
ٹول پالیسی کے تابع۔ `apply_patch` اختیاری ہے اور
`tools.exec.applyPatch` کے ذریعے محدود ہے۔ `TOOLS.md` یہ کنٹرول نہیں کرتا کہ کون سے ٹولز موجود ہیں؛ یہ اس بات کے لیے رہنمائی ہے کہ _آپ_ انہیں کیسے استعمال کرنا چاہتے ہیں۔

## Skills

OpenClaw تین مقامات سے Skills لوڈ کرتا ہے (نام کے تصادم میں ورک اسپیس کو فوقیت حاصل ہے):

- بنڈلڈ (انسٹال کے ساتھ فراہم کردہ)
- Managed/local: `~/.openclaw/skills`
- Workspace: `<workspace>/skills`

Skills کو کنفیگ/ماحول کے ذریعے محدود کیا جا سکتا ہے (دیکھیں `skills` در [Gateway configuration](/gateway/configuration))۔

## pi-mono integration

OpenClaw، pi-mono کوڈ بیس کے کچھ حصے (ماڈلز/ٹولز) دوبارہ استعمال کرتا ہے، لیکن **سیشن مینجمنٹ، ڈسکوری، اور ٹول وائرنگ OpenClaw کی ملکیت ہیں**۔

- کوئی pi-coding ایجنٹ رن ٹائم نہیں۔
- `~/.pi/agent` یا `<workspace>/.pi` کی سیٹنگز استعمال نہیں کی جاتیں۔

## Sessions

سیشن ٹرانسکرپٹس JSONL کی صورت میں یہاں محفوظ کیے جاتے ہیں:

- `~/.openclaw/agents/<agentId>/sessions/<SessionId>.jsonl`

سیشن ID مستحکم ہوتا ہے اور OpenClaw منتخب کرتا ہے۔
Legacy Pi/Tau سیشن فولڈرز **نہیں** پڑھے جاتے۔

## Steering while streaming

جب queue موڈ `steer` ہو، تو آنے والے پیغامات موجودہ رن میں داخل کیے جاتے ہیں۔
قطار کو **ہر ٹول کال کے بعد** چیک کیا جاتا ہے؛ اگر قطار میں پیغام موجود ہو،
تو موجودہ اسسٹنٹ پیغام کی باقی ٹول کالز چھوڑ دی جاتی ہیں (غلطی ٹول نتائج کے ساتھ "Skipped due to queued user message.")، پھر اگلے اسسٹنٹ جواب سے پہلے قطار میں موجود صارف پیغام داخل کیا جاتا ہے۔

جب queue موڈ `followup` یا `collect` ہو، تو آنے والے پیغامات اس وقت تک روکے جاتے ہیں جب تک
موجودہ ٹرن ختم نہ ہو جائے، پھر قطار میں موجود پے لوڈز کے ساتھ نیا ایجنٹ ٹرن شروع ہوتا ہے۔ موڈ + debounce/cap رویّے کے لیے
[Queue](/concepts/queue) دیکھیں۔

Block streaming مکمل شدہ اسسٹنٹ بلاکس کو مکمل ہوتے ہی بھیجتا ہے؛ یہ
**بطورِ طے شدہ بند** ہے (`agents.defaults.blockStreamingDefault: "off"`)۔
حدود کو `agents.defaults.blockStreamingBreak` کے ذریعے ٹیون کریں (`text_end` بمقابلہ `message_end`; ڈیفالٹ text_end)۔
نرم بلاک چنکنگ کو `agents.defaults.blockStreamingChunk` کے ذریعے کنٹرول کریں (ڈیفالٹ
800–1200 حروف؛ پیراگراف بریکس کو ترجیح، پھر نئی لائنز؛ جملے آخر میں)۔
اسٹریمنگ چنکس کو `agents.defaults.blockStreamingCoalesce` کے ساتھ یکجا کریں تاکہ
سنگل لائن اسپام کم ہو (بھیجنے سے پہلے idle-based merging)۔ غیر-Telegram چینلز کے لیے
block replies فعال کرنے کو واضح طور پر `*.blockStreaming: true` درکار ہے۔
تفصیلی ٹول خلاصے ٹول کے آغاز پر خارج کیے جاتے ہیں (کوئی debounce نہیں)؛ Control UI
جب دستیاب ہو تو ایجنٹ ایونٹس کے ذریعے ٹول آؤٹ پٹ اسٹریم کرتا ہے۔
مزید تفصیلات: [Streaming + chunking](/concepts/streaming)۔

## Model refs

کنفیگ میں Model refs (مثال کے طور پر `agents.defaults.model` اور `agents.defaults.models`) کو **پہلے** `/` پر اسپلٹ کر کے پارس کیا جاتا ہے۔

- ماڈلز کنفیگر کرتے وقت `provider/model` استعمال کریں۔
- اگر خود ماڈل ID میں `/` شامل ہو (OpenRouter طرز)، تو فراہم کنندہ کا prefix شامل کریں (مثال: `openrouter/moonshotai/kimi-k2`)۔
- اگر آپ فراہم کنندہ چھوڑ دیں، تو OpenClaw ان پٹ کو ایک عرف یا **ڈیفالٹ فراہم کنندہ** کے لیے ماڈل سمجھتا ہے (یہ صرف تب کام کرتا ہے جب ماڈل ID میں `/` موجود نہ ہو)۔

## Configuration (minimal)

کم از کم، یہ سیٹ کریں:

- `agents.defaults.workspace`
- `channels.whatsapp.allowFrom` (سختی سے سفارش کردہ)

---

_اگلا: [Group Chats](/channels/group-messages)_ 🦞
