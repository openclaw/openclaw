---
summary: "Referencia CLI para `openclaw uninstall` (eliminar servicio gateway + datos locales)"
read_when:
  - Quieres eliminar el servicio gateway y/o estado local
  - Quieres una ejecución de prueba primero
title: "uninstall"
---

# `openclaw uninstall`

Desinstalar el servicio gateway + datos locales (CLI permanece).

```bash
openclaw uninstall
openclaw uninstall --all --yes
openclaw uninstall --dry-run
```
