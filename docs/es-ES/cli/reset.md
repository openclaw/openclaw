---
summary: "Referencia CLI para `openclaw reset` (restablecer estado/configuración local)"
read_when:
  - Quieres limpiar el estado local manteniendo el CLI instalado
  - Quieres una ejecución en seco de lo que se eliminaría
title: "reset"
---

# `openclaw reset`

Restablecer configuración/estado local (mantiene el CLI instalado).

```bash
openclaw reset
openclaw reset --dry-run
openclaw reset --scope config+creds+sessions --yes --non-interactive
```
