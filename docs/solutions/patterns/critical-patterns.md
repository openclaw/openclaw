# Critical Patterns

Patterns that must be followed every time — non-obvious solutions that have burned us before.

No critical patterns yet. Add here via `/ce:compound` → Option 2 (Add to Required Reading), or manually when the pattern is clear.
