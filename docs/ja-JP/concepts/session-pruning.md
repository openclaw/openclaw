---
title: "セッションプルーニング"
summary: "セッションプルーニング: コンテキストの肥大化を削減するツール結果のトリミング"
read_when:
  - ツール出力による LLM コンテキストの増加を削減したいとき
  - agents.defaults.contextPruning を調整しているとき
---

# セッションプルーニング

セッションプルーニングは、各 LLM 呼び出しの直前にインメモリコンテキストから**古いツール結果**をトリミングします。ディスク上のセッション履歴（`*.jsonl`）は書き換えません。

## 実行タイミング

- `mode: "cache-ttl"` が有効で、セッションの最後の Anthropic 呼び出しが `ttl` より古い場合に実行されます。
- そのリクエストでモデルに送信されるメッセージにのみ影響します。
- Anthropic API 呼び出し（および OpenRouter の Anthropic モデル）にのみ有効です。
- 最良の結果を得るには、`ttl` をモデルの `cacheRetention` ポリシー（`short` = 5 分、`long` = 1 時間）に合わせてください。
- プルーニング後、TTL ウィンドウがリセットされるため、`ttl` が再び切れるまで後続のリクエストはキャッシュを維持します。

## スマートデフォルト（Anthropic）

- **OAuth または setup-token** プロフィール: `cache-ttl` プルーニングを有効にし、ハートビートを `1h` に設定します。
- **API キー** プロフィール: `cache-ttl` プルーニングを有効にし、ハートビートを `30m` に設定し、Anthropic モデルのデフォルト `cacheRetention: "short"` を設定します。
- これらの値を明示的に設定した場合、OpenClaw は**オーバーライドしません**。

## 改善されること（コストとキャッシュ動作）

- **プルーニングの理由:** Anthropic プロンプトキャッシングは TTL 内でのみ適用されます。セッションが TTL を超えてアイドル状態になった場合、最初に切り詰めないと次のリクエストでフルプロンプトが再キャッシュされます。
- **コストが削減されるもの:** プルーニングは TTL 切れ後の最初のリクエストの **cacheWrite** サイズを削減します。
- **TTL リセットが重要な理由:** プルーニングが実行されると、キャッシュウィンドウがリセットされるため、後続のリクエストは履歴全体を再キャッシュせずに新しくキャッシュされたプロンプトを再利用できます。
- **行わないこと:** プルーニングはトークンを追加したりコストを「倍増」させたりしません。TTL 切れ後の最初のリクエストでキャッシュされるものを変更するだけです。

## プルーニング対象

- `toolResult` メッセージのみ対象です。
- ユーザーとアシスタントのメッセージは**変更されません**。
- 最後の `keepLastAssistants` アシスタントメッセージは保護され、その後のツール結果はプルーニングされません。
- カットオフを設定するのに十分なアシスタントメッセージがない場合、プルーニングはスキップされます。
- **画像ブロック**を含むツール結果はスキップされます（トリミング/クリアされません）。

## コンテキストウィンドウの推定

プルーニングは推定されたコンテキストウィンドウ（文字数 ≈ トークン × 4）を使用します。ベースウィンドウは以下の順序で解決されます。

1. `models.providers.*.models[].contextWindow` オーバーライド。
2. モデル定義の `contextWindow`（モデルレジストリから）。
3. デフォルト `200000` トークン。

`agents.defaults.contextTokens` が設定されている場合、解決されたウィンドウの上限（最小値）として扱われます。

## モード

### cache-ttl

- 最後の Anthropic 呼び出しが `ttl` より古い場合のみプルーニングが実行されます（デフォルト `5m`）。
- 実行時: ソフトトリムとハードクリアの動作は以前と同じです。

## ソフトプルーニング vs ハードプルーニング

- **ソフトトリム**: サイズが大きすぎるツール結果のみ対象。
  - 先頭と末尾を保持し、`...` を挿入して元のサイズのメモを追加します。
  - 画像ブロックを含む結果はスキップします。
- **ハードクリア**: ツール結果全体を `hardClear.placeholder` で置き換えます。

## ツールの選択

- `tools.allow` / `tools.deny` は `*` ワイルドカードをサポートします。
- deny が優先されます。
- マッチングは大文字小文字を区別しません。
- 空の allow リスト => すべてのツールが許可されます。

## 他の制限との相互作用

- 組み込みツールはすでに自身の出力を切り詰めます。セッションプルーニングはさらなるレイヤーとして、長期間実行されるチャットがモデルコンテキストにツール出力を過剰に蓄積することを防ぎます。
- コンパクションとは別です: コンパクションは要約して永続化し、プルーニングはリクエストごとに一時的です。[/concepts/compaction](/concepts/compaction)を参照してください。

## デフォルト（有効時）

- `ttl`: `"5m"`
- `keepLastAssistants`: `3`
- `softTrimRatio`: `0.3`
- `hardClearRatio`: `0.5`
- `minPrunableToolChars`: `50000`
- `softTrim`: `{ maxChars: 4000, headChars: 1500, tailChars: 1500 }`
- `hardClear`: `{ enabled: true, placeholder: "[Old tool result content cleared]" }`

## 例

デフォルト（オフ）:

```json5
{
  agents: { defaults: { contextPruning: { mode: "off" } } },
}
```

TTL 対応プルーニングを有効にする:

```json5
{
  agents: { defaults: { contextPruning: { mode: "cache-ttl", ttl: "5m" } } },
}
```

プルーニングを特定のツールに制限する:

```json5
{
  agents: {
    defaults: {
      contextPruning: {
        mode: "cache-ttl",
        tools: { allow: ["exec", "read"], deny: ["*image*"] },
      },
    },
  },
}
```

設定リファレンスを参照: [Gateway 設定](/gateway/configuration)
