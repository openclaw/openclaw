---
summary: "`openclaw uninstall` の CLI リファレンス（Gateway サービス + ローカルデータの削除）"
read_when:
  - Gateway サービスやローカルステートの削除
  - 先にドライランを確認したい場合
title: "uninstall"
---

# `openclaw uninstall`

Gateway サービス + ローカルデータをアンインストールします（CLI は残ります）。

```bash
openclaw uninstall
openclaw uninstall --all --yes
openclaw uninstall --dry-run
```
