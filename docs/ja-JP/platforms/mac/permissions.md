---
summary: "macOSのパーミッション永続化（TCC）と署名要件"
read_when:
  - 欠落またはスタックしたmacOSパーミッションプロンプトのデバッグ
  - macOSアプリのパッケージングまたは署名
  - バンドルIDまたはアプリのインストールパスの変更
title: "macOSパーミッション"
---

# macOSパーミッション（TCC）

macOSのパーミッション付与は脆弱です。TCCはパーミッション付与をアプリのコード署名、バンドル識別子、およびディスク上のパスに関連付けます。これらのいずれかが変更されると、macOSはアプリを新しいものとして扱い、プロンプトを破棄または非表示にする場合があります。

## 安定したパーミッションの要件

- 同じパス：固定の場所からアプリを実行します（OpenClawの場合は`dist/OpenClaw.app`）。
- 同じバンドル識別子：バンドルIDを変更すると新しいパーミッションIDが作成されます。
- 署名済みアプリ：未署名またはアドホック署名のビルドではパーミッションが永続化しません。
- 一貫した署名：リビルド間で署名が安定するように、実際のApple DevelopmentまたはDeveloper ID証明書を使用してください。

アドホック署名はビルドごとに新しいIDを生成します。macOSは以前の付与を忘れ、古いエントリがクリアされるまでプロンプトが完全に消える場合があります。

## プロンプトが消えた場合の復旧チェックリスト

1. アプリを終了します。
2. システム設定 → プライバシーとセキュリティでアプリのエントリを削除します。
3. 同じパスからアプリを再起動し、パーミッションを再付与します。
4. プロンプトがまだ表示されない場合は、`tccutil`でTCCエントリをリセットして再試行してください。
5. 一部のパーミッションはmacOSの完全な再起動後にのみ再表示されます。

リセットの例（必要に応じてバンドルIDを置き換えてください）：

```bash
sudo tccutil reset Accessibility ai.openclaw.mac
sudo tccutil reset ScreenCapture ai.openclaw.mac
sudo tccutil reset AppleEvents
```

## ファイルとフォルダのパーミッション（デスクトップ/書類/ダウンロード）

macOSはターミナル/バックグラウンドプロセスに対してデスクトップ、書類、ダウンロードへのアクセスもゲートする場合があります。ファイルの読み取りやディレクトリの一覧表示がハングする場合は、ファイル操作を実行する同じプロセスコンテキスト（例：Terminal/iTerm、LaunchAgentで起動されたアプリ、SSHプロセス）にアクセスを付与してください。

回避策：フォルダごとの付与を避けたい場合は、ファイルをOpenClawワークスペース（`~/.openclaw/workspace`）に移動してください。

パーミッションをテストする場合は、常に実際の証明書で署名してください。アドホックビルドは、パーミッションが重要でない短時間のローカル実行にのみ許容されます。
