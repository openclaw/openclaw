# Гид по AI-инструментам для разработки: 8 инструментов в деталях

> Последнее обновление: февраль 2026

---

## 1. Spec Kit (GitHub) — 66.8K ⭐

### A. Детальное описание

**Что делает:** Spec Kit — официальный open-source тулкит от GitHub для **Spec-Driven Development (SDD)**. Идея: спецификации становятся исполняемыми — вместо того чтобы писать код и потом документировать, ты описываешь *что* хочешь, а AI-агент генерирует рабочую реализацию по спецификации.

Spec Kit — это CLI (`specify`) + набор slash-команд для AI-агентов. Workflow: создаёшь «конституцию» проекта (принципы), пишешь спецификацию на человеческом языке, получаешь техплан, дробишь на задачи, выполняешь.

**Какую проблему решает:** Борется с "vibe coding" — когда AI-агент кодит без чёткого плана и производит непредсказуемые результаты. Spec Kit заставляет сначала думать, потом кодить.

**Архитектура:** Python CLI (`specify-cli`), ставится через `uv`. Генерирует файлы в `.speckit/` директории. Работает через slash-команды внутри любого поддерживаемого AI-агента (Claude Code, Cursor, Copilot, Gemini CLI, Codex, Windsurf, и 15+ других). Агент-агностичный — не привязан к конкретной LLM.

### B. Практическое использование

**Начало работы:**
```bash
uv tool install specify-cli --from git+https://github.com/github/spec-kit.git
specify init my-saas-bot --ai claude
```

**Workflow для SaaS-бота:**
1. `/speckit.constitution` — задаёшь принципы (качество кода, тесты, UX)
2. `/speckit.specify` — описываешь бота: "Telegram-бот для управления подписками с Stripe-интеграцией..."
3. `/speckit.plan` — указываешь стек: "Node.js, TypeScript, PostgreSQL, Telegram Bot API"
4. `/speckit.tasks` — система дробит план на задачи
5. `/speckit.implement` — AI выполняет задачи по плану

**Что нужно:** Python + uv, любой поддерживаемый AI-агент.

### C. Плюсы и минусы

**Сильные стороны:**
- Backing от GitHub — зрелая документация, активная разработка
- Агент-агностичный (15+ агентов)
- Структурирует процесс от идеи до кода
- 66.8K звёзд — огромное коммьюнити

**Слабые стороны:**
- Может ощущаться как «enterprise-церемония» для соло-разработчика
- Нет встроенной анти-галлюцинации — полагается на качество спеки
- Не управляет параллельными агентами

**Зрелость:** Высокая. Поддержка GitHub, регулярные релизы, обширная экосистема.

---

## 2. Superpowers (obra) — 41.8K ⭐

### A. Детальное описание

**Что делает:** Полный development workflow для coding-агентов, построенный на «скиллах» — composable инструкциях, которые автоматически активируются в нужный момент. Агент **не прыгает в код сразу** — сначала brainstorm, потом план, потом TDD-имплементация через субагентов.

**Какую проблему решает:** Агенты без контроля пишут непредсказуемый код, пропускают тесты, не следуют плану. Superpowers навязывает дисциплину: RED-GREEN-REFACTOR TDD, субагентная разработка с двухэтапным ревью, YAGNI/DRY.

**Архитектура:** Плагин для Claude Code (через marketplace), адаптации для Codex и OpenCode. Набор `.md` скиллов в директории `skills/`. Каждый скилл — это инструкция, которая автоматически активируется при определённых условиях. Ключевая фича — **subagent-driven development**: каждая задача выполняется свежим субагентом с полным 200K контекстом, потом проходит 2-stage review.

### B. Практическое использование

**Начало работы (Claude Code):**
```bash
/plugin marketplace add obra/superpowers-marketplace
/plugin install superpowers@superpowers-marketplace
```

**Workflow для SaaS-бота:**
1. Запускаешь Claude Code, описываешь идею бота
2. Superpowers автоматически запускает **brainstorming** — задаёт вопросы, уточняет
3. После одобрения дизайна — создаёт **git worktree** на отдельной ветке
4. **writing-plans** — дробит на задачи по 2-5 минут каждая
5. **subagent-driven-development** — субагенты выполняют задачи, основной агент ревьюит
6. TDD enforced: тест → провал → код → зелёный → коммит

**Что нужно:** Claude Code (основной), Codex или OpenCode (экспериментально).

### C. Плюсы и минусы

**Сильные стороны:**
- Автоматические скиллы — не нужно помнить команды
- TDD из коробки — высокое качество кода
- Субагенты с 200K контекстом — не деградирует на больших задачах
- Git worktrees — изоляция работы
- Автономная работа часами без девиации

**Слабые стороны:**
- Привязан к Claude Code (Codex/OpenCode — экспериментально)
- Сложно кастомизировать без понимания системы скиллов
- Нет визуального трекинга задач

**Зрелость:** Средне-высокая. Активная разработка, 41.8K звёзд, автор Jesse Vincent (известный open-source разработчик).

---

## 3. Taskmaster AI — 25.2K ⭐

### A. Детальное описание

**Что делает:** Система управления задачами для AI-driven разработки. Работает как MCP-сервер — интегрируется в Cursor, VS Code, Claude Code и другие редакторы. Парсит PRD (Product Requirements Document) в структурированные задачи с зависимостями, подзадачами, тегами, статусами.

**Какую проблему решает:** AI-агенты в IDE не знают, что делать дальше. Taskmaster даёт им структурированный бэклог задач с приоритетами и зависимостями. Также поддерживает research-модель (Perplexity) для получения свежей информации.

**Архитектура:** Node.js MCP-сервер (`npx task-master-ai`). Данные хранятся в `.taskmaster/`. Поддерживает 3 типа моделей: main (генерация), research (свежие данные), fallback. Мультипровайдерный: Anthropic, OpenAI, Google, Perplexity, xAI, Groq, OpenRouter, Azure, Ollama. 36 MCP-инструментов (можно сократить до 7 core для экономии токенов).

### B. Практическое использование

**Начало работы (Claude Code):**
```bash
claude mcp add taskmaster-ai -- npx -y task-master-ai
```

**Workflow для SaaS-бота:**
1. Пишешь PRD в `.taskmaster/docs/prd.txt` — описание бота, фичи, API
2. "Parse my PRD" — Taskmaster создаёт задачи с зависимостями
3. "What's the next task?" — показывает оптимальную следующую задачу
4. "Implement task 3" — агент выполняет конкретную задачу
5. "Expand task 4" — разбивает сложную задачу на подзадачи
6. "Research JWT best practices for Node.js" — использует research-модель

**Что нужно:** Node.js, минимум 1 API-ключ (или Claude Code CLI).

### C. Плюсы и минусы

**Сильные стороны:**
- Лучшая интеграция с IDE через MCP
- Мультипровайдерность (10+ провайдеров)
- Research-модель для свежих данных
- Гибкая загрузка инструментов (core/standard/all)
- Хорошая документация

**Слабые стороны:**
- Лицензия MIT with Commons Clause (не полностью open-source)
- Не управляет кодом — только задачами
- Нет TDD/review enforcement
- Требует API-ключи (стоит денег)

**Зрелость:** Высокая. npm-пакет, CI, обширная документация, Discord-коммьюнити.

---

## 4. Repomix — 21.6K ⭐

### A. Детальное описание

**Что делает:** Упаковывает весь репозиторий в один AI-friendly файл. Это утилита подготовки контекста — берёт кодовую базу и превращает её в XML/Markdown/text файл, оптимизированный для LLM. Считает токены, уважает `.gitignore`, проверяет секреты через Secretlint, поддерживает сжатие через Tree-sitter.

**Какую проблему решает:** LLM нужен контекст. Копировать файлы по одному — мучение. Repomix решает проблему «как скормить весь проект AI» одной командой.

**Архитектура:** Node.js CLI + веб-версия (repomix.com) + Chrome/Firefox расширение + VS Code расширение. Поддерживает: упаковку локальных репо, удалённых GitHub-репо (по URL), stdin-пайпы, сжатие через Tree-sitter, git-логи.

### B. Практическое использование

**Начало работы:**
```bash
npx repomix@latest
# или
brew install repomix
```

**Workflow для SaaS-бота:**
```bash
# Упаковать весь проект
repomix

# Только backend
repomix src/api --include "**/*.ts"

# Сжатый вариант (меньше токенов)
repomix --compress

# Удалённый репо для изучения
repomix --remote yamadashy/repomix

# Через stdin — только изменённые файлы
git diff --name-only | repomix --stdin
```

Затем отправляешь `repomix-output.xml` в ChatGPT/Claude/etc. с промптом "Review this codebase".

**Что нужно:** Node.js (или браузер для веб-версии).

### C. Плюсы и минусы

**Сильные стороны:**
- Один файл = весь контекст для LLM
- Tree-sitter сжатие экономит токены
- Secretlint — защита от утечки ключей
- Веб-версия + расширения — доступность
- Git-aware (respects .gitignore)

**Слабые стороны:**
- Утилита, не workflow — нужно комбинировать с другими
- Большие проекты всё равно не влезут в контекст
- Не для агентов — скорее для chat-based LLM

**Зрелость:** Высокая. npm, Homebrew, Docker, расширения, спонсоры, активное коммьюнити.

---

## 5. Serena — 19.5K ⭐

### A. Детальное описание

**Что делает:** Coding agent toolkit, который даёт LLM **IDE-подобные инструменты** на уровне символов кода. Вместо чтения целых файлов и grep-поиска, агент использует `find_symbol`, `find_referencing_symbols`, `insert_after_symbol` — семантические операции через Language Server Protocol (LSP) или JetBrains плагин.

**Какую проблему решает:** Даже мощные агенты вроде Claude Code работают с файлами целиком — это дорого по токенам и неточно на больших кодовых базах. Serena даёт точечный доступ к коду на уровне символов, что радикально снижает расход токенов и повышает точность редактирования.

**Архитектура:** Python (uv), MCP-сервер. Два бэкенда:
1. **LSP** — open-source language servers для 30+ языков (Python, TypeScript, Java, Rust, Go, C#, и т.д.)
2. **JetBrains Plugin** — использует мощь IDE (IntelliJ, PyCharm, WebStorm, etc.)

Интегрируется с: Claude Code, Claude Desktop, Codex, Gemini CLI, Cursor, VS Code, Cline, Roo Code, OpenWebUI, ChatGPT (через mcpo).

### B. Практическое использование

**Начало работы:**
```bash
# Установка uv (если нет)
curl -LsSf https://astral.sh/uv/install.sh | sh

# Запуск MCP-сервера
uvx --from git+https://github.com/oraios/serena serena start-mcp-server --help

# Для Claude Code
claude mcp add serena -- uvx --from git+https://github.com/oraios/serena serena start-mcp-server /path/to/project
```

**Workflow для SaaS-бота:**
Serena не управляет задачами — она усиливает любого агента:
- Вместо `cat src/bot/handlers.ts` → `find_symbol TelegramHandler`
- Вместо `grep -r "stripe"` → `find_referencing_symbols StripeService`
- Вместо string-replace → `insert_after_symbol handlePayment`

На большом проекте (1000+ файлов) это экономит 50-80% токенов и даёт более точные правки.

**Что нужно:** Python + uv, language server для вашего языка (или JetBrains IDE).

### C. Плюсы и минусы

**Сильные стороны:**
- Радикальная экономия токенов на больших проектах
- 30+ языков через LSP
- JetBrains плагин — мощный бэкенд
- Совместим с любым MCP-клиентом
- Спонсируется Microsoft/VS Code
- Пользователи описывают как "game changer"

**Слабые стороны:**
- Малополезна для маленьких проектов / новых проектов с нуля
- Требует настройки language server
- Не даёт workflow — только инструменты
- Python-зависимость

**Зрелость:** Средне-высокая. Активная разработка, спонсорство Microsoft, растущее коммьюнити.

---

## 6. Claude Squad — 5.9K ⭐

### A. Детальное описание

**Что делает:** Terminal-приложение для управления **несколькими экземплярами** Claude Code (а также Codex, Gemini, Aider) одновременно. Каждый экземпляр работает в изолированном workspace (git worktree) на своей ветке. TUI-интерфейс позволяет переключаться между сессиями, просматривать diff, пушить ветки.

**Какую проблему решает:** Один Claude Code = одна задача. Claude Squad позволяет запустить 5-10 агентов параллельно, каждый работает над своей фичей/багфиксом без конфликтов.

**Архитектура:** Go (бинарник `cs`). Использует:
- **tmux** — изолированные терминальные сессии
- **git worktrees** — изолированные кодовые базы
- TUI-интерфейс для навигации

### B. Практическое использование

**Начало работы:**
```bash
brew install claude-squad
# или
curl -fsSL https://raw.githubusercontent.com/smtg-ai/claude-squad/main/install.sh | bash
```

**Workflow для SaaS-бота:**
```bash
cs                    # Запуск TUI
# n — новая сессия "Implement Stripe webhook handler"
# n — новая сессия "Add user registration flow"
# n — новая сессия "Setup CI/CD pipeline"
# N — новая сессия с промптом

# Переключение: ↑↓
# Просмотр diff: Tab
# Автоматический режим: cs -y (auto-accept)
# Push: s (коммит + push)
```

Три агента работают параллельно, каждый на своей ветке. Ты ревьюишь diff, мержишь когда готово.

**Что нужно:** Go-бинарник, tmux, gh CLI, Claude Code (или Codex/Aider/Gemini).

### C. Плюсы и минусы

**Сильные стороны:**
- Настоящая параллельная работа — 5-10 агентов одновременно
- Git worktree изоляция — никаких конфликтов
- Простой TUI — минимум обучения
- Поддержка разных агентов (Claude, Codex, Aider, Gemini)
- Auto-accept режим (yolo mode)

**Слабые стороны:**
- Лицензия AGPL-3.0
- Требует tmux
- Нет координации между агентами (каждый сам по себе)
- Нет планирования — только параллелизация
- Может быть дорого (несколько API-сессий параллельно)

**Зрелость:** Средняя. Активная разработка, растущее коммьюнити, но проект молодой.

---

## 7. CLEO — ~1K ⭐

### A. Детальное описание

**Что делает:** Task management система, спроектированная **agent-first**. Все операции возвращают JSON по умолчанию, с `--human` флагом для людей. 4 уровня анти-галлюцинации: schema validation, semantic validation, cross-file integrity, state machine transitions.

**Какую проблему решает:** Агенты галлюцинируют ID задач, теряют контекст между сессиями, нуждаются в структурированном выводе. CLEO — это контракт между разработчиком и AI-агентом.

**Архитектура:** Pure Bash CLI. Данные в `todo.json` + archive. Стабильные плоские ID (T001, T042...). Скиллы через YAML. Immutable audit trails.

### B. Практическое использование

```bash
curl -fsSL https://github.com/kryptobaseddev/cleo/releases/latest/download/install.sh | bash
cd /path/to/project && cleo init

cleo add "Implement Stripe webhook" --priority high
cleo list                    # JSON для агента
cleo list --human            # Читаемый формат
cleo exists T042 --quiet && echo "Found"
```

### C. Плюсы и минусы

**Сильные:** Agent-first дизайн, 4-уровневая анти-галлюцинация, стабильные ID, zero-dependency (bash).
**Слабые:** Ограниченная экосистема, BSL-1.1 лицензия, нет MCP-интеграции, нишевый.
**Зрелость:** Ранняя. Работает, но коммьюнити маленькое.

---

## 8. GSD (Get Shit Done) — ~10K ⭐

### A. Детальное описание

**Что делает:** Лёгкая мета-промптинговая система для Claude Code от TÂCHES. Противоположность enterprise-подхода: минимум церемоний, максимум эффективности. Context engineering + XML prompt formatting + subagent orchestration + state management — всё спрятано за простыми командами.

**Какую проблему решает:** "Я соло-разработчик, я не хочу играть в enterprise-театр. Мне не нужны спринты, story points и ретро. Мне нужно чтобы Claude Code просто делал работу."

**Архитектура:** npm-пакет, устанавливает `.claude/` команды. Файлы: PROJECT.md, ROADMAP.md, STATE.md (persistent memory), PLAN.md (XML-задачи), SUMMARY.md. Субагенты получают свежий 200K контекст на каждую задачу.

### B. Практическое использование

```bash
npx get-shit-done-cc

# Новый проект
/gsd:new-project          # Интервью: что строим?
/gsd:create-roadmap       # ROADMAP.md + STATE.md
/gsd:plan-phase 1         # Атомарные задачи
/gsd:execute-plan         # Субагент делает работу

# Существующий проект
/gsd:map-codebase         # Анализ через параллельные агенты
/gsd:new-project          # Описание фичи (уже знает кодовую базу)

# Итерации
/gsd:complete-milestone   # Архив v1, начало v2
/gsd:add-phase            # Новая работа
```

### C. Плюсы и минусы

**Сильные:** Минималистичный, XML-промптинг для Claude, субагенты с полным контекстом, brownfield-поддержка, MIT.
**Слабые:** Только Claude Code, нет анти-галлюцинации, качество зависит от описания проекта.
**Зрелость:** Средняя. Активная разработка, хорошие отзывы от соло-разработчиков.

---

## Сравнительная таблица

| Критерий | Spec Kit | Superpowers | Taskmaster | Repomix | Serena | Claude Squad | CLEO | GSD |
|----------|----------|-------------|------------|---------|--------|-------------|------|-----|
| **Фокус** | Full cycle SDD | Full cycle + TDD | Task management | Контекст | Контекст (семантический) | Параллельная работа | Task mgmt (agent-first) | Full cycle (lean) |
| **Подход к контексту** | Спецификация + план | Скиллы + дизайн-доки | PRD → задачи | Весь репо → 1 файл | LSP-символы | Изоляция (worktrees) | JSON + audit trail | PROJECT.md + STATE.md |
| **Мульти-агентность** | ❌ | ✅ (субагенты) | ❌ | ❌ | ❌ | ✅ (параллельные) | ❌ | ✅ (субагенты) |
| **Анти-галлюцинация** | Через спецификацию | TDD + review | Через структуру | ❌ (утилита) | Через LSP-точность | ❌ | ✅✅✅ (4 уровня) | Через verification steps |
| **Сложность освоения** | 🟡 Средняя | 🟢 Лёгкая (auto) | 🟡 Средняя | 🟢 Лёгкая | 🟡 Средняя | 🟢 Лёгкая | 🟡 Средняя | 🟢 Лёгкая |
| **Совместимость** | 15+ агентов | Claude Code (+ Codex) | Cursor, VS Code, Claude Code + 5 | Любая LLM | Любой MCP-клиент | Claude, Codex, Aider, Gemini | Claude Code | Claude Code |
| **Лицензия** | MIT (?) | MIT | MIT + Commons Clause | MIT | MIT | AGPL-3.0 | BSL-1.1 | MIT |
| **Зрелость** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐ |

---

## Как комбинировать

### Инструменты, которые хорошо работают вместе

| Комбинация | Зачем |
|------------|-------|
| **Serena + Claude Code** | Точный семантический доступ к коду на больших проектах |
| **GSD + Serena** | Lean workflow + эффективная навигация по коду |
| **Superpowers + Claude Squad** | TDD-workflow для каждого из параллельных агентов |
| **Spec Kit + Taskmaster** | Спека через Spec Kit → задачи через Taskmaster |
| **Repomix + ChatGPT/Claude web** | Быстрый review/анализ кода без CLI |
| **Claude Squad + GSD/Superpowers** | Параллельные агенты, каждый с собственным workflow |

### Рекомендованный стек для соло-разработчика (Claude Code + Moltbot)

**Уровень 1 — Начни с этого (день 1):**
- **GSD** — минимальная церемония, максимум продуктивности. `npx get-shit-done-cc` и работай.

**Уровень 2 — Когда проект вырос (неделя 2):**
- **Serena** — добавь MCP-сервер для семантической навигации по растущему коду. Экономия токенов = экономия денег.

**Уровень 3 — Когда нужна параллельность (неделя 3+):**
- **Claude Squad** — запускай 3-5 агентов параллельно для разных фич.

**Уровень 4 — Для серьёзных проектов:**
- **Superpowers** — замени GSD на Superpowers для полного TDD-цикла с двухэтапным ревью.
- **Spec Kit** — для проектов с несколькими участниками или когда нужна формальная спецификация.

### Что НЕ нужно комбинировать

- **Spec Kit + GSD + Superpowers** одновременно — три workflow-системы будут конфликтовать. Выбери одну.
- **CLEO + Taskmaster** — обе управляют задачами, но по-разному. Выбери одну.
- **Repomix + Serena** — решают одну проблему (контекст) разными путями. Serena для агентов, Repomix для chat.

---

## TL;DR — Что выбрать?

| Я хочу... | Бери... |
|-----------|---------|
| Просто начать кодить с Claude Code | **GSD** |
| Максимальное качество кода + TDD | **Superpowers** |
| Параллельно 5 задач | **Claude Squad** |
| Скормить код ChatGPT | **Repomix** |
| Экономить токены на большом проекте | **Serena** |
| Формальный процесс для команды | **Spec Kit** |
| Управление задачами в IDE | **Taskmaster** |
| Agent-first task tracking | **CLEO** |
